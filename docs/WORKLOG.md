# WORKLOG — current execution evidence

Last updated: 2026-09-26

Historical evidence is immutable and is not current task state. Current state is generated from `docs/agent/status.json` and `docs/agent/tasks.json`.

## 2026-09-09 — Maintainability baseline

Jay approved the full maintainability plan: isolated local testing, repository-owned task state, scoped authorization, separate verified batches and Articles refactoring without UI/data-contract changes. The implementation uses a clean worktree based on main 2e7977b; unrelated local edits are preserved. No production data mutation or external email is authorized by this maintenance task.

## 2026-09-09 — State/archive batch

Root instructions are 3,558 bytes; default startup documents total 11,353 bytes at this checkpoint. Completed tasks and the previous WORKLOG are archived without deleting records; archive hashes reproduce the complete original WORKLOG. Current CMS handoff stays revalidation_required. Generated HANDOFF/NEXT_STEPS/README and read-only GC pass; injected invalid task/CMS/Preview and stale summary cases are rejected. Initial clean Node 20 container gate passed, followed by a final repeat after this evidence and architecture-index cleanup. External branch protection and isolated local stack remain pending in the full maintenance task.

## Historical evidence index

The concatenated archive parts reproduce the previous WORKLOG byte-for-byte. Integrity is checked against `docs/archive/2026-09-09/worklog-index.json`.

- [worklog-01.md](archive/2026-09-09/worklog-01.md): original lines 1–1777.
- [worklog-02.md](archive/2026-09-09/worklog-02.md): original lines 1778–3547.
- [worklog-03.md](archive/2026-09-09/worklog-03.md): original lines 3548–5346.
- [worklog-04.md](archive/2026-09-09/worklog-04.md): original lines 5347–7113.
- [worklog-05.md](archive/2026-09-09/worklog-05.md): original lines 7114–8897.
- [worklog-06.md](archive/2026-09-09/worklog-06.md): original lines 8898–10688.
- [worklog-07.md](archive/2026-09-09/worklog-07.md): original lines 10689–12471.
- [worklog-08.md](archive/2026-09-09/worklog-08.md): original lines 12472–12615.

<a id="phase-0-tagged-media-role-boundary-proof"></a>
Original evidence: [phase-0-tagged-media-role-boundary-proof](archive/2026-09-09/worklog-01.md#phase-0-tagged-media-role-boundary-proof).

## 2026-09-09 — Maintenance state release and verification graph

Scope: approved maintainability plan batches 1–2; no UI changes, production data writes, external email or DNS changes.

- PR #40 (`01b284886f2d4233a23e74aa86c3b38d6a154022`) merged as `709af75165b366999ee24ef24c666e8243b6aa56`. CI run `34323595831` and immutable Preview `https://7a83ed7d.urblo-site.pages.dev` passed; production CI `34323951402` deployed `https://78bdc657.urblo-site.pages.dev`. Immutable, apex and www each passed bound route/asset/MIME/redirect/Function smoke.
- First PR CI run `34323166603` failed on a stale HANDOFF phrase guard. The guard now validates the structured Published-overlay/static-fallback state, with a mutation test; full existing public runtime checks remained. Local Admin predeploy and clean Node 20 gate passed after repair.
- `agent:verify` selects a dependency graph from Git rename/delete/untracked-aware changes. The 22-check source graph executes each node once. Build includes TypeScript. Runtime adds the browser config boundary; an already verified env-less build is reused, while configured builds require a separate env-less browser build. Old smoke/predeploy/check commands remain compatible.
- Pure record docs select state/path/harness/classifier checks and no deployment. Tooling selects the complete source graph. Runtime/build/release/unknown paths require deployment and browser checks; migrations have their own category. Actual temporary-Git tests verify rename into docs, deletion, untracked files and fingerprint invalidation.
- Each run saves a unique attempt directory under ignored `.tmp/verification/`, sanitized child output, exit codes, elapsed times, repository SHA and runtime fingerprint. A failed attempt is never overwritten by retry. First local graph run could not bind Vite inside the sandbox; the next uncovered one stale Capabilities smoke-wrapper string. That integration assertion now checks the graph; later full source and runtime/browser runs passed.
- Cloudflare API readback found current `urblo-site` is direct upload, while legacy `urblo` Git integration still built all branches. Within the approved consolidation scope, disabled legacy production and preview automatic builds, read back false/none, and verified its canonical deployment pointer stayed unchanged. No rollback deployment was deleted. API schema: [Cloudflare project settings](https://developers.cloudflare.com/api/resources/pages/subresources/projects/).
- CI now produces stable `quality` on PRs and main, skips deployment for records, automatically smokes immutable Preview and production domains. Wrangler is pinned at 4.130.0; its CLI requires Node 22, while build/container checks remain Node 20. GitHub administrator configuration remains external until `quality` exists and the owner applies protection.

Residual work: verify the new workflow on its own PR; prove a record-only PR has zero deployments; apply main protection with administrator access; local isolated database/journeys and Articles/server refactor remain subsequent batches. Broader CMS acceptance remains unproven.

- PR #41 first CI run `34324917881` passed all 22 source nodes, then stalled in browser process shutdown and was cancelled before deployment. Linux retained the Vite grandchild behind `npx`; the gate now launches Vite directly. The graph streams sanitized logs as checks run, enforces check time budgets and retains interruption evidence so cancellation cannot lose the active check log. CI cancellation correctly made `quality` fail.

## 2026-09-09 — Record-only CI acceptance

PR #41 passed repaired run `34326148845`, including 23 unique verification nodes, immutable Preview `https://84f83fa9.urblo-site.pages.dev` and stable `quality`, then merged as `28cd2a1`. The earlier cancelled run remains recorded above; Linux reproduction confirmed that `npx` left Vite serving after termination while the direct Node launcher stopped it.

This entry deliberately changes only historical evidence. Its PR must run the docs graph and stable `quality`, with dependency installation but no website build, browser installation or Cloudflare deployment. The subsequent main run must also skip deployment. Runtime SHA/URL observations remain unchanged until fresh production readback is recorded; a newer record-only repository SHA does not invalidate an unchanged runtime fingerprint.

## 2026-09-09 — Isolated local QR stack

- PR #41 merge `28cd2a1f4ad1dee293afd7d4f9f5dff3208ccd94` passed production run `34326552945` and immutable/apex/www readback at `https://98b6a225.urblo-site.pages.dev`. PR #42/main `03767bc6bdf5164a6b1644a4100378f9c2fa7ed5` passed runs `34326719464` and `34327122789` with no build/browser/deploy. Both release and later record-only artifacts have runtime fingerprint `03eec5670136b87337c146d89044c364c304b637ae851764041763af415212c9`.
- Pinned Supabase CLI 2.117.0, local Postgres 17, project `urblo-isolated-v1`, API 57321 and app 8788. All wrappers validate exact loopback target, project identity, Docker socket and current checkout, strip inherited provider credentials, and reject linked projects. Actual CLI negative tests reject production before attempting any Docker command.
- Initial fresh migration failed on absent hosted-only `public.rls_auto_enable()`. Conditional revocation preserves the existing helper branch; the local transaction proof confirms anon/authenticated denial and rolls back. No production migration was applied or rerun.
- Initial local login failed because the email provider was disabled; corrected only local config while keeping global registration disabled. A first synthetic PNG failed browser decode; replaced with deterministic valid PNG generation. First QR selector attempt used a label text matcher affected by nested option text; switched to observed accessible combobox role. Separate attempt evidence is retained.
- Full local proof `full-1788941176621` passed guards, start, two resets/reseeds, helper permission proof and QR browser journey. QR proof covers actual private upload/public promotion, Function-created stable link, material Save + refresh, actual Open page navigation, public material rendering, Hide 404, Restore same slug, and anonymous mutation 401. Browser plugin not available; repository-pinned Playwright Chromium used at 1440x1000. Screenshots and safe request metadata are stored per attempt under ignored `.tmp/local/`.
- Runtime CI now runs the isolated proof before deployment and uploads its safe logs/JSON/screenshots. Local test credentials are synthetic; real notification delivery, CMS handoff, Projects/Articles representative coverage and user acceptance are not inferred. Main protection remains the administrator-access dependency already raised with Jay.

## 2026-09-09 — Behavioral coverage batch

PR #43 merged as `810fd9b0eed97fa2a5183b46549db8423700bda5`; Preview run `34328136267` and production run `34328859066` passed, including the isolated local stack. Production immutable `https://31fd0c27.urblo-site.pages.dev`, apex and www passed bound smoke.

Local attempts retain their original result and screenshots. `journeys-1788942412526` passed QR plus Projects save/refresh/private draft/public Publish/Hide and a deterministic delayed-load New regression. `journeys-1788942585567` also passed Articles invalid validation/API failure recovery, parent-bound child Save, switching locks and both form/inbox flows. `journeys-1788942651608` passed additional Articles public rendering, then failed because the test navigated before asynchronous Sign out completed; `journeys-1788942733567` failed only because Sign out correctly retained a next query parameter. The test now waits for the login pathname. Fresh two-reset verification follows before release.

Confirmed Projects bug M13: New during an existing draft fetch invalidated the request generation but never cleared loading. The new-route branch now clears loading while retaining the stale-response guard. Brand/design alignment: no layout, copy, style, publish or role changes; the existing New action becomes usable during an old read.

Functions JS lint is now included. Exact control-character regex suppressions preserve upload/public-URL validation; an unused QR text-response helper is removed. Synthetic Article fixtures now use the actual rich-text body contract. Runtime browser behavior is split into small journey modules. Real notification delivery, production data writes, CMS handoff and user/device acceptance remain outside this proof. Articles and shared server configuration refactors remain next.

Fresh local `full-1788942811006` passed two resets, migration permission proof and all 16 browser journeys (`journeys-1788942870502`), including unprofiled account rejection. No external requests or uncaught page errors were reported. Full lint passed. Initial container invocation stopped at a trailing blank line left by unused-helper removal; corrected before rerunning the gate.

The next container run caught a documentation-path false positive: the retired-archive substring guard rejected the real UI button label New project. The guard now requires a path separator, with positive archive-path and negative UI-prose cases. Runtime checks had passed up to this docs failure; final gate rerun retains the original failed attempt.

Final clean Node 20 container gate passed all 23 unique source checks. The separate configuration-missing browser gate passed all 12 routes. This batch is ready for its independent PR, expanded isolated CI journeys and immutable Preview smoke; release completion is not inferred before those finish.

## 2026-09-09 — Articles and identity refactor

PR #44's behavior baseline passed run `34330297192` and merged as `97e4f284b25616bb9cb8c56229ed0380eb7de9ac`. Its production run `34331232509` passed; the immutable URL will be recorded with the final release observation.

The 2,226-line Articles route is now an 11-line protected wrapper over explicit types, forms/validation, browser-key data access, editor-state/audit and presentation modules. Data columns/order, parent-bound block updates, generation guards, locks, roles, primary-save/audit order and notices remain unchanged. An AST comparison against the committed pre-refactor source matched all 12 presentation functions/returns, including labels, classes and action bindings. TypeScript and module lint passed. Fresh isolated `full-1788943371457` / `journeys-1788943433474` passed all 16 workflows after extraction; shared server source also matched the tested local copy.

QR and Projects now share configuration/client construction and user/active-profile reads in `functions/_lib/admin-runtime.js`. Modules retain their own roles and exact response/error contracts. New handler-level tests cover missing configuration/session with zero network work, invalid identity without profile lookup, missing/profile-error/unknown-role rejection, viewer mutation denial and owner/admin/editor admission to input validation. Existing QR and Projects behavior verifiers passed.

The Articles validation-order string assertion was replaced with the real invalid-Save recovery assertion. Build-only mutation `article-mutation-1788943722411` deliberately takes the save lock before validation and proves that same browser assertion fails; repository source fingerprint remains unchanged. This fault is available only through the guarded local runner and is included after the normal CI journeys. Remaining useful source boundaries are preserved across the explicit module files. Structured browser console arguments now retain useful error details with nested credential/token redaction.

One subsequent full attempt (`full-1788943873996`) stopped before browser work because a new diagnostic import was accidentally placed before the shebang. It was corrected and syntax-checked; retry evidence remains separate. No live business data, Storage objects or outbound emails were touched. Brand/design alignment: presentation structure and public behavior remain unchanged. Follow-up module work and administrator main protection now have their own queue entries.

Final isolated retry `full-1788944053598` passed two resets, all normal workflows and the deliberate mutation proof. The first refactor container run passed build/lint/server behaviors, then the overlay verifier still looked for extracted guards in the old route file. Its three retained checks now target the state, data and form modules respectively; the boundary assertions themselves remain unchanged.

A second container attempt found the same stale-file assumption in the Cloudflare packaging source guard. Its server-only credential/auth checks now include the shared identity module for both handlers. Browser-key prohibitions remain checked across the combined server dependency. The new handler behavior tests independently prove the actual identity/configuration outcomes.

Final clean Node 20 container gate passed all 24 unique source checks. The structured console capture reported the deliberate Article API 500, a harmless unused-preload warning and the existing duplicate GoTrue client/storage-name warning when public/admin clients coexist. Public client source has persistSession=false; no session failure was observed, so namespace behavior is a separate investigation task, not an asserted data-loss bug. PR #44 production immutable is `https://c0006474.urblo-site.pages.dev` and its bound custom-domain readback passed.

The final missing-configuration browser gate passed all 12 routes. Refactor source, synthetic workflows and mutation evidence are ready for the independent Preview/CI release; production readback remains a separate next step.

## 2026-09-10 — Cold-start and closeout verification

A fresh process read only repository startup state and returned checkout/observed-runtime versions, current task, next action, six module entry groups, verification and recorded authorization. Root instructions measure 3,575 bytes; startup files 12,096 bytes plus init output 4,115 bytes = 16,211 bytes. Active state, explicit idle state in a temporary directory without Git/history, invalid/archived task rejection and rejection of idle hiding active work pass. This is a reproducible repository/command handoff exercise, not an independent human study.

Fixed the local doctor's stale Node 20 requirement: local Functions need Node 22+, while source compatibility remains tested in the Node 20 container. All local read-only prerequisite observations were ready. Preview requires a supplied origin and never substitutes production's immutable URL. Root entry now describes the deployed classified CI rather than future migration text. The closeout clean Node 20 gate passed.

GitHub readback still reports admin=false/push=true and main protected=false. The existing authorization covers protection, but the connected identity cannot apply administrator-only settings. The runbook and a separate blocked task identify the exact required PR/quality rule; no new fees, production data changes or external messages were introduced.

PR #45 merged as `8a25265771424bb4680624a25cb3bfaa2564c3ae`. Production run `34445064304` passed all source/browser checks, isolated database/Functions workflows and mutation proof, deployment, immutable `https://deef89f2.urblo-site.pages.dev` smoke and bound apex/www smoke. The maintenance implementation is archived and currentTaskId is explicitly null; candidate work does not become authorized automatically. Final closeout changes affect tooling and documentation only. A browser settings read also found no signed-in administrator session, so main protection remains a documented external blocker.

Final idle-state container rerun found the CMS acceptance guard still required the physical-delete constraint in the active queue after maintenance archival. The existing future CMS task now retains that exact constraint; runtime controls are unchanged. The first launch was denied Docker socket access by the sandbox, then retried with approved local Docker access; neither failed attempt is represented as a passed gate.

## 2026-09-10 — Stone Library workspace candidate and isolated adoption proof

Scope: replaced the three-column table editor with a visual collection and page-shaped editor, serialized automatic private-draft saving, shared `StonePageView` preview, ordered exact finish imagery, paginated private media upload/selection, history and reference-safe publication/hide. Added the protected Function, service-only aggregate RPC and separate browser-write/reference lockdown migrations. Projects/Products/Articles now share published catalogue choices and retain unavailable saved selections. Origin/internal sources remain private; managed hidden keys cannot revive static content.

Local evidence: build/lint/typecheck passed; configured entry is 415.91 kB and the Supabase chunk remains separate. `agent:stone-workspace` passed 13 behavior/security groups including failed-second-copy compensation, definite transaction rollback, uncertain-outcome retention and successful publication. On a disposable Postgres database, both migrations applied and 28 transactional assertions passed: complete draft isolation, stable IDs, receipt replay/conflicts, Viewer denial, reference protections, tombstones, history and grants. A two-session Product-publication/Stone-hide race waited for the first transaction and rejected the hide after the new public reference committed.

The loopback browser fixture uses real local PostgREST/SQL plus synthetic sessions. Browser checks passed create/save/refresh, private upload/preview/publication/public readback, media pagination beyond 160 records, failed-save navigation retention/retry, lost-response replay, multi-editor conflict retention, 1280px layout and mobile no-horizontal-overflow. New Grey hide was rejected in the UI with the linked public Product/Project list. The no-config gate passed all 12 admin routes. Synthetic sessions are not production Auth proof.

Local adoption of exact plan `b9a4e1f1c3fce163793263641139cc69c7620cce3853b2e03ed1a5a114784f9f` passed: 12 baseline stones, 55 photographs, archives 1/13, retained originals/history; resumed readback compared every public variant/finish/image relationship and all 55 published photo hashes. Products/defaults, Articles/blocks, Projects/materials and all 32 Image QR records had unchanged before/after fingerprints. The original production read-only snapshot showed six formal Product drafts, four formal Article drafts and Moon Gate draft; none was discarded or published. Private sample rows were not fetched or changed.

Release inventory and rollback: `docs/STONE_LIBRARY_WORKSPACE_RELEASE.md`; exact machine plan: `docs/agent/stone-library-adoption-plan.json`. No production migration, content/Storage write or runtime promotion occurred. Fresh production workflow proof and Jay's five-minute usability acceptance remain open. The legacy direct-write CRUD verifier now refuses live mode before login/mutation, since it cannot safely exercise the new protected Stone boundary. Gate/Preview closeout follows below.


### Current-main integration and final local verification

Integrated latest main `5cae8f380993f76ee0bba199062165a7259674dd`, preserving maintenance's classified verification graph, generated state, Articles layer split, shared server identity and Image QR material pages. Ported Stone selectors into Articles data/components and registered `stone-workspace` once in the verification graph. QR selections now use the managed catalogue; active explicit/default QR material references participate in the Stone transaction guard without changing the 32 resource rows or permanent URLs. The expanded SQL fixture passes 30 assertions, including QR blockers and legacy default-name matching.

Failures retained: the initial host smoke attempt could not reach its loopback server under the filesystem/network sandbox; the allowed local rerun passed. Legacy source checks initially expected direct Stone CRUD and old file locations; they were updated to check the protected endpoint and new module boundaries. Current-main integration initially failed the Articles relative import and new server lint scope; both were corrected. The complete current-main container-profile graph then passed on the host. The original isolated candidate also passed the clean Node 20 Docker gate; the integrated candidate's final Docker result is recorded at closeout. Admin no-config browser proof remains 12 passing routes; the shared Stone editor UI was unchanged by the Articles/QR integration.

Production remains on its previously recorded verified runtime; no production migration, adoption or content/Storage write occurred. Next is immutable branch Preview smoke, then item-specific approval of `docs/STONE_LIBRARY_WORKSPACE_RELEASE.md`. The branch's editor depends on the expand migration and cannot function against the unexpanded production database until that step is approved.

Integrated Node 20 Docker gate passed with 25 unique checks. The final QR default-registry assertion also passes; final committed source is gated again before push. Browser Back automation did not yield reliable completion evidence and is not counted as a passed browser case; in-app link/save-failure guards and queue flush behavior are verified.


### Release boundary — push blocked, production unchanged

Runtime candidate `482927dc0943b50b775dd961b7e4d9b9ae4974fc` on `codex/stone-library-workspace-review` passed the final clean Node 20 container gate (25 unique checks); the final SQL definitions passed all 30 disposable-database assertions. Existing no-config browser proof covers 12 routes. The independently reviewed original dirty workspace was preserved; the final candidate lives in an isolated worktree.

HTTPS push failed because the local credentials could not be opened. Connected GitHub metadata reported user push permission, but the Git tree creation endpoint returned `403 Resource not accessible by integration`. An attempt to use the existing SSH identity for the same repository was rejected by automatic approval review: it requires explicit user authorization to send source to that destination. No authentication/security settings were changed and no remote branch, PR or Cloudflare Preview was created. Stop for that exact push authorization; do not claim deployment complete. The production migration/adoption/tagged-QA approval boundary in `docs/STONE_LIBRARY_WORKSPACE_RELEASE.md` remains separate.

## 2026-09-10 — PR #47 push and Preview CI repair

Jay explicitly authorized the repository push and then repair of the failed cloud check. Restored GitHub authentication pushed `4cc8b977c3b2fcacaa9f5b79b40387ebaa02aecc` to `codex/stone-library-workspace-review`, creating draft PR #47. The prior credential/approval blocker is resolved. Production migrations, adoption/publication, Storage writes and tagged workflows remain outside this authorization.

CI run `34464456956`, attempts 1 and 2, passed source checks and isolated real-database/Functions workflows. Each deployed successfully, respectively to `https://0f6ed478.urblo-site.pages.dev` and `https://59d5be24.urblo-site.pages.dev`, but immediate smoke failed because GET `/api/enquiries` returned HTML. The first immutable Preview subsequently passed the complete independent smoke including API boundaries. This does not replace either failed attempt; propagation is a hypothesis, since the old failure log lacked status/body classification.

The repair adds an immutable-only, GET-only, 120-second readiness budget before strict smoke, with attempt/status/MIME/body-class/Cloudflare diagnostics retained as artifacts. Persistent errors, redirects, access/rate limits and invalid contracts still fail. Tests cover these failure boundaries and temporary recovery without network or writes. Clean gate and new CI evidence are pending at this checkpoint. No product UI or brand/claim change is made by this delivery repair.

Follow-up evidence: the 26-check clean Node 20 gate passed for `929d54d`. CI `34466247487` passed its source and isolated database/Functions checks, deployed `https://21313b11.urblo-site.pages.dev`, and the initial readiness GET returned 405 JSON at IAD. Five seconds later strict smoke got 404 HTML at IAD. Thus one successful GET does not establish stable deployment routing. Thirty later local GET probes all returned the expected 405. The follow-up requires three consecutive successes five seconds apart within the same 120-second limit, resetting on temporary failure; a deterministic early-success→404→stable-success case covers this observation. Response diagnostics now include final target and redirect state. Root infrastructure cause remains unproven; no failure is suppressed in strict smoke.

Closeout: the stability correction passed the clean Node 20 container graph (26 checks) and was pushed as `3771ad89dd09821ef5ced3d39ae6ebbd35bd7106`. CI `34467099328` passed all selected checks, isolated database/Functions workflows, deployment and strict smoke. Immutable Preview `https://3bbca096.urblo-site.pages.dev` returned three consecutive 405 JSON readiness responses five seconds apart at ORD, without redirects, followed by the unchanged full route/asset/redirect/API smoke. Both `verify-and-deploy` and `quality` are SUCCESS; PR #47 remains draft pending live release approval. The infrastructure cause of the earlier IAD 404 is not proven; stable readiness prevents a single early success being treated as deployment readiness, while all strict checks remain blocking. No further runtime test repetition is needed absent changes or new failures.

Production main, migrations and business data remain unchanged. The expanded database, exact 12-stone/55-photo adoption and tagged Owner/Editor/Viewer workflows are separately approval-gated by the concrete release inventory. Cloud Preview cannot certify the new editor against production before expansion, and Jay's five-minute acceptance plus broader CMS handoff remain open. This record-only closeout retains runtime evidence from `3771ad8`; its documentation does not alter deployed inputs.

## 2026-09-10 — Approved Stone Library production release

Jay approved the reviewed release scope with “发布上线”: backup, expand and lockdown migrations, exact 12-stone/55-photo baseline adoption, runtime promotion and tagged editing/role verification. Stone editing is operationally paused during this sequence. Pre-expand backup captured 16 Stone groups, 18 variants, 156 capabilities, 56 image links, 12 finish definitions, 195 media metadata rows, grants/policies and sequence state; private Stone draft/history/request tables did not yet exist. The ignored mode-0600 backup SHA-256 is `af247a732d4e90ef7900948306972971a0e2c2aed7f6bd056044b9065ea32bed`. Protected-module fingerprints include Products, Articles, Projects/private drafts, all 32 Image QR records and historical Sample Requests/items.

All formal Stone baseline rows/dependents match the approved plan. Historical test ID 1 alone is already archived (audit event 379, 06:41:20 UTC); only status/archive/update timestamps differ. The adoption runner now retains that completed operation without a write, with regression checks that changed identity/content or a changed formal baseline remain blocking. The plan bytes/hash and intended publication inventory are unchanged. This is a reduction of the approved write scope, not approval to overwrite a changed record.

Expand applied successfully through Supabase as migration `20260910105905_stone_library_workspace`; source remains the reviewed `20260910064551_stone_library_workspace.sql` (SHA-256 `88915af5863bc92bc01fd8e0a58de340f16b81dac3f54f934a296f59941fb428`). Readback confirms all five private Stone tables have RLS and no anon/authenticated SELECT; the admin RPC is service-role-only, with empty search_path, and only the intentionally Published-only catalogue is publicly executable. Advisor notices for private deny-all tables and the deliberate public catalogue are expected; existing archived-project RPC and leaked-password-protection notices persist. Preview `8ca3170c` passed real-admin sign-in, three blocked-Supabase public fallbacks and ten authenticated routes. The Stone list/detail endpoint returns 200 through the real owner session, and anonymous catalogue correctly exposes zero managed stones before adoption. The 27-check clean Node 20 gate passed the strict already-archived no-op adjustment.


## 2026-09-10 — approved Stone production release and tagged UI closeout

Jay approved “发布上线” for the reviewed backup/migration/adoption/promotion/tagged QA scope, then requested no additional hashing and prompt deployment. The already completed adoption integrity evidence was retained without extra hash runs. The final preflight classified already archived historical ID 1 as a strict no-op; formal baseline drift still fails closed. Clean Node 20 gate passed 27 checks before candidate 26849df; PR CI 34469274930 passed. PR #47 merged as `d612fcd23ada54b1bf94e75fcdb52e79875da074`. Production CI `34469804985` passed immutable `https://24e7566f.urblo-site.pages.dev`, apex and www route/asset/MIME/redirect/Function smoke.

Before expansion, canonical Stone/media snapshots, grants and policies were captured in ignored restricted local release evidence. Expand `20260910064551_stone_library_workspace.sql` applied as database version `20260910105905`; lockdown `20260910065803_stone_library_reference_lockdown.sql` applied after production smoke as `20260910111627`. Source/version mapping reflects connector timestamps, not different SQL. Readbacks confirm private draft/history/request state remains protected, public catalogue exposes Published content, all four Stone tables deny authenticated direct DML, service writes remain available and 27 mutex/reference triggers are installed. Security advisors retain intentional private deny-all RLS notices and intentional public catalogue SECURITY DEFINER visibility; existing project tombstone and disabled leaked-password warnings remain. No broader security handoff is claimed.

Real owner Preview Auth passed static fallback and ten authenticated routes. Approved adoption receipt completed: 12 Published stones, 55 exact photos in media IDs 196–250, all private originals retained and denied anonymously; historical ID 13 archived, ID 1 no-op. Protected Products/Articles/Projects/Image QR/sample data matched before/after. Original drafts/history and media were preserved. Production editing freeze lifted.

Tagged real Firefox UI marker `qa-stone-release-20260910-1110`, Stone 17: Owner login/list, create/autosave/refresh, private upload, shared preview, publish and anonymous rendered readback passed. A subsequent saved draft remained private after refresh. At 390px viewport no horizontal overflow; no page errors. Editor login/edit/save/refresh passed. Intercepting one publish request before network transmission preserved input/public content; “Check previous result” safely replayed and published with anonymous readback. This is pre-network failure evidence, not a real copy outage. Owner hide/anonymous not-found, restore/publication, final hide and formal New Grey reference rejection passed.

Test automation corrections retained: owner exact Introduction label did not match its accessible label; the retry selector originally used disabled Publish instead of the explicit recovery control; cleanup resumed from an already-hidden record. These were selector/state assumptions, not production code repairs. The corrected workflows passed. Screenshots and detailed result are retained in restricted local QA evidence. Stone 17 ended archived; exact tagged media 251 archived by conditional SQL after parent archive; all original/QA Storage objects retained. Final readback: 12 Published stones and New Grey still Published.

Viewer live proof remains open: automatic review rejected changing existing QA profile `b0af7050-2565-4da5-ab26-3f2d5b2efc96` Editor→Viewer because the identified account change lacked specific authorization. No role changed; readback remains Editor. No workaround attempted. Local Viewer proof is not promoted to production evidence. Jay's five-minute usability acceptance and broader CMS golden-workflow handoff remain open. Brand/design execution follows the approved page-shaped editor and exact current-public baseline; Harcourt remains Upcoming/TBC without invented photos.


## 2026-09-10 — remove duplicate verification instructions

Jay approved cleanup and release of the verification documentation. Removed standalone command lists already covered by the classified graph, clarified reuse for unchanged inputs/environment, and retained additional browser, migration, deployed-smoke and live-authorization requirements. No verification scripts, CI or runtime behavior changed. Validation uses the classified docs profile and final diff check; CI results are recorded on the pull request.

## 2026-09-11 — Stone/Project references phase 1

Task `NOW-STONE-PROJECT-REFS-001`, temporary plan file §2 (docs/plans, removed after Claude acceptance). Scope: public Project material-point cards, Stone detail `Used in projects`, and `?point=` deep links. No admin, Function, RPC, migration, production data or email change.

- `ProjectHotspotImage` now renders quiet 28px points, a per-point card (Published finish thumbnail, stone, finish, where it is used, two-line note, `View stone` with variant/finish), a text legend and the deep-link pin/scroll; the sticky inspector and mobile button list are removed. `ProjectService.getAll()` shares one in-flight request without TTL caching; `getProjectsUsingStone()` / `findStoneProjectUsages()` match materials and hotspot points by canonical Stone key over the merged Published + static collection and return `[]` on failure.
- `scripts/check-public-content-overlay.mjs` gained reverse-lookup assertions (static Moon Gate reference, point-only and material-only matches, canonical keys, order, de-duplication, blank key). No existing assertion changed.
- Clean Node 20 `npm run gate` passed all 27 container nodes (attempt `88435e16-e5b6-4b52-97aa-92b74ddc6e90`); host no-config browser gate passed 12 routes. `agent:verify --plan` classifies the change as runtime with deployment.
- Rendered QA on local Vite (static fallback, no Supabase env), 1280×860 and 375×812: Moon Gate points show no card by default; hover opens a card that stays open while the pointer moves onto it; the x=61% point opens right-aligned below and the y=80% point opens above; Esc and an outside pointer-down close; click pins and survives pointer leave; Tab reaches the card's `View stone` link (`/stone-library/angola-black?finish=polished`; static data has no variant); at 375px tap opens/closes, cards clamp to 24–284px inside the frame and document width stays 375px. `/stone-library/angola-black` shows `Used in projects` with Moon Gate; default Flamed leaves the Polished chip plain and selecting Polished highlights it; `See placement` opens `/projects/moon-gate-woolley-street?point=angola-black-marker`, scrolls to the image and pins the Angola Black card. `/stone-library/zen-grey` renders no section. The Admin Stone preview passes `preview` and never requests usages (source check; browser proof needs an authenticated session). No console errors.

Residual: ordering with several matching Projects is only exercised by source logic until more Projects reference one Stone; Stone detail pages now issue the Project collection reads (measured in phase 3).

Delivery: PR [#50](https://github.com/jayyy-3/jayyy-3.github.io/pull/50) at `1fba735` passed CI run `34594618314` (source graph, isolated `local:verify`, immutable Preview `https://27097513.urblo-site.pages.dev` and its deployed smoke; `quality` passed). A separate `agent:cloudflare-preview-smoke` against that Preview also passed. Read-only Preview QA with production data: `/stone-library/angola-black` renders Used in projects with Moon Gate (Polished chip, `See placement` → `?point=angola-black-marker`); `/projects/moon-gate-woolley-street?point=…` pins the card, keeps Featured Materials and has no inspector aside. Direct-load deep links were confirmed in headless Chromium against the Preview at 1280×860 (figure top 103px, card pinned) and 375×812 (figure top 268px). An earlier in-app pane reading of `scrollY 0` came from the hidden pane pausing smooth scrolling (`visibilityState: hidden`), not from the page. The Stone page issues 6 Project reads (tombstone RPC + projects, then media/materials/maps/facts; no hotspot read while no Published maps exist), about 955 ms wall time in parallel with the Stone catalogue, without delaying the detail render. Awaiting Claude acceptance and Jay merge approval; not merged.

## 2026-09-11 — Stone/Project references phase 2

Task `NOW-STONE-PROJECT-REFS-001`, plan §3, branch `claude/stone-project-refs-phase2-admin` stacked on phase 1 PR #50. Scope: Projects admin editor only. `functions/` and `supabase/` are unchanged (`git diff --stat main -- functions supabase` is empty); no migration, production data or email.

- Editor: four sections in public-page order (Hero and overview / Project information / Page images and video / Material schedule), all expanded and individually collapsible, under a sticky single-row section bar with counts and amber blocker dots. The Material maps section, the Interactive material image type and the map selector are removed. Any image block has `Mark materials on this image`: canvas, per-image point tabs (roving keyboard), point panel with a material select that can add a material in place, `Where it is used` inheriting the material wording, point note, Stone Library preview or in-place material fields; `Turn off material points` deletes the points and returns a plain image. Materials show `Used at N points` / `Not placed on any image yet`.
- Confirmations are inline in-app banners (Keep/Delete), not browser dialogs: `check-admin-projects-aggregate.mjs` forbids `window.confirm`/`confirm(` in Projects browser source, and that assertion is kept. The first gate run failed `coverage` because the visible label `Remove points from this image` matched the archive-first rule against visible `Remove` controls; it was renamed `Turn off material points` and the gate passed on the rerun. Both runs are recorded.
- Aggregate: `mediaBlockImageId`, `hotspotsForBlock` (draft array order = legend order used by Save), `countHotspotsForMaterial`, `enableMediaBlockPoints`, `disableMediaBlockPoints`, `setMediaBlockImage`, `removeMediaBlock`, `adoptLegacyMaterialMaps` (also fills an untitled points block from its map title), `getProjectBlockerSections`. Save copies the block title or `Stone and finish placement` to the map title (server publish requires one), clears the retired map intro, and fills an empty point use from its material. Blockers point to `media`/`materials`. `AdminProjectsPage` adopts older maps in `withResponseStatus`, so draft and baseline are the same object and nothing is dirty on open.
- Check script changes: in `check-admin-projects-aggregate.mjs` the stable selected-map media key assertion was replaced by per-block ``instanceKey={`media-${block.key}`}`` / ``key={`media-${block.key}`}`` assertions, the broken-reference blocker expectation moved from `maps` to `media`, and a forbid was added for the removed map editor labels. New pure tests cover the enable/disable round trip, server draft shape and publish validation of normalized points drafts, image replacement, cascade removal, save normalization, adoption (including idempotence and an unchanged public sequence) and the absence of `maps` blockers. All existing forbid assertions remain. `scripts/local-journeys/projects.mjs` gained a points journey. `scripts/_lib/local-environment.mjs` now inspects `--type container`: after `local:stop` the retained `supabase_db_urblo-isolated-v1` volume matched a bare `docker inspect`, and `local:start` crashed on its missing `Config`.
- The isolated stack belonged to idle checkout `/private/tmp/urblo-maintainability` (last used 2026-09-09); it was stopped there with `local:stop` (volume retained) and started here. `local:verify` passed twice (`full-1789127582890`, then `full-1789128015410` after the copy change) with 17/17 journeys, including `Projects material points on a page image survive Save/refresh and turn off cleanly`. The local database seeds no Stone Library records, so that journey asserts the material publish blocker and a disabled Publish rather than publishing a point; the existing Publish/Hide journey then runs after the points are turned off.
- Clean Node 20 `npm run gate` passed 27/27 (`ab9ef717-011e-4d22-bdb2-69436daac9b2`, after the failed first attempt `305b4d3d-424a-4191-8c27-26056ebe5623`); host no-config browser gate passed for 12 routes; `git diff --check` is clean.
- Rendered QA used a scratch Playwright script against `local:dev` with the synthetic owner, at 1116×900 and 375×812. A project saved through the protected endpoint with a standalone map (no image block) opened with `Block 1 · Image`, `1 material point`, title `Legacy map title` and `All changes saved`. With Media collapsed, the material blocker and the section bar each expanded and scrolled their section to 80px under the bar. Replacing the image kept the marker and showed the notice. Save persisted the adopted `hotspot_image` block with the map title and an empty intro, and the reload stayed clean. The block-removal and material-removal confirmations appear and Keep preserves the data. 375px has no horizontal overflow; no console errors. A publish with a real point card cannot be exercised locally without Stone Library data.

Delivery: PR [#51](https://github.com/jayyy-3/jayyy-3.github.io/pull/51) (base: phase 1 branch) at `d368738` passed CI run `34597059624`, including isolated `local:verify` journeys, immutable Preview `https://f267fac3.urblo-site.pages.dev` and its deployed smoke; `quality` passed. A separate `agent:cloudflare-preview-smoke` also passed. Read-only Preview checks: `/admin/projects` redirects to `/admin/login`; public Stone usage and `?point=` deep links behave as in phase 1 (headless 1280/375). Authenticated editing on Preview would use production data and is outside this task's authorization.

Residual: publishing a points image end to end needs Stone Library data (Preview uses production data and is read-only), so it rests on the aggregate/server unit coverage plus phase 1 public rendering. Older maps are persisted as image blocks only when an editor next saves that project.

## 2026-09-11 — Stone/Project references phase 3

Task `NOW-STONE-PROJECT-REFS-001`, plan §4, branch `claude/stone-project-refs-phase3-cleanup` stacked on phase 2 PR #51. No new behaviour, Function, RPC, migration or data change.

- Cleanup: `src/components/projects/ProjectMaterialMap.tsx` had no importers and is deleted; its ARCHITECTURE line is removed, the Admin UX plan reference and two `next` task file lists now point at the current components. `scripts/check-doc-paths.mjs` allows that one retired path because immutable archived WORKLOG/ARCHITECTURE evidence still cites it. Phase 2 had already removed the map tab/panel state and helpers; ESLint is clean.
- `ProjectEditor.tsx` was 2037 lines. The Media section with its point panel, the Materials section and the shared field controls moved unchanged into `src/pages/admin/projects/sections/` (`MediaSection.tsx`, `MaterialsSection.tsx`, `EditorControls.tsx`, `editorContext.ts`); the editor is now 951 lines. `check-admin-projects-aggregate.mjs` reads the editor source as `ProjectEditor.tsx` plus those files, so every existing require/forbid assertion still applies to the whole editor. After the split, `local:verify` passed again (`full-1789128659280`, 17/17 journeys) and the scratch admin QA returned identical results (legacy map opens clean, 80px jumps, notice, persistence, confirmations, no 375px overflow, no console errors).
- Docs closeout: `ADMIN_PRODUCTION_WALKTHROUGH.md` Projects steps now use the four sections, the section bar and Mark materials / Turn off material points. The ARCHITECTURE, DESIGN, ADMIN_EDITOR_GUIDE, ADMIN_IA_ACCESS, PROJECT_MAP and verification wording from phases 1–2 were re-read against the code. `agent:harness-gc` and `agent:harness-gc:review` passed with no findings.
- Measurement (§4.2), headless Chromium, 5 cold runs each, `/stone-library/angola-black` at 1280×860: production immutable baseline `https://24e7566f.urblo-site.pages.dev` has a median detail heading at 1112 ms with 2 REST reads and 25 resources. Phase 1 Preview `https://2bf6e062.urblo-site.pages.dev` has a median detail heading at 1063 ms; `Used in projects` appears within 1–2 ms of it; there are 6 Project reads (tombstone RPC and `projects`, then media/materials/maps/facts) with a median span of 217 ms (178–286), 8 REST reads and 38 resources (the extra resources include the project card image). This does not exceed the >6 requests / >400 ms threshold, so no `NEXT-STONE-PROJECTS-RPC-001` task was created.
- Clean Node 20 `npm run gate` passed 27/27 (`6433b520-b06e-4a52-be6d-c6fba88dd3c3`); host no-config browser gate passed for 12 routes; `git diff --check` is clean; `git diff --stat main -- functions supabase` is empty.
- Task phase is `awaiting_external`: Claude acceptance, then Jay's merge approval for #50 → #51 → the phase 3 PR. The temporary plan file stays for Claude to delete after acceptance.

Delivery: PR [#52](https://github.com/jayyy-3/jayyy-3.github.io/pull/52) (base: phase 2 branch) at the phase 3 cleanup commit passed CI run `34598051213`, including isolated `local:verify` journeys, immutable Preview `https://b03a03ab.urblo-site.pages.dev` and its deployed smoke; `quality` passed. A separate `agent:cloudflare-preview-smoke` also passed, and headless public deep-link checks at 1280/375 match phases 1–2. This evidence commit changes only records; the runtime inputs are those verified by that run. Not merged.

## 2026-09-11 — Stone/Project references Claude acceptance

Task `NOW-STONE-PROJECT-REFS-001`. Claude reviewed stacked PRs #50 → #51 → #52 against the plan's acceptance checklist; no code change was required. The temporary plan file under docs/plans is removed in this record-only commit.

- Source review of every changed runtime file on `f71b95b`: public point cards, Stone `Used in projects`, `?point=` deep links, aggregate helpers, four-section editor with `sections/`, legacy map adoption, inline confirmations, and the check-script changes with their reasons. `git diff --stat main -- functions supabase` is empty. All previously existing forbid assertions remain.
- Host verification on `f71b95b` (Node 22): `npm run build`, `npm run lint`, `npx tsc -b`, `agent:admin-projects-aggregate`, `agent:stone-library-detail`, `agent:public-content-overlay`, `agent:admin-crud-coverage`, `agent:check` and `git diff --check` passed. One `agent:admin-crud-coverage` attempt failed only because this worktree lacked `tsx` in `node_modules`; it passed after `npm install`.
- Read-only Preview `https://6454b862.urblo-site.pages.dev` (production data) in the in-app browser. Desktop: Angola Black `See placement` opened `/projects/moon-gate-woolley-street?point=angola-black-marker`, scrolled to the image and pinned the Angola Black card (finish thumbnail, stone, Polished, where it is used, note, `View stone`); `Esc` closed it; no side inspector; legend present. Mobile 375×812: the deep-linked card stayed within 24–284px and the document width stayed 375px; tapping Point 2 opened the New Grey card and a second tap closed it; `Esc` closed cards. `/stone-library/angola-black?finish=polished` rendered `Seen on site` with the Moon Gate card, the Polished chip highlighted and the `See placement` link; `/stone-library/zen-grey` rendered no section. No console errors.
- Admin: reviewed the phase 2/3 local QA screenshots (1116 full editor, legacy map adoption opening with `All changes saved`, 375 block) and the CI `local:verify` 17/17 result including the points journey. Authenticated editing on Preview is out of scope (production data).
- Minor, not blocking: a card opened above a low point on 375px can extend over the block's intro text; the admin preview no longer shows a legacy map's retired intro while the public page still does until the next Save clears it; publishing a points image end to end remains unexercised locally because the isolated database seeds no Stone Library records.

Result: accepted. Merge order #50 → #51 → #52 awaits Jay's approval; production apex/www readback follows the merge.

## 2026-09-12 — Stone/Project references production release

Jay approved "merge。上线" in chat after Claude acceptance. Stacked PRs were merged in order with head-SHA guards: #50 (`0e7644f`) → merge `aa4f299`; #51 retargeted to main (`d368738`) → merge `3957622`; #52 retargeted to main (`704ee3a`, including Claude's acceptance record and plan-file removal) → merge `7de1af716fe54fcd7566a17b3693ecb838fb4722`. Main runs for `aa4f299` (`34606532052`) and `7de1af7` (`34606801956`) passed. The concurrency group cancelled the queued intermediate runs `34606707277` and `34606716889` before they started, which is expected.

Production CI `34606801956` deployed immutable `https://905e7161.urblo-site.pages.dev` and passed isolated `local:verify`, deployed smoke and production domain verification against that immutable reference. A separate `agent:cloudflare-preview-smoke --reference-url https://905e7161.urblo-site.pages.dev` also passed for `https://urblo.com.au` and `https://www.urblo.com.au`. Read-only live checks on urblo.com.au in headless Chromium: `/stone-library/angola-black` lists Moon Gate with `See placement` → `?point=angola-black-marker`; `/stone-library/zen-grey` has no section; `/projects/moon-gate-woolley-street?point=new-grey-seating` scrolls to the image and pins the New Grey card at 1280 and 375; `/admin/projects` redirects to `/admin/login`; no page errors.

No migration, Function/RPC change, production data, Storage write or email. Authenticated production editing was not exercised by the agent. Jay's usability check of marking materials on a real project image and publishing it remains open, and is also the first end-to-end publish of a points image with Live Stone Library data.


## 2026-09-13 — company address consolidation

Jay approved implementation and release of Office `Suite 375, Level2, UL40 1341 Dandenong Road, Malvern East 3145` and Warehouse `457-461 Springvale Rd, Glen Waverley VIC 3150`, centralized in existing Settings. New dedicated inputs serialize reserved footer text entries; public Contact/footer/Organization data share the same settings and current fallbacks. Removed obsolete company-address paragraphs from four static articles and replaced PDF page 25 contact text; the other 24 PDF pages remain unchanged. The Glen project location remains unchanged. Brand/design alignment: retain existing layouts and distinguish the two destinations with plain labels.

The existing clean worktree started from latest main; GitHub authentication and local prerequisites passed preflight. Browser plugin not available: use installed Playwright with synthetic Settings API responses for save/refresh/public synchronization before the approved production update. Supabase changelog has no applicable change to the existing Settings JSON contract. No database schema change or new management module. Release checks and final production evidence follow on the PR and closeout record.

### Address verification and production release

- Local: clean Node 20 container gate passed on candidate tree `418f1129ad53c1508e7040d416dac96ee2ae0552`; uncovered `agent:admin-config-gate` passed all 12 routes and `agent:seo-readiness` passed. Synthetic owner browser proof saved changed Office/Warehouse values, refreshed Settings, verified Contact/footer/Organization synchronization, restored approved values, preserved navigation and passed desktop/mobile width checks with zero page errors. Early temporary-script attempts failed on accessible-name matching, duplicate Save buttons and the lazy Contact route still loading; selectors/waits were corrected without runtime changes after the gate. Preview captures initially preceded CMS rendering and included the existing welcome overlay; the final capture explicitly waited for the CMS footer and used a returning-visitor state.
- PDF page 25 was rendered and visually checked. The other 24 pages' extracted text remains identical to the original; The Glen project address is unchanged. No new design direction beyond labeled Office/Warehouse contact blocks.
- PR #54 passed Preview CI `34704373106` at `https://15b467f9.urblo-site.pages.dev`; read-only deployed browser proof covered both static fallback and loaded Published Settings, responsive Contact/footer, Organization data, article cleanup and the exact PDF bytes. The existing bounded Function-readiness wait recovered its initial not-ready response before the unchanged full smoke passed. Jay's existing release approval was used to merge the verified head; production runtime is `0ef7835ba293c012e0d3910f8726c390c4a096dd` at `https://e80309fb.urblo-site.pages.dev`. Production CI `34704740559` passed source checks, isolated database/Functions verification and immutable/apex/www deployment-bound smoke.
- At `2026-09-12 16:22:00 UTC` (September 13 Melbourne), the approved production data update replaced only the old Address entry in Published `site_settings.id = 1` with Office/Warehouse text items. The update matched the previously observed row timestamp and old entry; returned before/after data confirmed all other settings unchanged. Email, phone and Navigation entries are preserved. No migration, Storage mutation or email was needed.
- Read-only production Firefox proof on `https://urblo.com.au/contact` passed after CMS rendering at 1365px and 390px: both addresses appear in Contact/footer, Organization data matches, the old address is absent, no horizontal overflow and no page errors. Anonymous Published Settings returned both exact new values. All four served article HTML files and the downloadable PDF matched their released files byte for byte. The PDF remains a separate asset that needs a future edit when an address changes. This scoped proof does not certify unrelated CMS golden workflows.

Address delivery is complete. The task is archived; current work returns to the existing Stone/Project user acceptance. This closeout changes records only and uses the classified docs graph.

## 2026-09-13 — Our Story experience wording

Jay requested and approved release of exactly the caption under `20+`: “Founded by stone experts with 20+ years of experience.” The number, layout and other SAI references remain unchanged. This uses the supplied founder-experience wording within the existing proof-metric design. Other mentions were located in the Our Story body, capability PDF pages 5 and 11, and internal brand/design guidance; they are outside this release. Required gate and deployment results are recorded in the associated PR/CI.

Our Story release evidence: candidate `50d735e56b2a485b3be6f9acd1ab417fe48ebdf0` passed the clean Node 20 gate and 12-route browser configuration gate. PR #56 passed Preview CI `34742683245` at `https://45e0c0e8.urblo-site.pages.dev`, including targeted desktop/mobile caption and preserved-body-copy checks. Early screenshots preceded the existing entrance animation; the capture wait was corrected without changing runtime code. Approved merge `21ce7b8cf9e81eef8a9703919bf0c150b1e719d2` deployed through production run `34743018878` at `https://0653930e.urblo-site.pages.dev`; immutable/apex/www smoke passed. Production browser readback verified the exact new caption, retained `20+` number and SAI body sentence, no horizontal overflow at 390px, and zero page errors. No database, PDF or other public content changes. This evidence-only closeout preserves the existing user-owned acceptance tasks.

## 2026-09-14 — Our Story founders introduction

Jay supplied and approved a three-paragraph introduction: retain the company description with the missing F restored in Founded, replace the partnership paragraph with the supplied Natalie Ma / Jun Hu / Bob Lu / Cameron Grover backgrounds, and add the independent Design / Stone closing paragraph. Preserve the current paragraph styling and the previously approved 20+ caption. These are client-supplied claims; this edit does not independently verify biographies or change the PDF. Jay subsequently approved publication with “上线”. The unchanged runtime tree passed the clean Node 20 gate; local desktop and fresh mobile paragraph/layout checks passed. Release CI and deployed readback follow.

Founder introduction release: PR #58 candidate `bbc0482b9199773e528207f39a33018fefbc0f6f` passed Preview CI `34818378061` at `https://57b25957.urblo-site.pages.dev`; deployed browser checks matched all three paragraphs exactly to source at 1365px and fresh 390px viewports, with no horizontal overflow. Jay’s publication approval was used to merge as `5b57b7d4ec074aa33f27f84e2166421d86b7ecb8`. Production CI `34818888611` passed source/isolated verification and immutable/apex/www smoke at `https://777af336.urblo-site.pages.dev`. The same exact-paragraph and responsive rendering checks passed on `https://urblo.com.au/our-story`; both production captures were visually inspected. No database writes, PDF changes or changes to the 20+ caption. This closeout records evidence only.

## 2026-09-25 — Stone/Project references user acceptance

Jay stated the Stone Library ⇄ Projects references work is done. Read-only production readback confirmed use since release 7de1af7: the anonymous public REST API returned 25 published `project_hotspots` created between 2026-09-15 and 2026-09-17 by one admin account across Artisan Park, Greenline, Cowes Streetscape and other published projects. `https://urblo.com.au/projects/artisan-park-yarrabend` renders the Material points list with a View stone deep link to `/stone-library/new-grey?variant=new-grey&finish=flamed`; that Stone page renders Used in projects with nine See placement `?point=` deep links. No production write, migration, Storage change or email. Runtime is unchanged; this closeout records evidence only and uses the classified docs graph. NOW-STONE-PROJECT-REFS-001 is archived; the current task returns to the existing Stone workspace acceptance. The separate 2026-09-24 four-track optimization audit (performance/SEO, UI consistency, admin usability, code health) is held in session scratch pending Jay's task selection and is not repository state.

## 2026-09-25 — Repository hygiene (NOW-OPT-REPO-HYGIENE-001)

Scope per the task authorization: dependency/advisory fixes, dead-code and unused-dependency removal, dependency classification, a committed knip config in the verification graph, and deletion of remote branches already merged into main. No production access, database, Storage or email.

- Advisories: `npm audit --omit=dev` before: 6 (4 high: react-router 7.16.0 open redirect/DoS, postcss, nanoid, brace-expansion via the typography plugin's peer tree; 1 moderate; 1 low). `npm audit fix` (no `--force`, in-range only) moved react-router/react-router-dom to 7.18.4, dompurify 3.4.16, vite 6.4.3, postcss 8.5.28; after: 0 vulnerabilities. Full `npm audit` keeps 3 high dev-only findings from the exact-pinned `wrangler` 4.130.0 (miniflare/sharp); a wrangler bump is outside this task.
- `.vite/` (16 tracked optimizer-cache files) is untracked and ignored. Deleted with zero importers (grep + knip): `src/App.css`, `src/components/ProjectCard.tsx`, `src/scripts/generate-article-index.ts`, `src/utils/articles.ts`; they are allow-listed as retired paths for historical citations. Removed unused `lodash.throttle`, `@types/lodash.throttle`, `clsx`.
- Classification: `dompurify`, `lucide-react`, `zustand` moved to dependencies (runtime imports); `@tailwindcss/typography` moved to devDependencies (build-time Tailwind plugin). `framer-motion` stays in devDependencies because a parallel wave-1 branch removes it. CI and the gate build with `npm ci` including dev dependencies, so the build input is unchanged in kind.
- knip 6.38.0 (exact devDependency) with `knip.json` declaring Pages Functions, scripts, fixtures and local journeys as entries. Before (no config): 31 unused files (mostly false-positive entries), 4 unused dependencies. After: 0 unused files, 0 unused/unlisted dependencies; unused exports/types are warnings only. An orphan-file probe made knip exit 1. The `knip` node runs in the tooling, container, runtime and migrations suites, asserted by the classifier check.
- Merged remote branches recorded before deletion (each tip verified with `git merge-base --is-ancestor <sha> origin/main` after `git fetch --prune`; none has an open PR; open PR heads, `gh-pages`, the unmerged `codex/maintenance-closeout`, `codex/project-hotspots-*`, `codex/project-image-delivery-closeout`, `codex/qr-*` and `claude/nostalgic-mccarthy-e8e857` are kept): `claude/stone-project-refs-phase1-public` 0e7644f, `claude/stone-project-refs-phase2-admin` d368738, `claude/stone-project-refs-phase3-cleanup` 704ee3a, `claude/stone-project-refs-release-evidence` f8e8ce3, `codex/admin-projects-contract-closeout` ca847bf, `codex/admin-reliability-ux` 0a7e545, `codex/admin-ux-reshape` f317e06, `codex/company-addresses` 418f112, `codex/company-addresses-closeout` 4c9e170, `codex/maintenance-articles-refactor` efa706e, `codex/maintenance-behavior-tests` 4ff512b, `codex/maintenance-docs-proof` f0454ab, `codex/maintenance-local-stack` 9b3f0ca, `codex/maintenance-state` 01b2848, `codex/maintenance-verification` db38c19, `codex/our-story-copy-closeout` f01c9f5, `codex/our-story-experience-copy` 50d735e, `codex/our-story-founders-closeout` 9775b8a, `codex/our-story-founders-copy` bbc0482, `codex/projects-clear-counts` 667c2d6, `codex/projects-clear-counts-closeout` 8d3e65b, `codex/projects-production-evidence` 4c9f4fe, `codex/projects-user-acceptance` 984c2fa, `codex/public-ui-admin-simplify` 435a354, `codex/stone-library-workspace-review` 26849df, `codex/stone-workspace-release-evidence` ce1cac8, `codex/verification-doc-dedup` 7ec62e1 (27; the audit estimate of 34 counted differently). Tips remain reachable from main.
- Verification: first clean Node 20 gate failed at `paths` (the task record and immutable archives cite the deleted files); fixed by the retired-path allow-list and retained here as a failure.
- Runtime candidate `d0a48af754a16edfff0f57aa13d80df5ec1f1741`:
  - Clean Node 20 gate (second run): all 28 container nodes passed, including `knip`.
  - Host checks (Node 22.22.3): the build passed, the no-config browser gate passed on 12 routes, and `git status` stayed clean after the build.
  - `npm audit --omit=dev` reported 0 vulnerabilities, and `npx knip` exited 0.
  - Host `npm run local:verify` failed at `local-stack.mjs start` with "Local container belongs to a different checkout; refusing startup". The shared `urblo-isolated-v1` stack belongs to another worktree and was left untouched.
  - PR #61 CI run `36141692162` passed the classified runtime graph, including knip.
  - The same CI run passed isolated `local:verify`: start, reset, migrations, journeys and article mutation.
  - CI deployed the branch Preview to `https://0fa64dc7.urblo-site.pages.dev`, and its smoke check passed.
  - The host run of `agent:cloudflare-preview-smoke --base-url https://0fa64dc7.urblo-site.pages.dev` also passed.
- Branch deletion: the list above was pushed first. All 27 branches were then deleted, each with `--force-with-lease` on its recorded tip. The remaining heads are `main`, `gh-pages`, open PR heads and unmerged branches.
- Status: not merged; production unverified.
## 2026-09-25 — Admin safety stop-gap (NOW-OPT-ADMIN-SAFETY-001)

Scope: Jay-approved optimization wave 1, stop-gap only; branch `claude/opt-admin-safety-stopgap`, PR #62 stacked on #60; not merged. No schema, Function, RLS, production data, Storage or email change.

- Settings: Status dropdown removed; Save validates and writes `published` (a stored Draft/Archived row now says the website uses built-in contact details until Save).
- Products, Articles, Media: Save on an already-live record (including a live product's model/spec/material default and a live article's published section) is labelled "Update live page" and opens the shared `StoneDialog`-based confirmation naming the public effect; Keep editing writes nothing; drafts save directly. Finding: before this change, Media "Save draft" on a published asset wrote `status: draft`, silently unpublishing it.
- Products, Articles: new-record URL key follows the name until edited, stays editable until first publish (`published` or `published_at`), locked after.
- Projects: source confirmed the aggregate envelope and list report `draft` whenever the private draft is ahead of the published revision. Editor now shows "Live · unpublished changes" (list "Live · changes", counted as Live, Open live page kept) using `getLiveProjectAddresses()`, which reads only ids/slugs through the public published+approved policy.
- `check-admin-crud-coverage.mjs`: removed required copy `Safe to edit before public use` and `Draft settings are safe to prepare before they become public.`; added `checkLegacyModuleSafetyStopGap()` (Settings always Published and no Draft/Archived option, live-save confirmation wiring, URL-key lock helpers, Projects live label, journey markers, tsx behaviour test of the pure helpers). All existing forbid assertions kept; Projects aggregate verifier unchanged and passing.
- Evidence: clean Node 20 `npm run gate` passed at the code tree of `3232b83`; `agent:admin-crud-coverage`, lint and `tsc -b` passed. PR CI run `36143030913` failed in `local:verify` (new Articles journey step hit two "Update live page" buttons, article and live section); fixed by scoping to the article form in `33448ca`. Retry CI run `36143665645` passed full `local:verify` (including "Articles published parent and section render together…" with the new live-save confirmation, "Articles new draft URL key follows the title until edited", and "Projects Publish exposes approved parent and Hide removes it" with the new live-with-changes label), deployed immutable Preview `https://3e20e17d.urblo-site.pages.dev`, and its smoke passed; a workstation rerun of `agent:cloudflare-preview-smoke` against it also passed. Preview was not logged into.
- Workstation `local:start`/`local:verify` were not run: the isolated stack container belongs to another checkout (idle since 2026-09-11) and the ownership guard refused it. Mocked-network headless Playwright before/after runs against static builds of `7b42ab6` and this branch are kept under ignored `.tmp/admin-safety/`.
- Residual: user acceptance by Jay/colleagues; Archive/Hide still have no confirmation (outside this scope); Products/Articles aggregate rewrite not started.
## 2026-09-25 — Stone media variants (NOW-OPT-PERF-STONE-IMG-001)

Scope from Jay's approved optimization wave 1: Stone media must stop downloading 2–10MB Supabase originals on public pages while designers keep full-resolution texture. Reused the existing Project delivery path (Supabase `render/image` on `urblo-public-media`) through a shared `toPublicMediaRenderUrl`; Project URLs are byte-identical. New `src/lib/stoneImageDelivery.ts` profiles and `StoneResponsiveImage` (one-time fallback to the source on render error) cover option swatches (240/360w, lazy), product preview tiles, Stone Library cards, detail-stage panels (480–2560w, quality 88, active panel eager/high priority, others lazy), secondary/hotspot thumbnails, Project material rows, the QR stone surface, and the finish lightbox / QR enlarged view (1280–3000w, quality 90; 2x zoom doubles `sizes`). The render endpoint clamps at 3000px (read-only probe: `width=4000` returned 3000px). The detail stage caption and lightbox header add an explicit `View original` link to the untouched Storage object. Stone `og:image` uses a 1200×630 `format=origin` cover variant. No Function, RPC, migration or data change. Contract assertions were added to `agent:public-content-overlay`.

Local evidence on runtime commit `6e15599`: clean Node 20 container gate passed. The first attempt failed `projects` because the refactor removed the literal `format', 'webp'` / `resize', 'contain'` contract tokens; they were restored and the re-run was green. Host `check-admin-config-gate` passed 12 routes and `git diff --check` was clean. Isolated synthetic browser check (Playwright; configured build with a fake Supabase origin; all Storage/RPC traffic intercepted locally; no production data): `/stone-library`, a synthetic Stone detail at 1440@2x and 390@3x, and `/products/prime-block` at 390@3x made zero `object/public` Storage requests, and every Storage request used `render/image`. Stage panels chose the 1024w variant. The lightbox chose 2560w and switched to 3000w on 2x zoom. `og:image` was the 1200x630 variant. `View original` links pointed at the untouched object in a new tab. There was no horizontal overflow and zero page errors. The agent's own `git push` was refused by the session permission layer, and the coordinator pushed head `6716117` as PR #63 (stacked on #60).

Preview evidence: branch CI `36143175914` succeeded on `6716117` and deployed the immutable Preview `https://49839330.urblo-site.pages.dev`. `agent:cloudflare-preview-smoke` passed. Read-only Playwright network check (Preview uses production data): `/stone-library` 1440@2x, `/stone-library/harcourt` 1440@2x with lightbox and 2x zoom, and 390@3x, `/products/prime-block` 390@3x and 1440@2x, and `/projects/artisan-park-yarrabend` 1440@2x made zero `object/public/.../stone-assets` requests. Every Storage request used `render/image`, with zero page errors. The lightbox chose 2560w and then 3000w on zoom. The Harcourt og:image is the 1200x630 `format=origin` variant, and View original points to the untouched object. Lighthouse 13.5.0 (single headless runs; JSON in the worktree `.tmp/lighthouse-stone-media/`), before (production audit 2026-09-24) → after:
- prime-block mobile: 52.3MB → 3.01MB, LCP 8.5s.
- prime-block desktop: 52.3MB → 2.91MB.
- Stone Library mobile: 15.7MB → 1.92MB.
- Stone Library desktop: 50.4MB → 1.21MB.
- Harcourt mobile: 29.4MB → 1.58MB.
- Harcourt desktop: 29.4MB → 1.58MB, LCP 23.7s → 1.7s, Perf 58 → 77.

Acceptance met: no originals except the explicit link; srcset reaches 2560/3000px; the og:image is a variant; Stone Library desktop is under 5MB; Harcourt desktop LCP is under 2.5s. Prime-block mobile misses the < 3MB target by about 10KB. Stone variants there are now 0.33MB (previously 49.6MB); the remainder is static non-stone assets outside this scope (batten/frame JPGs 1.27MB, materials banner 498KB, global hero-poster preload 422KB). Harcourt mobile Perf dropped from 80 to 57. CLS rose from 0.002 to 0.317, and Lighthouse attributes 0.316 of it to the `<footer>` shift (audit P0-2). Stone panels are fixed-size. This needs repeat runs. The Project og:image is still a Project original PNG, which is outside this Stone scope. No merge; Jay approval pending.
## 2026-09-25 — UI cascade fixes (NOW-OPT-UI-CASCADE-FIXES-001)

Scope: the four audit defects only. There is no token, type-scale or shared-component change; those belong to wave 2. There were no production writes. Branch `claude/opt-ui-cascade-fixes`, candidate `b8d5ba503fd3bc1a5cdf3ced223d3360104d4782`, PR #64. It stacks on #60 and is not merged.

- **Cascade:** the `urblo-*` classes moved into `@layer components`, so Tailwind classes written next to them now apply. Eyebrows and meta labels on dark Contact and Capabilities surfaces now render as `white/62`–`/64` instead of dark grey, which on black is about 7:1.
  - Changes that surfaced once the utilities applied:
    - Contact cards: `shadow-none` removes their shadow, and the image card now sits on `bg-black`.
    - Projects H1: `mt-0` applies.
    - Stone detail eyebrow: `mt-6` applies.
  - How they were resolved: `text-[10px]`/`[11px]` on `urblo-meta` were removed so labels keep the 12px visitors see today. `text-black/45`/`/50` meta labels were raised to `/60`, so no meta text drops below 4.5:1.
- **StatusPill:** the overlay surface is now a dark translucent backplate with white text and a lime dot only, per DESIGN.md.
- **Focus:** there is one global `:focus-visible` ring (2px black with a white halo). The inset `.urblo-focus-inset` variant draws the same ring inside elements whose parent clips overflow. Per-element blue and lime focus styles were removed from public UI. Local tab pass over five pages (118 stops, identical on both builds): before, 102 stops were flagged (95 browser-blue, 18 lime); after, 0. On Preview the same pass recorded 151 stops across the five pages with 0 flagged.
- **Contact:** the "Stone Library" and "Send enquiry" buttons measured 2 lines at 1440px before the fix. After, they are 1 line at 1440px and at 375px, locally and on Preview.
- **Evidence:** before/after screenshots at 1440px and 375px for Home, Capabilities, Contact, Stone Library, Projects and Angola Black, plus zoom crops, computed-style dumps and focus screenshots, are under ignored `.tmp/ui-fixes/`.
- **Checks:**
  - ESLint, `tsc -b`, and the source checks `forms-ui`, `capabilities-ui` and `stone-library-detail` passed.
  - Clean Node 20 `npm run gate` passed, 28 checks.
  - CI run `36143351963` passed at `https://44c93f47.urblo-site.pages.dev`.
  - `agent:cloudflare-preview-smoke` passed.
  - Preview screenshots and the tab pass were read-only and repeated the local results.
- **Residual:** Jay has not yet given visual acceptance. The Image QR page's focus styles and admin screens with custom focus styles were left unchanged. Safari/iOS rendering and the 768px width were not checked.
## 2026-09-25 — CLS and page shell (NOW-OPT-PERF-CLS-SHELL-001)

Scope: public page shell only. No production data, Storage, email, DNS or credential changes. Not merged.

- Code commit `24f0847` on `claude/opt-cls-page-shell`, PR #65. It is based on `7b42ab6` and stacks on PR #60.
- `<main>` now reserves one viewport (`min-h-[100svh]`) in `DefaultLayout` and `HomepageLayout`. The footer can no longer paint in view and then be pushed down.
- The homepage Suspense fallback is a black 100svh hero placeholder.
- The route transition is now a CSS keyframe: opacity plus a 10px rise, 220ms, reduced-motion aware. It runs only after client-side navigation, so the first page paints without starting at opacity 0.
- `hero-poster.jpg` is preloaded only on `/`.
- framer-motion stays a dependency because lazy pages use it (Home, Articles, Our Story, Capabilities). It is now in a shared lazy chunk. `package.json` is unchanged.
- Entry JS: 417.60KB → 304.80KB raw, 129.35KB → 91.88KB gzip. The framer-motion lazy chunk is 37.18KB gzip.
- Checks passed:
  - Clean Node 20 `npm run gate` (Docker), all 27 checks.
  - CI run `36144515149`.
  - `agent:cloudflare-preview-smoke` on `https://85e797f2.urblo-site.pages.dev`.
- Lighthouse 9 URLs × mobile/desktop, one run each.
  - Before, on `777af336`: CLS 0.037–0.440; 15 of 18 runs were at or above 0.1.
  - After, on `85e797f2`: CLS 0.000–0.003 on all 18 runs. Non-home routes no longer request `hero-poster.jpg`.
  - JSON is in ignored `.tmp/lighthouse-cls-shell/`.
- Local Playwright screenshots at 375px and 1440px for the 9 URLs are in `.tmp/cls-shell/`. Page heights are identical before and after. Pixels are identical except the playing homepage hero video frame.
- Open item: the homepage LCP element is no longer the footer, but it is not the hero either.
  - Lighthouse reports the header logo on mobile and the first-visit Acknowledgement popup text on desktop.
  - Chrome ignores images that fill the viewport for LCP; a local probe showed a 100svh poster is ignored while a 90svh one counts.
  - The hero headline is made of per-letter spans.
  - Fixing this needs a design decision.
## 2026-09-25 — Test runner (NOW-OPT-TEST-RUNNER-001)

Scope: Jay-approved optimization wave 1 (dev dependency, script migration, isolated local tests, push and PR). No deployment, production access or runtime behaviour change; the only `src/` edit adds `export` to 23 pure admin editor helpers. Branch `claude/opt-vitest-runner`, stacked on PR #60.

- vitest 4.1.11 with `npm test` (vitest 5 requires Node 22; 4.x supports Node 20 and Vite 6.4.2 without a second Vite). npm 10.9.8 on the host failed `npm install` with an arborist `edgesOut` error; the lockfile was produced with npm 11, and `npm ci` with npm 10.8.2 in the Node 20 gate image passed.
- Migrated assertions (static `assert*(` count, before → after): forms API 119 → 119, Projects aggregate behaviour 116 → 116, public overlay 84 → 84, Stone workspace 67 → 67, Image QR 51 + 5 → 56, admin identity 19 → 19; total 461 → 461. Source-string checks remain in their scripts (Projects 206 → 207 and QR 11 → 12, the extra entry records a vitest failure). `check-forms-api.mjs`, `check-admin-runtime.mjs` and `check-image-qr-behavior.mjs` were removed; archived evidence still cites the first, so it joins the doc-path allow-list.
- New admin editor tests with Supabase mocked: Products 8, Media 6, Settings 4, Leads 2 (123 `expect` calls). Suite: 10 files, 73 tests, under 1 s locally.
- Mutation proof: changing the Products hero-image publish gate (`heroMediaId.value === null` → `=== undefined`) failed exactly one test, "locks Publish until the product has a short description and a hero image" (1 failed, 72 passed); after the revert, 73 passed.
- `agent:verify --plan --base HEAD` with a temporary edit: `src/pages/admin/AdminLeadsPage.test.ts` → tooling/container, deploy=false, 24 checks including `unit`; `src/pages/admin/AdminLeadsPage.tsx` → runtime, deploy=true, 25 checks including `unit` and `browser`. The branch as a whole is runtime (package.json).
- Clean Node 20 gate: attempt 1 failed `state` (summaries not regenerated after the task update); attempt 2 failed `paths` (archive citation of the removed forms script); attempt 3 on `72e4f92` plus the doc-path allow-list passed all 24 checks (`unit` 1.7 s). Host no-config browser gate passed 12 routes. `check-verification.mjs` covers the unit node and the test-path classification.
- Residual: PR CI and Jay's merge decision are pending. The package.json and lockfile diff will likely need a rebase after the repo-hygiene and framer-motion branches. `tests/*.test.ts` are migrated JavaScript and are not type-checked; the admin editor tests in `src/` are type-checked by `tsc -b`.
## 2026-09-26 — Project point marker (NOW-OPT-PROJECT-POINT-MARKER-001)

Scope: Jay's approved marker spec only (task authorization: branch, local tests, push, PR, branch Preview and smoke; no merge, production write, email, DNS or migration). Branch `claude/opt-project-point-marker` from `e3be4ce`, stacked on PR #60. Changed `src/components/projects/ProjectHotspotImage.tsx` (marker button, halo replay, IntersectionObserver start), a localized `.urblo-hotspot-marker*` block in `src/index.css`, and the `Project point marker — 2026-09-26` decision in `docs/DESIGN.md`. Card, legend, deep-link and close logic are unchanged. The previous marker measured 28px on production (the task note said 36px).

- Values: 44px transparent button; 14px dot, 2px `--urblo-lime` border, white fill, `0 0 0 1px rgba(0,0,0,0.72), 0 1px 4px rgba(0,0,0,0.35)`; halo lime disc 1x→3x, opacity 0.6→0 over 70% of a 2s ease-out cycle, 3 iterations, started at 35% image visibility and replayed on marker hover/focus; dot `scale(1.3)` on open, `:focus-visible` and hover-capable `:hover`, 160ms; keyboard focus ring on the dot `0 0 0 2px #fff, 0 0 0 4px #000, 0 0 0 6px #fff` (PR #64's black ring framed in white). Reduced motion hides the halo and drops the transition.
- Local Playwright (Chromium, Vite dev, static fallback `moon-gate-woolley-street` with `?point=angola-black-marker`): 30/30 checks passed — dot geometry and colours, no centre dot, 44px hit area at 1440 and 375, no halo before viewport entry, 2s x3 then rest at opacity 0, hover/focus/open scale 1.3, hover replay, hover opens and leave closes, click pins, second click, Esc and outside click close, focus ring, reduced-motion static dot, 375 tap toggle, card inside the frame and viewport, legend pin, no horizontal overflow, deep link pins and scrolls.
- Before captures: production `https://urblo.com.au` (read-only GET) at 1440/375 for Artisan Park (light paving, mean crop luminance 187) and Moon Gate (dark stone, 84); saved under ignored `.tmp/marker/`.
- Delivery: runtime commit `d95ca1b` passed the clean Node 20 container gate (27 checks) plus host `agent:admin-config-gate` (12 routes) and `git diff --check`. PR #67 (stacked on #60) CI run `36148759867` passed; branch Preview `https://838c5d5b.urblo-site.pages.dev` passed `agent:cloudflare-preview-smoke`. Read-only Playwright on that Preview (production data, no writes): Artisan Park 30/30 including the `?point=` deep link from the New Grey stone page, Moon Gate 28/28 (no deep-link id supplied). After captures at 1440/375 for both projects plus a 250ms pulse frame sequence, focus and 375 tap captures are in `.tmp/marker/`. First 375 captures were covered by the existing welcome popup; recaptured in a returning-visitor state without runtime change.
- Not verified: real touch hardware, Safari/Firefox rendering of the halo, and a live keyboard session by a person. Jay's visual acceptance and merge decision are outstanding; no merge, production write, email or DNS change.

## 2026-09-26 — Optimization wave 1 release

Jay approved merging all wave 1 PRs ("合并") and, after the first production readback false failure, "继续合并". Claude rebased each PR onto main in order and merged with merge commits: #61 `ea77563` (CI 36151973670), #62 `b75cca5` (CI 36153157216), #63 `af7ca33` (CI 36154225145), #64 `7f34682` (CI 36155029231), #65 `ae029de` (CI 36155997367), #66 `518a4b6` (CI 36156948535), #67 `e1548af` (CI 36157672301). Each PR's own CI (checks, isolated local:verify, Preview deploy and smoke) passed on the rebased head before merge.

Conflict resolutions: WORKLOG and tasks.json note conflicts were unioned (both sides kept in order); generated HANDOFF/NEXT_STEPS/README were regenerated with `agent:state`. #65: `src/index.css` kept only the route-transition block because #64 had already moved the container media queries into `@layer components`. #66: `package.json` kept main's dependency classes plus vitest; the lockfile was regenerated with npm install; `check-verification.mjs` and `check-doc-paths.mjs` kept both sides; the Stone media assertions #63 had added to `check-public-content-overlay.mjs` were ported into `tests/public-content-overlay.test.ts` (74 tests pass) because #66 turned that script into a vitest wrapper. #67: the marker `className` line kept #67's version over #64's focus classes.

Production run 36153157216 (after #62) failed only at the apex/www readback step: `/assets/Projects-*.js` returned the SPA shell because the apex edge had not yet converged on immutable `3e842e58`. The same smoke against apex and www passed from the workstation minutes later; the deployment itself was healthy. Later production runs passed the same step. The CI readback needs a convergence wait (NOW-OPT-AGENT-DOCS-SLIM-001).

Final readback on `e1548af3df842874e0f995d7cb34afcd18a3fb40`, immutable `https://21ec9a4c.urblo-site.pages.dev`: `agent:cloudflare-preview-smoke` passed on the immutable URL and on apex and www with the immutable reference. Read-only Playwright on production: `/stone-library` requested 0 Storage originals and 18 render variants; `/projects/artisan-park-yarrabend` markers measure 14×14px in a 44×44px hit area with lime border and white fill; Contact "Stone Library" and "Send enquiry" buttons render on one line at 1440px (`white-space: nowrap`); the footer is not in the first viewport at load. Lighthouse 13.5 single runs on production: `/` mobile 85 (LCP 4.3s, CLS 0.001, 3.69MB), desktop 98; `/stone-library` mobile 93 (LCP 2.9s, 1.40MB), desktop 99 (0.72MB); `/stone-library/harcourt` mobile 81 (LCP 4.7s, 1.08MB), desktop 98 (LCP 1.1s); `/products/prime-block` mobile 79 (LCP 5.3s, 2.44MB), desktop 99; `/projects/artisan-park-yarrabend` mobile 92, desktop 98. CLS was <= 0.002 on every run. Audit baseline for comparison: mobile performance 54–80, CLS 0.14–0.44 on 8 of 9 pages, product detail 52MB, stone detail desktop LCP 23.7s.

Not verified: real-user Core Web Vitals; Safari/iOS; the admin stop-gap in production by a colleague (only isolated local journeys and mocked before/after runs). Seven wave 1 tasks are archived. Wave 2 tasks are registered with Jay's 2026-09-26 approval; Jay decided the homepage hero headline keeps its letter animation. This batch record does not deploy.

## 2026-09-26 — Colleague quick guide and Help entry (NOW-OPT-ADMIN-GUIDE-001)

Scope (Jay's wave 2 approval of 2026-09-26; owner decisions: English only, desktop only, non-technical colleagues): branch `claude/opt-admin-guide`, stacked on PR #68. No production writes, no login to any environment, no email.

- `docs/ADMIN_EDITOR_GUIDE.md` is now a 36-line (29 non-empty) task-based quick guide: sign in; Save vs Publish vs Update live page; change a project image (and a Stone photo); add/update a project and mark materials; edit a product; handle an enquiry or sample request; update company addresses and contact details; who to ask. It describes only controls that exist on main now; the wave 2 shared feedback/unsaved-changes dialogs are not described yet.
- Developer material (handoff status, roles, account setup and Auth, lifecycle per module, public fallbacks, media and storage, leads export, verification and rollback) moved to the new `docs/ADMIN_OPERATIONS_RUNBOOK.md`, linked from `docs/PROJECT_MAP.md`, `docs/ARCHITECTURE.md` and the `auth-settings` module rules. The stale Stone text (the "publish in Media first, then return to Stone Library" chain and the "new workspace candidate" section) and the retired "Projects task workspace"/"child-save isolation" wording were dropped. The previous guide stays readable with `git show e1548af:docs/ADMIN_EDITOR_GUIDE.md`.
- `docs/ADMIN_UX_RESHAPE_PLAN.md`: completed phases (0, 1, Stone Library, wave 1 stopgaps, Phase 3 guide shrink) absorbed into the runbook's Completed admin reshape record; the plan now lists only the five rules, remaining work (Articles, Products, Media, Settings/Leads/Change history, shared layer, Phase 3 leftovers), acceptance, anti-goals and the sunset clause. Full original: `git show e1548af:docs/ADMIN_UX_RESHAPE_PLAN.md`.
- Help entry: `src/pages/admin/AdminHelp.tsx` adds a **Help** button beside Refresh/Sign out in `AdminShell` (all ten module routes), on the login and account-setup headers, and on the shared access-state card (loading, unauthorized, configuration and error states). It opens a right-hand sheet (Escape, backdrop or Close; focus trapped and returned) that renders the guide Markdown bundled at build time with `?raw` through `src/pages/admin/adminGuide.ts` (unit tests in `adminGuide.test.ts`). `AdminApp.tsx` is unchanged.
- `scripts/check-admin-guide.mjs` (`npm run agent:admin-guide`, graph node `guide` in the docs and admin suites): ≤45 lines, ≤32 non-empty lines, ≤3,600 characters; the eight task headings; no technical terms (Supabase, bucket, migration, localhost, handoff, storage, deploy and similar); only the Markdown subset the sheet renders; every bold label (39 unique) must exist as a whole string or JSX text in `src/pages/admin` or `src/features/stone-library`. A negative run with an invented label and "Supabase bucket" failed as expected. Because the guide is now a build input, `pathCategory` classifies `docs/ADMIN_EDITOR_GUIDE.md` as runtime (classifier test added). The guide assertions in `check-harness.mjs` and `check-admin-handoff-readiness.mjs` now target the runbook.
- `admin-handoff-evidence.json` `editorGuideUsability` stays `missing`; its note records what was verified and that a colleague still has to complete two guide tasks on the deployed interface.

Local verification on the working tree that was committed: clean Node 20 container gate passed (26 checks including `guide`, `unit`, `coverage`, `knip`); host `npm test` 11 files/77 tests; host `npm run build`; host `agent:admin-config-gate` 12 routes; `agent:harness-gc` no errors; `git diff --check` clean. Headless Playwright (Chromium, Vite dev, fake Supabase origin answered in-process, synthetic owner session, no network to real services): Help button present once on `/admin`, projects, products, leads, settings, stone-library, media, image-qr, articles and audit with the shell rendered; the sheet shows the title and eight sections; at 1440×900 the whole guide fits without scrolling (scrollHeight = clientHeight = 835); Escape closes and focus returns to Help; login page Help opens the sheet; zero page errors. Screenshots in ignored `.tmp/guide/`.

Not verified: a colleague using the guide; the Help sheet on the production admin; widths below desktop (desktop only by owner decision).

Preview evidence: the coordinator pushed runtime commit `86b648e` and opened PR #69 (base main, stacked on #68). CI run `36160848011` passed `verify-and-deploy` and `quality` on `86b648ef4c1c526f6eca870abd6f0d06b5e6077d` and deployed the branch Preview `https://445df750.urblo-site.pages.dev`. `agent:cloudflare-preview-smoke` passed on that Preview. A read-only headless Playwright check (Chromium 1440×900, no credentials, no login; Preview uses production data) opened `/admin/login`. It found one Help button, and the sheet opened with the title "Urblo admin quick guide" and eight sections. The guide fit without scrolling, and Escape closed it. There were zero page errors and zero non-GET requests. Screenshots are in ignored `.tmp/guide/preview-login-help*.png`. The signed-in shell Help button was verified only locally with synthetic auth. The task is awaiting a colleague's two-task usability test and Jay's merge approval.
## 2026-09-26 — Edge SEO head, 404 and sitemap (NOW-OPT-SEO-EDGE-HEAD-001)

Scope: task authorization (branch, Pages Functions middleware, sitemap generation, push, PR, branch Preview and smoke; merge excluded). Branch `claude/opt-seo-edge-head` from `5b3e68c`, stacked on PR #68. No production data, Storage, Settings, email, credential, DNS or zone change was made.

- Design: `functions/_middleware.js` → `functions/_lib/edge-seo.js` (request handling, public reads, caching) and `src/lib/edgeSeo.ts` (pure model: static registry + Published rows → status, head, sitemap). `/api/*` and `/image/*` pass straight through; static files and `_redirects` answer first; only the SPA shell (detected by `<div id="root"></div>`) is rewritten with HTMLRewriter. Unknown paths, archived static projects and managed/unpublished stones get 404 with the same shell; trailing-slash variants 301 on the same origin (leading slashes collapsed, so `//host/` cannot become an open redirect); `/admin/*` passes through unchanged; `/sitemap.xml` is generated. `public/_routes.json` now includes `/*` and excludes `/assets/*`, `/fonts/*`, `/media/*`, `/images/*`, `/downloads/*`.
- Data boundary: Published rows are read over REST with the browser-safe key the bundle was built with, emitted by a Vite plugin as `seo-edge-config.json` (the build fails on a secret/service-role key; the path answers 404 publicly and is read through `env.ASSETS`). RLS decides visibility exactly as in the browser; no service-role key is read (readiness check enforces it). Cache: 5 min fresh, 24 h stale-while-revalidate in isolate memory plus the Cache API; a detail miss older than 30 s rechecks once; a failed CMS read serves the unchanged shell for undecidable detail slugs instead of a 404.
- Hydration: CMS detail heads come from `src/lib/publicContentSeoMeta.ts`, now also used by `PublicContentSeo`, so server and client values are identical (unit-tested); tags are upserted in place (one title/canonical/description/JSON-LD after hydration). `TitleUpdater` skips a path whose `urblo:edge-seo` marker says `cms`. CMS share images use the 1200×630 render variant on both sides.
- Homepage title: production `site_settings.seo.title` is `Urblo` (audit). Code floor in `getSeoMetaForPathname`: a CMS homepage title under 20 characters (or description under 50) is ignored, so the rendered title is `Urblo | Natural Stone Streetscape Systems` without a Settings write. Optional for Jay: Admin → Settings → Homepage search settings → SEO title `Urblo | Natural Stone Streetscape Systems`.
- Also: meta descriptions clipped at 158 characters drop a dangling comma before the ellipsis (audit P2-6); the preview smoke now expects 404 for the unknown route, checks one canonical/title/og:image/edge marker per public route, trailing-slash 301s and the generated sitemap, and verifies a www→apex 301 when that rule exists.
- www → apex: the agent has no Cloudflare API token (CI's token is a GitHub secret), so the rule was not created. Exact rule for Jay (Cloudflare dashboard → urblo.com.au zone → Rules → Redirect Rules → Create rule): name `www to apex`; when incoming requests match custom filter expression `(http.host eq "www.urblo.com.au")`; then Dynamic redirect, expression `concat("https://urblo.com.au", http.request.uri.path)`, status 301, preserve query string on. Readback: `curl -sI https://www.urblo.com.au/projects/xavier-college?x=1` → `301`, `location: https://urblo.com.au/projects/xavier-college?x=1`. The workflow's www readback smoke then switches to redirect verification automatically.
- Verification: clean Node 20 `npm run gate` passed 25/25 on `116b804` (two earlier attempts failed on a WORKLOG trailing blank line and on `public-readiness` still requiring the old `_routes.json`; both fixed), host `agent:admin-config-gate` 12 routes, `npm test` 11 files / 89 tests (new `tests/edge-seo.test.ts` 15). PR #71 CI run `36161583511` failed at `local:verify`: the isolated local Functions copy lacked `public/articles/index.json` imported through the route registry; `scripts/local-app.mjs` now copies it (`4cd9942`, gate 26/26). CI run `36162258620` passed both jobs, including isolated `local:verify`, and deployed Preview `https://614c9ceb.urblo-site.pages.dev`.
- Preview (production data, read-only GETs): `agent:cloudflare-preview-smoke` passed (edge head per public route, sitemap 54 URLs, trailing-slash 301s, functions boundary). curl first responses: `/projects/the-glen` 200 `x-urblo-edge-seo: cms`, title `The Glen Stone Streetscape Project | Urblo`, self canonical, og:image `render/image/...?width=1200&height=630`; `/stone-library/ausred` 200 cms `AusRed Dolomite | Urblo Stone Library`; `/products/prime-block` and the Aitken article 200 static with self canonical; `/` title `Urblo | Natural Stone Streetscape Systems`; `/this-does-not-exist-xyz` and `/products/not-a-real-product` 404; `/projects/` and `/stone-library/harcourt/` 301 to the slashless path; `/sitemap.xml` 200 `application/xml`, 54 URLs containing all 13 projects and 6 stones the audit found missing. Headless Chromium after hydration: exactly one title/canonical/description/og:title/JSON-LD per page, values equal to the first response; the unknown path renders "Page not found" with `noindex,follow`.
- Not verified: production (not merged), real crawler/social debugger fetches, Cache API behaviour under production traffic, Safari/Firefox. The `www` rule and merge remain with Jay. Noted for content follow-up: `/stone-library/harcourt` displays as Mica Grey and several project share images are `gemini-generated-image` files (audit P2-5/P2-7).
## 2026-09-26 — Design tokens and shared components (NOW-OPT-DESIGN-TOKENS-COMPONENTS-001)

Scope: wave 2 task authorization (branch, isolated local tests, push, PR, branch Preview and smoke, DESIGN.md and task state; no merge, production write, email, credentials or migration). Branch `claude/opt-design-tokens-components` from `5b3e68c`, stacked on PR #68. Changed `tailwind.config.js` (the misplaced `primary` removed; colour, type-scale, tracking and spacing tokens), `src/index.css` (`--urblo-gutter`, `--urblo-section*`, button variant classes, `.urblo-hero-title`), new `src/components/ui/` (Button, SectionHeading, PageIntro, Card, Reveal, class builders, 18 vitest tests), `SiteHeader` (page gutter), `HomepageSections.tsx` and `CapabilitiesPage.tsx` (adoption), `scripts/check-public-ui-tokens.mjs` with `scripts/public-ui-tokens-baseline.json` (graph node `ui-tokens` in the smoke suite), and DESIGN.md Visual System → Tokens.

- Arbitrary `text-[Npx]` in public pages/components/layouts (admin and QR page excluded): 327 on `5b3e68c` (the audit's 344 used a broader grep), 159 after. HomepageSections 100 → 0, CapabilitiesPage 68 → 0 (100%). The baseline records 159 per file and total; the check fails on any increase. The unreachable homepage sustainability/tabbed section (about 420 lines, rendered nowhere since the DESIGN.md decision) was removed rather than tokenized; its data in `homepage.ts` is kept.
- Local Playwright (headless Chromium, env-less `vite build`, `reducedMotion: reduce`, returning visitor) before/after captures at 375/768/1440 for Home, Capabilities, Projects, Stone Library and 404 under ignored `.tmp/tokens/{before,after,compare}/`. Logo left edge vs page content edge: before 20/30.7/57.6 vs 24/40/94; after 24/40/94 vs 24/40/94 (0px) on all five pages. Pixel diff below the 102px header: 0 changed pixels on Projects, Stone Library and 404 at all three widths; only the header band changed. Page heights: Home 1440 5804 → 5788, Capabilities 1440 10244 → 10316, 768 17242 → 16497. No console errors. Stone Library at 768 already overflowed horizontally before the change (838px document), unchanged.
- Deliberate visual changes for Jay: header, Home hero and Capabilities hero move onto the page gutter (hero text 57.6 → 94px at 1440); Home "Our Capabilities" and "View project" pills become 4px-radius ghost buttons (700 weight, 0.12em); Home H2s "Stone has always shaped cities." and "Explore our latest products" move from Space Grotesk (the latter uppercase) to the Avenir semibold section level; Capabilities sidebar and next-step H2s become section level (semibold); type sizes snap to the scale (for example partner banner 38/52/60 → 34/44/64, metrics numbers 72/96 → 64/104, small 10–11px labels → 12px); section padding snaps to 72/96 (Home metrics was 80/112, Capabilities 64/96); muted text is `#63666D`.
- Delivery: commit `24e442e` passed the clean Node 20 container gate (26 checks including `ui-tokens`, `unit` 92 tests and `knip`), the host no-config browser gate (12 routes, Node 22) and `git diff --check`. PR #72 (stacked on #68) CI run `36162027777` passed, including isolated local verification and immutable deployment smoke. Branch Preview `https://79c51662.urblo-site.pages.dev` passed `agent:cloudflare-preview-smoke`. Read-only Playwright on that Preview (production data, no writes) saved 375/768/1440 captures of the five pages under `.tmp/tokens/preview/`. Logo and content edge match at 24/40/94px on every page, with no console errors. Page heights differ from the local captures because the Preview has the full CMS collections.
- Not verified: Safari/iOS, real touch hardware and a person's keyboard pass. The Home Latest Projects fixed-height panel at 768 still clips its lower controls (the facts row and View project); this is pre-existing and less clipped than before. Jay's visual approval of the listed changes and the merge decision are outstanding. No merge, production write, email or DNS change.
## 2026-09-26 — Admin shared UX layer (NOW-OPT-ADMIN-SHARED-UX-001)

Scope: Jay-approved optimization wave 2 task (branch, isolated local tests, push, PR, branch Preview and smoke; no merge, production write, email, credentials or migration). Branch `claude/opt-admin-shared-ux` from `5b3e68c`, stacked on PR #68. No schema, Function or RLS change; the Products/Articles aggregate rewrite was not started; `AdminApp.tsx` and `AdminShell.tsx` are untouched (Sign out and Refresh are intercepted by the guard's document click handler, as Stone and Projects already do).

- Errors: new `src/lib/adminErrors.ts` translates Supabase/PostgREST/Postgres/Storage/Auth/network errors (whole responses keep the HTTP status). Unique violation → URL key / model website key / material category / email / file-specific or generic duplicate sentence; 42501, 403 or RLS text → "Your account is not allowed…"; 23503 → linked item changed or in use; 23502 → required field; 23514/22P02/22023 → value format; PGRST301/401/"JWT expired" → "Your sign-in has expired…"; fetch/Load failed/NetworkError/offline → "Could not reach the website server…"; 5xx/timeout → server problem; PGRST116/404 → changed or removed; 413 → file too large; anything else → plain fallback. The original text only appears under a Details disclosure. Media's composed promotion/rollback messages were rewritten as plain sentences with the technical text moved to Details.
- Feedback: `AdminFeedback` + `useAdminFeedback` replace the per-page `error`/`notice` blocks at the bottom of the aside. Messages render beside the action bar that produced them (product, model, spec, material default; article, section; media, upload; lead workflow; site settings, invite, access) with always-mounted `role="status"` (polite) and `role="alert"` regions. Success clears after 6 s; errors persist with a dismiss button.
- Unsaved guard: `useUnsavedGuard` (StoneDialog pattern) compares each form with the record it was loaded from. Record switch, New, in-app links, browser Back, Sign out and Refresh prompt Save and continue / Discard changes / Keep editing; `beforeunload` warns on refresh/close; navigation is blocked while a save is running. Save is offered only when exactly one section is unsaved; with several (e.g. product details and a spec) the dialog asks to save each part or discard, because the older Products editor reloads sibling forms after each save. Settings' invite and access forms report their unsaved state with Discard only.
- Archive confirmation: Archive on a live product, a published model of a live product, a live article, a published section of a live article, and published media opens a confirmation naming the public page (e.g. "removes it from the public page at /products/synthetic-bench straight away… you can publish it again later"); Keep editing writes nothing.
- Checks: `check-admin-crud-coverage.mjs` adds `checkAdminSharedUxLayer()` (every module uses the feedback hook/component and the guard; no `setError`/`setNotice`/`setInfo` argument contains `.message`; no `new Error(x.message)` re-wrap; no page-local error state; live Archive goes through `archiveConfirmRequest`; journey markers; tsx behaviour test of translator and guard helpers). A probe re-introducing `setError(response.error.message)` in Leads failed the check. All existing required/forbid assertions kept. New vitest files: `src/lib/adminErrors.test.ts` (26 cases), `src/pages/admin/unsavedGuard.test.ts`, `src/pages/admin/AdminFeedback.test.tsx`; suite 13 files / 113 tests.
- Local journeys: new `scripts/local-journeys/products.mjs` (save feedback within 1 s in the polite region; real duplicate URL key 409 translated with `products_slug_key` in Details; record-switch Keep editing/Discard; network abort translated with the edit kept; `beforeunload` blocked while dirty; Sign out prompt; nav link Save and continue writes then lands on Media) and `media.mjs` (record-switch guard; simulated 401 PGRST301 translated; live Archive confirmation with zero writes; nav Discard). `articles.mjs` now expects the translated 500 message with raw text in Details, confirms live Archive, and ends by discarding its unsaved draft.
- Evidence: clean Node 20 `npm run gate` passed 25 checks on the code tree (`.tmp/admin-shared-ux/gate-1.log`); host (Node 22.22.3) `agent:admin-config-gate` passed 12 routes; `npm test`, `npm run lint`, `tsc -b` and `agent:admin-crud-coverage` passed. Workstation `local:start` refused: "Local container belongs to a different checkout"; the shared stack was left untouched, so the new local journeys run only in PR CI `local:verify`. Mocked-network headless Playwright (static build with a fake Supabase origin, all traffic answered by route mocks, synthetic rows) passed 33 checks across the five modules: duplicate, permission denied, expired session and network failure each showed the plain sentence with the raw text hidden until Details, success appeared 42–47 ms after the click with `aria-live="polite"`, record-switch/navigation/Sign out guards, live Archive confirmations, and Leads Save and continue writing the notes before switching. Results and screenshots: ignored `.tmp/admin-shared-ux/evidence/`.
- CI failure 1: PR #73 run `36162088858` (head `8101b0a`) passed the classified graph but failed `local:verify` in the new step "Products navigation guard saves then continues to the chosen module". All earlier journeys passed, including the new Products save-feedback, duplicate-URL-key, record-switch and network/refresh/Sign out checks. Save and continue wrote the product and navigation reached `/admin/media`. The immediate `beforeunload` probe then returned `true` instead of `false`. The URL changes before the lazily loaded Media screen replaces Products, so the probe measured the page during that transition. The CI failure screenshot shows Media still loading. Fix: the journey now waits until Media is ready (External media enabled) and polls the probe until the page has settled. The assertion itself is unchanged. The mocked run also covers Products → Media after Save and continue and reads `false` both at once and after 1.5 s. The product code is unchanged by this fix.
- CI failure 2: retry run `36172238648` (head `ca07c03`) passed the whole Products journey, including the fixed navigation step, and the first Media step. It then failed in "Media expired sign-in is translated…": the live-save confirmation never opened. The failure screenshot shows the new feedback working as designed. Beside the Media action bar it reads "Publish is locked for now. Start with: Usage notes…". The seeded local media fixture is Published but has no usage notes, so the publish checklist stops Save before the confirmation opens. The mocked fixture had usage notes, which is why the mocked run passed. Fix: the journey also fills Usage notes in the form. That value is never written, because the save is answered with the simulated 401. The product code is unchanged.
- Delivery: CI run `36172983504` on `2d1d222` passed:
  - the classified runtime graph and the no-config browser gate
  - isolated `local:verify`: 26 journeys, including the 5 Products and 3 Media shared-UX steps and the updated Articles steps (evidence `.tmp/local/journeys-1790360797306/` in the run artifact, copied to ignored `.tmp/admin-shared-ux/ci-artifact-3/`)
  - the branch Preview deploy to `https://de547bd5.urblo-site.pages.dev` and its CI smoke

  A workstation `agent:cloudflare-preview-smoke --base-url https://de547bd5.urblo-site.pages.dev` also passed. It was read-only and did not log in. PR #73 is not merged; production is unverified.
- Not verified: real session expiry (RequireAdmin still redirects to login and drops unsaved edits; only the save-time JWT error is translated), Safari/Firefox, screen-reader output by a person, and colleague acceptance. The Leads/Products middle columns are narrow at 1440px (Save workflow overflows its bar); this comes from the unchanged page grid. Login, Dashboard, Stone, Projects and Image QR keep their existing error text.
## 2026-09-26 — Agent docs slimming and CI readback wait (NOW-OPT-AGENT-DOCS-SLIM-001)

Scope: task authorization only (docs restructuring, harness scripts, the CI production readback wait, push and PR; no runtime `src/` change, no other deployment behaviour change, no production access). Branch `claude/opt-agent-docs-slim` from `5b3e68c`, stacked on PR #68.

- Measure: new `scripts/check-agent-startup-budget.mjs` (graph node `startup-budget`, a dependency of `harness`; also run by `agent:check`) counts AGENTS.md + `status.startup.files` + module `rules`, per module and for three representative tasks: projects `NOW-STONE-PROJECT-REFS-001` (archived), products `NEXT-ADMIN-MODULE-BOUNDARIES-001`, public-ui `NOW-OPT-DESIGN-TOKENS-COMPONENTS-001`. It fails above 40,960 bytes per set or 8,192 bytes per rule file. Module `references` are on-demand and not counted.
- Before (bytes; base AGENTS + OPERATING_PROTOCOL + status.json + HANDOFF = 15,260): harness 25,499; delivery 78,896; qr 133,038; projects 133,038; articles 70,433; forms 133,038; stone-library 146,600; products 146,600; media 115,186; auth-settings 65,877; public-ui 83,995. Tasks: projects 175,619; products 246,526; public-ui 83,995. Nine rule files exceeded 8KB (ARCHITECTURE 88,759; SUPABASE_SCHEMA 70,907; DESIGN 42,581; CLOUDFLARE_DEPLOYMENT 31,487; ADMIN_IA_ACCESS 29,019; verification 27,371; brand-baseline 26,154; ADMIN_EDITOR_GUIDE 21,598; MAINTAINABILITY_AUDIT 10,239).
- After (base AGENTS + OPERATING_PROTOCOL + status.json = 12,020): harness 12,799; delivery 24,806; qr 14,966; projects 26,762; articles 16,901; forms 17,521; stone-library 20,875; products 14,917; media 15,083; auth-settings 22,724; public-ui 23,130. Tasks: projects 35,617; products 26,835; public-ui 23,130. Largest rule file 7,608 (`docs/architecture/projects.md`). Active five-module `NOW-ADMIN-STONE-WORKSPACE-001` still reads 56,230 and is reported, not failed.
- ARCHITECTURE split: `docs/ARCHITECTURE.md` is now an index; its sections moved verbatim into 19 files under `docs/architecture/` (all <= 7.7KB). A line-coverage script confirmed every non-empty original line is in exactly one new file except the `- Admin IA/access:` label, which is repeated as the parent of each module's fragment; four parent headings were replaced by prefixed headings (`## Deployment and Build Contract — …`, `### Supabase Launch Data Contract — …`). No `ARCHITECTURE.md#anchor` links existed. `check-public-supabase-readiness.mjs` now reads the index plus all module files. The Functions routing/source lines (former lines 66, 69, 101–107) sit unchanged and contiguous in `docs/architecture/delivery.md`.
- modules.json: every module has `rules` (<= 8KB, read at startup) and `references` (DESIGN, brand-baseline, ADMIN_IA_ACCESS, SUPABASE_SCHEMA, CLOUDFLARE_DEPLOYMENT, LOCAL_DEVELOPMENT, MAINTAINABILITY_AUDIT, `docs/agent/live-verification.md`, shared architecture files). `agent:init` prints both lines and now prints open external dependencies, so `docs/HANDOFF.md` left `status.startup.files`; HANDOFF says it is a human summary.
- Archive `docs/archive/2026-09-26/` with `manifest.json` (bytes + SHA-256, verified by `verifyArchive` in state/GC; tamper test added to `check-agent-state.mjs`): executed Stone adoption plan (229,392 bytes, SHA `b9a4e1f1…84f9f`, unchanged, so the approved plan SHA still matches; scripts default to the new path, the planner now writes to `.tmp`), full previous `status.json`, the removed `stoneWorkspace` block, and full previous `verification.md`. `verification.md` is 6,630 bytes of live rules plus a path→suite table; specialized sections moved to `docs/agent/live-verification.md`; only the executed Phase 1 Projects acceptance bullet and the adoption-plan command bullet were retired to the archive copy.
- Task ID prefixes: not renamed. WORKLOG, the immutable archive, branches and `status.json` cite the IDs, and the harness has no alias mechanism, so renaming would break traceability. OPERATING_PROTOCOL now states IDs are permanent, the prefix records the registration queue and `status` is authoritative. Mismatches today: NOW-FORMS-SUPABASE-001, NOW-ARTICLE-STRUCTURE-CLAIMS-001, NOW-ADMIN-UX-RESHAPE-001 (next); NOW-ADMIN-SETTINGS-CRUD-001, NOW-ADMIN-RELIABILITY-UX-001, NOW-PUBLIC-CATALOG-INTEGRITY-001, NEXT-DELIVERY-MAIN-PROTECTION-001 (blocked); NEXT-SAMPLE-REQUEST-001 (later).
- OPERATING_PROTOCOL also records Jay's per-wave release-record policy (PRs carry Preview evidence; production readback and the `status.json` release block once per wave) and the wave 1 docs-conflict rule (WORKLOG and task notes append-only, union-merged, generated views regenerated).
- CI: production run 36153157216 failed its apex readback because the edge still served the SPA shell for `/assets/Projects-*.js` (see "Optimization wave 1 release"). `deploy.yml` now runs `scripts/wait-for-domain-convergence.mjs` first: read-only GETs without following redirects, polling apex and www every 10s for up to 180s until `/` references the immutable entry assets and every asset in the immutable graph is byte-identical; the unchanged smoke then runs once per domain and the step fails if convergence never happened. `tests/domain-convergence.test.ts` reproduces the lagging-chunk case, non-convergence at the budget (19 attempts, t = 0…180s), redirect/transport handling and input rejection with injected fetch only.
- Verification: clean Node 20 container gate (`npm run gate`, v20.20.2) passed 27 checks including `startup-budget`, `unit` (82 tests) and `knip`; host `agent:admin-config-gate` passed 12 routes; `agent:check`, `agent:state:check`, `agent:harness-gc` and `check-doc-paths` passed. `agent:verify --plan` classifies the branch as runtime (because of `.github/`), so PR CI deploys a branch Preview and runs its smoke; the production readback step only runs on main.
- Cold-start drill: `npm run agent:init -- --task NOW-OPT-SEO-EDGE-HEAD-001` printed 3,146 bytes; with AGENTS.md, OPERATING_PROTOCOL, status.json and the listed rules (verification.md, delivery.md, public-routes.md, public-metadata.md, 35,916 bytes) the set is 39,062 bytes. It states the task and acceptance, the authorization included/excluded lists, AGENTS boundaries and the verification commands without reading history.
- Delivery: PR #70 (base main, stacked on #68). CI run 36161231695 on `ccc4fc4` passed verify-and-deploy (5m53s, including isolated `local:verify`, branch deploy and immutable smoke) and `quality`. Branch Preview `https://e4c29c01.urblo-site.pages.dev` also passed `agent:cloudflare-preview-smoke` from the workstation (read-only; Functions and protected boundaries ok). Not merged.
- Not verified: the convergence wait against the real apex/www edge (runs only on the next main release); no production access was used. Parallel wave 2 branches that edit `docs/ARCHITECTURE.md` (the SEO Functions edit) must re-apply their change in `docs/architecture/delivery.md` when rebasing; if the SEO task adds the www→apex 301, the www lines in this step (waiter and smoke) must change with it.

## 2026-09-26 — Optimization wave 2 release

Jay approved merging wave 2 and made merging a standing authorization ("合并，以后不要问我这种问题了"). Claude rebased each PR onto main in order and merged with merge commits: #69 `9732e9d` (CI 36207742405), #71 `d3c688d` (CI 36208117841), #72 `f90b723` (CI 36208496155), #73 `fc4ff3e` (CI 36208902070), #70 `194976d` (CI 36209299045). Each PR's own CI (checks, isolated local:verify, Preview deploy and smoke) passed on the rebased head before merge; every production run passed, including the first run of the new apex/www convergence wait from #70.

Conflict resolutions: WORKLOG and task notes unioned; generated summaries regenerated with `agent:state`. #72: `scripts/_lib/verification.mjs` smoke suite gained `ui-tokens` while the admin suite kept `guide` from #69. #70: `docs/ARCHITECTURE.md` took the index version and the three edits main had made since the split were ported into the per-module files (the runbook line into `docs/architecture/platform.md`; the `_routes.json` routing, edge SEO middleware and `/*` scope lines into `docs/architecture/delivery.md`; the first-response head paragraph into `docs/architecture/public-metadata.md`); `docs/PROJECT_MAP.md` kept both the rules/references sentence and the runbook/guide sentence. Tests after the last rebase: 157 passing.

Final readback on `194976d50b79bbad89b949f85aba3903e1a99ee7`, immutable `https://21b51af8.urblo-site.pages.dev`: `agent:cloudflare-preview-smoke` passed on the immutable URL and on apex and www with the immutable reference. curl on production: `/projects/the-glen`, `/stone-library/ausred` and `/products/prime-block` return 200 with entity titles in the first response; `/this-does-not-exist-xyz` returns 404 with "Page Not Found | Urblo"; `/projects/` returns 301 to `/projects`; `/sitemap.xml` lists 54 URLs; `https://www.urblo.com.au/` still returns 200 because the zone redirect rule is not yet created. Read-only Playwright on production: the header logo sits at 94px at 1440 (page gutter); "Our Capabilities" and "View project" render as 4px-radius outline buttons; `/admin/login` shows one Help button that opens the "Urblo admin quick guide" panel. Lighthouse 13.5 single runs on production: `/` mobile 82 (LCP 4.7s, CLS 0, 3.67MB), desktop 98; `/stone-library` mobile 92, desktop 99; `/stone-library/harcourt` mobile 91 (LCP 3.0s), desktop 98; `/products/prime-block` mobile 80, desktop 99; `/projects/artisan-park-yarrabend` mobile 91, desktop 98; CLS <= 0.002 everywhere.

Pending for Jay: the www→apex redirect rule in the urblo.com.au Cloudflare zone (Rules → Redirect Rules: if `http.host eq "www.urblo.com.au"` then dynamic redirect to `concat("https://urblo.com.au", http.request.uri.path)`, status 301, preserve query string); optional descriptive homepage SEO title in Admin → Settings (code fallback already restores it); content review of the Harcourt stone title ("Mica Grey Granite") and AI-generated project share images. Not verified: real crawlers and social debuggers; Safari/iOS; colleague acceptance of the admin changes and the quick guide. Five wave 2 tasks are archived; wave 3 is not yet registered. This batch record does not deploy.

# HANDOFF - Current Agent State

Last updated: 2026-06-04

## Current Focus
The launch direction is now Cloudflare Pages + Supabase + an Urblo-owned admin CMS:
- Public website hosting is now on Cloudflare Pages for production domains; remaining launch work is Supabase/admin/form operational proof rather than hosting cutover.
- Contact and Sample Request source now uses Supabase-backed Pages Function submissions instead of mailto/local-only main submit behavior; basic deployed production persistence and source-route audit rows are verified on `https://urblo.pages.dev`.
- `/capabilities` now renders a service-style web version of Natalie Ma's 2026 Capability Statement with a concrete capability hub, selected-project proof ledger, shared CTA data, Founder-sourced imagery, and an email-gated PDF download flow that posts to `/api/enquiries` as `Capability statement download`; live lead capture depends on the same server-side form credentials as Contact.
- `/projects` and `/projects/:slug` now use a reference-style project archive/case-study structure in Urblo visual language: page-owned opening, functional filters/view controls, equal-sized archive images, oversized detail opening, previous/next navigation, full-width hero/media, Project Information facts, ordered media blocks, hotspot inspector, Featured Materials where data supports it, and shared CTA.
- Desktop public navigation keeps primary links and the hamburger as one right-aligned group; Articles and Products stay inside the desktop hamburger, while mobile exposes the full navigation list in the hamburger.
- Projects, Stone Library, Products, Articles, media records, and lead records should become customer-maintainable through `/admin`.
- Public Projects, Products, Articles, and the Stone Library listing/detail now prefer published Supabase content with static fallback. Article detail bodies prefer Published Supabase `article_blocks` with sanitized legacy HTML fallback; Stone Library detail now reads Published Supabase families, variants, finish capabilities, and finish images before falling back to static data.
- The long-form plan and cost baseline live in `docs/SUPABASE_CLOUDFLARE_LAUNCH_PLAN.md`.
- Supabase project access is available through the connector for project `Urblo` (`npkidywzwddbnfrnxlmo`, `ap-southeast-2`); foundation and baseline seed migrations are applied.
- Cloudflare account access has been rechecked for the production zone: `urblo.com.au` is readable in Hunter's account (`077afae2c6f4e77badadf21e49e58eb7`), and the `urblo` Pages project now exists with default domain `urblo.pages.dev`. GitHub source is connected to `jayyy-3/jayyy-3.github.io`, latest verified CMS code deployment `bc830b5f-4c98-46ab-8962-586478ff9259` for commit `7a318ab` is successful, and `npm run agent:cloudflare-preview-smoke -- --base-url https://urblo.com.au` passes for the current deployment. Production custom domains `urblo.com.au` and `www.urblo.com.au` are attached, apex and `www` website DNS point to `urblo.pages.dev`, and Google MX/SPF/TXT, NS records, and `qa.urblo.com.au` were not changed.
- Cloudflare Pages production environment has the server-side form variables `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, SMTP2GO notification variables, and the browser-safe Supabase publishable key configured for the `urblo` project. Preview environment variables remain empty. Approved tagged live form QA writes were run against `https://urblo.pages.dev` and verified through Supabase connector readback for base persistence/audit. Approved tagged SMTP2GO proof against `https://urblo.com.au` returned `notificationStatus = sent` for Contact and Sample Request, and Supabase connector readback confirmed `enquiries.id = 3`, `sample_requests.id = 2`, `sample_request_items.id = 2`, and matching `admin_audit_events.id = 4/5`. Approved browser-key private-row boundary proof created `enquiries.id = 4`, `sample_requests.id = 3`, `sample_request_items.id = 3`, and `admin_audit_events.id = 6/7`; anonymous REST reads through the deployed publishable key returned HTTP 401 for the private lead tables. Turnstile remains unconfigured for the final bot-protection proof.
- Supabase foundation source readiness is now repeatable with `npm run agent:supabase-foundation-readiness`; it verifies the expected migration files, 24 launch tables, RLS source, anonymous read-only posture, baseline seeds, atomic Sample Request RPC, Storage bucket/listing hardening, private SECURITY DEFINER helper posture, and admin profile email uniqueness without credentials or live writes.
- Forms backend source is implemented for `/api/enquiries` and `/api/sample-requests`, including server-side audit-event attempts after successful lead inserts. Sample Request now uses a service-role-only Supabase RPC so the request row and first item row are created atomically. Basic deployed insert/audit verification, SMTP2GO notification delivery, and browser-key private-row denial are complete; final launch proof still needs Turnstile and admin lead workflow verification.
- Live form persistence verification now has a credential-gated and approval-gated runner: `npm run agent:forms-live -- --allow-writes`. It verifies valid enquiry/sample-request rows, sample item rows, server-side source-route audit metadata, invalid-payload no-row/no-audit behavior, response-vs-stored notification status, and browser-key private-row denial once a service-role key and browser-safe key are configured and Jay has approved tagged live form QA writes. SMTP2GO notification proof and browser-key private-row proof are complete for the current provider path; final Turnstile proof is explicit with `--require-turnstile --turnstile-token <token>`, which now refuses to start without `VITE_TURNSTILE_SITE_KEY` and requires stored `turnstile_success = true`. The runner intentionally fails without `--allow-writes` or required credentials and retains tagged test rows until cleanup is approved.
- Contact form UI source verification now has a no-secret runner: `npm run agent:forms-ui`. It guards the main submit flow against mailto/window-navigation regressions, checks `/api/enquiries` and `/api/sample-requests` routing, and verifies inline validation, success, error, submitting, sample-request fields, the optional `VITE_TURNSTILE_SITE_KEY` widget/token path, and direct contact fallback source contracts.
- `/admin` auth shell source is implemented and config-gated. Production now has the browser-safe Supabase publishable key in the deployed bundle. Supabase connector readback on 2026-06-03 confirmed Auth user `info@urblo.com.au` exists and is linked to one active `owner` admin profile; bootstrap audit row `admin_audit_events.id = 8` records `admin_profile.bootstrap`. No-write active-admin browser QA passed on 2026-06-04 against `https://urblo.com.au`: `npm run agent:admin-auth-browser -- --allow-login --strict --base-url https://urblo.com.au` verified all 9 authenticated route shells plus Sign out and wrote ignored screenshots under `.tmp/admin-auth-browser/screenshots`.
- Admin no-config browser verification now has a repeatable no-secret runner: `npm run agent:admin-config-gate`. It starts built Vite preview by default, checks all launch-critical admin routes in Firefox for the configuration-required state, rejects private admin/module text, and writes ignored screenshots under `.tmp/admin-config-gate/screenshots`.
- Admin configured-auth browser verification now has a gated runner: `npm run agent:admin-auth-browser`. It reads `.env.local`, `.env`, `.dev.vars`, and shell values without printing secrets. Default mode is plan-only. With `--allow-login --strict`, browser-safe Supabase config and `URBLO_ADMIN_EMAIL`/`URBLO_ADMIN_PASSWORD` are required before it signs in through `/admin/login`, checks authenticated admin route shells, verifies Sign out returns the protected route to `/admin/login?next=...`, and captures ignored screenshots under `.tmp/admin-auth-browser/screenshots` without creating content rows, Storage objects, or audit events. With `--allow-login --expect-unauthorized --strict`, it uses `URBLO_UNPROFILED_EMAIL`/`URBLO_UNPROFILED_PASSWORD` to verify a valid Auth user without an active `admin_profiles` row lands on `/admin/unauthorized`, then probes all launch-critical admin routes and keeps them on the unauthorized shell without private admin module content.
- `/admin` dashboard source now includes a real content health queue in addition to published-row metrics and recent lead signal. It checks published media missing alt/usage metadata, project claim-review rows, published Products/Articles missing key media, Stone Library TBC records, and stale new leads once live admin RLS access exists. `npm run agent:admin-crud-coverage` guards those checks source-side, and `npm run agent:admin-crud-live` now stages tagged dashboard-health predicate readback for the eventual approval-gated live run; live data QA now requires explicit tagged-write approval and an owner/admin session.
- `/admin` CMS UI/UX productization is now underway. The admin shell groups navigation into Work queue, Content library, and Operations; global copy explains that only `Published` content can appear on the public website; Dashboard now shows the edit-review-publish workflow plus Draft/Published/Archived counts for content modules; Projects has search/status filtering, a project-editor Publish checklist that locks Publish until title, URL, public copy, project proof review, fact proof review, and material proof review are ready, plus media pickers/previews instead of raw media ID entry for core project image links; Products has product-level and model-level Publish checklists so editors can see why a product or model cannot go public; Stone Library, Projects, Products, and Articles editor headers now show an Open public page control only when the current record is Published; and Articles now uses Website URL key, Migration note/link, Article sections, cover/section media pickers with Published in Media / Not published in Media previews, and an Article publish checklist that locks Publish until article metadata and published section content are ready.
- `docs/ADMIN_EDITOR_GUIDE.md` is the current editor handoff guide for `/admin`: it records the production admin URL, Website owner / CMS manager / Editor / Viewer roles, account setup path, Draft/Published/Archived rules, module editing flow, publish checks, current public fallback boundaries, and explicit handoff gaps including push/deploy plus final production walkthroughs. `npm run agent:check` now guards that guide so current editor-facing terms and handoff gaps stay present and older Supabase/Auth/Profile/Owner/Admin terminology does not drift back in.
- Admin credential/profile readiness now has a read-only runner: `npm run agent:admin-live-readiness -- --admin-email <first-admin-email>`. It verifies browser-safe key presence, service-role verification access, the named Auth user linked to the active admin profile, baseline seed rows, and the browser-key anonymous public/private REST boundary without mutating Supabase.
- First-admin bootstrap now has a default no-write operational runner: `npm run agent:first-admin-bootstrap`. The command prints the approved path without connecting to Supabase; `--verify-only` performs read-only service-role checks for the Auth user, exactly one active profile linked to that Auth user with the planned role, and baseline seeds; write/invite mode requires Jay approval plus `--allow-writes` and a matching `--confirm-email`. The live `admin_profiles` table now enforces case-insensitive normalized email uniqueness, and write mode refuses an email already linked to a different Auth user before upsert.
- Live verification input readiness now has a no-secret runner: `npm run agent:live-readiness`. It reports missing/present inputs for local/deployed form verification, final email proof, final Turnstile proof including the public `VITE_TURNSTILE_SITE_KEY`, first-admin readiness, first-admin profile/invite writes, no-write active-admin browser QA, no-write unprofiled unauthorized browser QA, tagged admin live writes, and Cloudflare preview smoke without printing secrets or mutating Supabase/Cloudflare. It accepts non-secret `--base-url`, `--admin-email`, `--form-writes-approved`, `--first-admin-writes-approved`, `--admin-writes-approved`, `--content-import-approved`, `--content-merge-approved`, `--content-public-cutover-approved`, and `--turnstile-token-provided` readiness flags; copied placeholders or malformed `--base-url`/`--admin-email` values remain missing, and `--strict` fails on missing/manual-gated live inputs.
- Actual live runners now share that manual-input boundary: `agent:forms-live`, `agent:cloudflare-preview-smoke`, `agent:admin-auth-browser`, and `agent:admin-config-gate` reject placeholder or non-origin `--base-url` values before any network/live-write work, `agent:admin-live-readiness` requires a real first-admin email shape, and active-admin/unprofiled browser QA plus admin CRUD live writes require real email-shaped login inputs rather than copied placeholders.
- Live verification input readiness now also reports the guarded static-to-Supabase content import apply, merge/upsert, and public read cutover approval gates. Use `--content-import-approved`, `--content-merge-approved`, and `--content-public-cutover-approved` only after Jay has approved those specific operations; these flags do not apply SQL, publish content, or switch runtime reads.
- Cloudflare Pages repo-side readiness remains guarded by the no-secret runner `npm run agent:cloudflare-readiness`. It verifies build settings, clean-route fallback, `/api/*` Function routing scope, launch headers, API handler files, env placeholders, and the deployment runbook if deployment contracts change.
- Cloudflare Pages deployed smoke is guarded by `npm run agent:cloudflare-preview-smoke -- --base-url <origin>`. It verifies direct-refresh public/admin route shells, recursively discovered deployed assets and route chunks, deployed admin config-required/profile-gate bundle markers, browser service-role env boundaries, Cloudflare-applied legacy redirects, and no-write API safe-failure behavior including malformed JSON. It passes on `urblo.pages.dev`, `urblo.com.au`, and `www.urblo.com.au`.
- `/admin` dashboard now acts as the editor start page: Start here quick actions point editors to Leads, content publishing, and Media; content status rows distinguish Draft/Published/Archived and route to the correct module including Stone Library; module cards use editor-facing handoff labels instead of Supabase/RLS dependency language; health labels avoid database terms; and the handoff panel explains account/role setup plus a real-content walkthrough instead of stale launch-secret setup.
- `/admin/settings` source is implemented for the default `site_settings` row plus CMS team access management for existing login accounts, including form validation for duplicate login setup codes and duplicate access emails before save. Supabase migrations `admin_settings_role_hardening`, `admin_profile_owner_hardening`, `security_definer_function_grants`, `security_definer_private_helpers`, and `admin_profile_email_uniqueness` are applied so settings writes require owner/admin, owner-role profile changes stay owner-protected, admin profile email lookups stay unique, public helper RPC execution is revoked from browser roles, and RLS/Storage policies call non-exposed private SECURITY DEFINER helpers. Settings now use shared CMS status language, include editor-facing guidance for public website settings versus CMS team access, edit footer columns/items through form controls instead of raw JSON, use Global contact and search defaults / Search defaults language, and explain team access through People and access, Access setup checklist, Copy setup code controls, Login setup code label, Grant CMS access wording, Website owner / CMS manager role labels, and Active access language.
- Supabase media Storage source is implemented. Migrations `media_storage_foundation` and `media_storage_listing_hardening` are applied, creating `urblo-public-media` and `urblo-admin-media` buckets with admin/editor object-write policies and no broad public object-listing policy.
- `/admin/media` source is implemented as a protected media library screen for upload-backed draft records, external records, metadata editing, activity-logged visible manifest export, and publish/archive validation. Approved live upload/save/export verification is complete, including the private Storage upload/readback/anonymous-denial proof. Media now uses shared CMS status language, searchable/filterable asset lists, editor-facing private/public library labels, File or link type and Website visibility location guidance, and a Media Publish checklist that locks Publish until source, public location, image alt text, and usage notes are ready. Media list fallbacks avoid exposing raw uploaded-file paths as primary labels.
- `/admin/stone-library` source is implemented as the first content CRUD screen. It can load Stone Library groups, variants, finish definitions, finish capabilities, finish image links, and media records from Supabase; active editor/admin/owner roles can save group and variant records, publish/archive them, update per-finish capability rows, and link finish imagery to approved media records once browser-safe Supabase config and an active profile exist. Stone Library group lists now support search/status filtering, Needs confirmation is treated as an editor-visible state counted with Draft rather than public-ready content, group/variant status help explains public visibility, finish capabilities display as Available/Needs confirmation/Not available, family/variant Publish actions are locked behind readiness checklists, and finish image links show selected-media previews plus a Publish lock that sends editors to publish the chosen media record in Media first.
- `/admin/projects` source is implemented as the next content CRUD screen. It can load and save project records, facts, material schedule rows, ordered media blocks, material maps, and hotspots from Supabase; active editor/admin/owner roles can draft, publish, and archive project/media-block/map/hotspot records. Project publishing now has a visible Publish checklist inside the Project editor for title, website URL, required public copy, project proof review, fact proof review, and material proof review; Publish is locked while blockers remain, and clicking a blocker scrolls/selects the affected Project/Facts/Materials section instead of only surfacing a hidden validation error. The visible editor language now explains Needs review, Approved for public use, and Deferred / keep private without exposing `claim_status` terminology; Projects also uses Website URL key and Structured detail wording instead of slug/JSON labels. Project cover/hero, material image, media block asset, material map image, and hotspot preview image fields now use Media library selectors/previews with Published/Draft/Archived in Media labels instead of raw ID entry or `#id` preview text. `supabase/migrations/20260603142359_project_media_blocks.sql` is applied and verified in the live target database.
- `/admin/products` source is implemented as the next content CRUD screen. It can load and save product families, product models, material defaults, and product specs from Supabase; active editor/admin/owner roles can draft, publish, and archive product/model records once browser-safe Supabase config and an active profile exist. Products now use shared CMS status language, searchable/filterable product lists, a visible website-visibility explanation in the product editor, product/model status help, editor-facing Website URL key and Model website key labels, URL-key list/search labels, media selectors/previews that show Published in Media / Not published in Media for hero and model images, Search title/description fields instead of product SEO JSON, a Product Publish checklist that locks Publish until name, URL, short description, hero image, published model, material defaults, and specs are ready, a Model Publish checklist that locks model publication until model website key/label/image are ready, and Stone Library status feedback plus Material reference wording for material defaults.
- `/admin/articles` source is implemented as the next content CRUD screen. It can load and save article metadata and structured article-section rows from Supabase; active editor/admin/owner roles can draft, publish, and archive article/section records once browser-safe Supabase config and an active profile exists. Articles now use shared CMS status language, searchable/filterable article lists, URL/date list labels, article status help, editor-facing Website URL key and Migration note/link labels, media pickers/previews that show Published in Media / Not published in Media for cover and section images, Search title/description fields instead of article SEO JSON, Article sections instead of visible block-row language, and block-type-specific content forms instead of direct block content JSON entry.
- `/admin/leads` source is implemented as the first lead workflow screen. It can load enquiries, sample requests, sample request items, active admin profiles, Stone Library labels, and finish labels from Supabase; active lead-manager roles can update lead status, assignment, internal notes, and export the currently visible filtered queue to CSV once browser-safe Supabase config and an active profile exist. CSV export is blocked unless change history is recorded first, and export metadata records the active kind/status/search filters plus total loaded rows. Leads now have searchable/filterable inbox queues by kind and workflow status, editor-facing website-source labels, friendly email-delivery/spam-check badges, status-specific Recommended next step guidance, Workflow rules, team-role labels, Stone not selected / Finish not selected sample-item labels, and editor-facing CSV export headers for the visible queue.
- `/admin/audit` source is implemented as the Change history screen. It can load saved-change/export/publish activity and CMS team labels from Supabase; Website owner and CMS manager roles can inspect readable action/area/detail history once browser-safe Supabase config and an active profile exist. The route remains `/admin/audit` for compatibility, but visible CMS language now uses Change history with friendly action/entity labels.
- Admin Settings, Media, Stone Library, Projects, Products, Articles, and Leads save flows now call `src/lib/adminAudit.ts` after successful primary mutations. Stone Library activity-log coverage now includes finish image create/update/publish/archive source actions. Activity-log insert failure is surfaced in the success notice using owner/admin follow-up language and does not roll back the primary save. Live activity row creation verification still requires browser-safe Supabase key configuration and an active admin/editor or owner/admin profile.
- Admin source coverage now has a no-secret runner: `npm run agent:admin-crud-coverage`. It verifies `/admin` route/module registration, protected shell coverage, expected Supabase table references, role-gated controls, validation/save state paths, publish/archive lifecycle paths, structured Article block authoring guardrails, audit writer actions, Media/Leads export audit gates, and absence of destructive delete/RPC/control regressions without mutating Supabase.
- Admin live write verification now has a credential-gated runner: `npm run agent:admin-crud-live`. Default mode is plan-only and performs no writes. With `--allow-writes`, it requires a browser-safe Supabase key plus a real owner/admin Supabase Auth session via `URBLO_ADMIN_EMAIL`/`URBLO_ADMIN_PASSWORD` or `URBLO_ADMIN_ACCESS_TOKEN`; `URBLO_FIRST_ADMIN_EMAIL` is only for bootstrap/readiness and is not accepted as a live-login fallback. The verifier creates tagged QA rows plus exact expected audit actions through browser-key RLS, verifies dashboard-health predicates against tagged QA rows before archive cleanup, publishes then archives public-facing QA rows, and verifies tagged archived public-content QA rows plus private lead QA rows are not anonymously visible. It does not physically delete data. The approved 2026-06-04 Storage run passed with marker `admin-live-1780497462544-23b1d5e3`, uploaded `urblo-admin-media/live-check/admin-live-1780497462544-23b1d5e3.png`, recorded 48 audit rows, verified signed-in admin readback, and verified anonymous browser-key denial through private/public Storage object endpoints.
- Static-to-Supabase content import now has both a no-write dry-run command and an approval-gated live draft runner. `npm run agent:content-import` maps current static Stone Library groups, variants, finish capabilities, finish image links, Products, Projects, Articles, and media candidates into Supabase-shaped draft payloads and fails on missing assets or unknown stone/finish references. `npm run agent:content-import:live` is plan-only by default; with `--allow-writes`, browser-safe key, and owner/admin credentials it imports/upserts the reviewed payload through normal browser-key RLS, keeps all status-bearing rows as `draft`, and verifies anonymous public reads expose zero imported draft parent rows.
- The approved 2026-06-04 live content import passed for `info@urblo.com.au`, writing/upserting 115 media assets, 13 stone groups, 15 variants, 153 finish capabilities, 53 finish image rows, 6 products, 28 product models, 18 material defaults, 18 specs, 5 projects, 41 project facts, 2 project materials, 1 project material map, 15 project media rows, 2 hotspots, 4 articles, and 95 article blocks. Stored parent counts are 13 stone groups, 6 products, 5 projects, and 4 articles, and anonymous browser-key reads returned zero imported draft parent rows.
- Public Supabase cutover readiness now has a no-write source verifier for content import/public-read cutover: `npm run agent:public-supabase-readiness`. It verifies static-to-Supabase import rows remain `draft`, structured article blocks do not regress to placeholder/newsletter-artifact imports, guarded draft apply/rollback SQL keeps its safety gates, public RLS policy source is published-only, anonymous grants are read-only, public runtime uses browser-key Supabase reads with static fallback, and Cloudflare routes only invoke Pages Functions under `/api/*`.
- Capability Statement source verification now has `npm run agent:capabilities-ui`; it guards the Founder-sourced `/capabilities` page, concrete capability modules, selected-project proof ledger, shared CTA data, PDF asset path, email-gated `/api/enquiries` submit flow, Turnstile reuse, and required static assets.
- Email notification follow-up is complete for the current provider path: Jay has an SMTP2GO subscription, SMTP2GO DNS records are present in Cloudflare, the form Functions prefer SMTP2GO while keeping Resend compatibility, production is redeployed at commit `3408f34`, and approved tagged live proof stored `notification_status = sent` for Contact and Sample Request rows.

## Current Branch State
- Branch: `main`
- Remote tracking: `origin/main`
- Current working tree is the production launch workspace; continue to avoid DNS, secret, or live-write changes unless Jay explicitly approves that exact operation.

## Canonical Entry Points
- Agent entry: `AGENTS.md`
- Current state: `docs/HANDOFF.md`
- Machine task queue: `docs/agent/tasks.json`
- Verification guide: `docs/agent/verification.md`
- Brand authority: `docs/brand-baseline.md`
- Design authority: `docs/DESIGN.md`
- Architecture contract: `docs/ARCHITECTURE.md`
- Admin editor handoff guide: `docs/ADMIN_EDITOR_GUIDE.md`
- Historical evidence: `docs/WORKLOG.md`

## Latest Verification Snapshot
- Latest Projects archive/detail checkpoint on 2026-06-02: `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:check`, `npm run agent:admin-crud-coverage`, `npm run agent:content-import -- --out .tmp/content-import-preview.json`, and `npm run agent:content-import:apply-sql` pass. In-app Browser QA on `http://127.0.0.1:4174` covered `/projects` desktop/mobile filters and grid/list controls, Moon Gate desktop/mobile hotspot click/tap inspector behavior, West Side Place desktop/mobile normal media detail flow, `/admin/projects` no-config gate, no framework overlays, and `0` horizontal overflow. Browser console output only showed the pre-existing Cloudflare Turnstile `?ver=` warning. Current static project data has no client-approved YouTube block, so the optional YouTube module is source/admin/migration verified but not shown as live public content.
- Latest live-readiness manual-input validation checkpoint on 2026-05-29: `scripts/check-live-readiness.mjs` now validates non-secret `--base-url` and `--admin-email` values before counting them as present. Copied placeholders like `<preview-origin>` / `<first-admin-email>`, malformed emails, and preview URLs with path/query/hash remain missing in report-only and JSON output. `node --check scripts/check-live-readiness.mjs`, placeholder-mode `npm run agent:live-readiness -- --base-url '<preview-origin>' --admin-email '<first-admin-email>' --form-writes-approved --first-admin-writes-approved --admin-writes-approved --content-import-approved --content-merge-approved --content-public-cutover-approved --turnstile-token-provided`, valid-override JSON `npm run agent:live-readiness -- --json --base-url https://example.pages.dev --admin-email first@example.com --form-writes-approved --first-admin-writes-approved --admin-writes-approved --content-import-approved --content-merge-approved --content-public-cutover-approved --turnstile-token-provided`, `npm run agent:live-readiness`, `npm run agent:admin-config-gate`, `npm run agent:admin-crud-coverage`, `npm run agent:cloudflare-readiness`, `npm run agent:public-supabase-readiness`, `npm run agent:forms-ui`, `node scripts/check-forms-api.mjs`, plan-only `npm run agent:admin-auth-browser`, plan-only `npm run agent:admin-crud-live`, plan-only `npm run agent:first-admin-bootstrap`, `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass. Supabase connector read-only sanity still shows project `Urblo` active healthy on Postgres 17.6.1, 12 launch migrations, 24/24 expected launch tables with RLS, 114 public-schema policies, 12 finish rows, one default site settings row, zero admin/form/content parent rows, and zero security advisor lints. No Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes were created.
- Latest production dependency audit checkpoint on 2026-05-29: upgraded `react-router-dom` to `^7.16.0`, `swiper` to `^12.2.0`, and `postcss` to `^8.5.15`, then added `tailwindcss/nesting` before Tailwind so Swiper 12 nested CSS builds without PostCSS nesting warnings. `npm audit fix` refreshed the lockfile and `npm audit --omit=dev --audit-level=critical` now reports zero vulnerabilities. Runtime verification after the dependency update: `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:admin-config-gate`, `npm run agent:admin-crud-coverage`, `npm run agent:check`, and a Playwright Chromium homepage carousel render check pass. Build still keeps the existing Browserslist data staleness notice only.
- Latest live-readiness documentation guard checkpoint on 2026-05-29: `docs/ARCHITECTURE.md`, `docs/CLOUDFLARE_DEPLOYMENT.md`, and `docs/agent/verification.md` now document the full current `npm run agent:live-readiness` non-secret flag set: `--base-url`, `--admin-email`, `--form-writes-approved`, `--first-admin-writes-approved`, `--admin-writes-approved`, `--content-import-approved`, `--content-merge-approved`, `--content-public-cutover-approved`, and `--turnstile-token-provided`. `scripts/check-harness.mjs` now fails `npm run agent:check` if those docs drift from the runner's approval/readiness flag contract. This checkpoint is docs/tooling-only and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes.
- Latest Cloudflare/live-readiness env contract checkpoint on 2026-05-29: `npm run agent:cloudflare-readiness` now guards the complete `.env.example` and `docs/CLOUDFLARE_DEPLOYMENT.md` placeholder/runbook contract for canonical vars, compatibility aliases, preview URL helpers, first-admin bootstrap email, active-admin credentials, token login, and unprofiled QA credentials. `npm run agent:cloudflare-readiness` passes after the expansion. This remains repo-side only; Cloudflare Pages project creation, environment configuration, preview URL, custom domain, DNS, and rollback remain account-level external work.
- Latest admin live login credential-boundary checkpoint on 2026-05-29: `scripts/check-admin-crud-live.mjs` now reads live password login only from `URBLO_ADMIN_EMAIL`/`URBLO_ADMIN_PASSWORD`, or from explicit `URBLO_ADMIN_ACCESS_TOKEN`; `URBLO_FIRST_ADMIN_EMAIL` remains only for first-admin bootstrap/readiness. `scripts/check-admin-crud-coverage.mjs` guards that boundary so setup identity cannot silently become a live-login fallback again. Verification at that checkpoint showed 24 expected launch tables, no missing RLS, 114 public-schema policies, 12 applied launch migrations in the expected set, 12 finish rows, one published default settings row, and no private/admin/form/import rows before the later 2026-06-02 form QA writes. `node --check scripts/check-admin-crud-live.mjs`, `node --check scripts/check-admin-crud-coverage.mjs`, `jq empty docs/agent/tasks.json`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:check`, `git diff --check`, `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` passed. This checkpoint was source-only and created no Supabase Auth users, profiles, rows, Storage objects, Cloudflare state, credentials, or live writes.
- Latest Supabase foundation source-readiness checkpoint on 2026-05-29: added `npm run agent:supabase-foundation-readiness` and wired it into `npm run agent:check`. The gate passes and verifies the local migration/source contract for the 12 expected migrations, 24 launch tables including `project_media`, RLS enablement, public-select policies, private-table anonymous revokes, read-only anonymous public grants, 12 finish seed rows, the default published site settings row, service-role-only `submit_sample_request_with_item(jsonb, jsonb)`, Storage buckets/listing hardening, private helper grants, and normalized admin profile email uniqueness. A read-only Supabase connector query at that time also confirmed no private admin/form or imported content rows; the form-row portion is now superseded by the 2026-06-02 tagged form QA rows. This does not replace live admin auth, live CRUD, or browser verification.
- Fresh read-only Supabase connector sanity on 2026-05-29: migration list included all 12 applied launch migrations through `sample_request_atomic_insert`; the security advisor returned zero lints; SQL sanity confirmed 24/24 expected public launch tables present, 24/24 with RLS enabled, no missing expected tables, 12 finish definition rows with 12 distinct finish keys, one published default `site_settings` row, no active admin profiles, no imported media/Stone Library/Project/Product/Article rows, and no form lead rows before the later tagged live QA run. This was read-only and did not mutate Supabase; the form lead row fact is superseded by the 2026-06-02 tagged live form QA rows.
- Fresh Cloudflare Pages account action on 2026-06-02: created the `urblo` Pages project in Hunter's Cloudflare account (`077afae2c6f4e77badadf21e49e58eb7`) with ID `3c4c5af3-a2a8-4058-bc0e-0ee6e8cfcaca`, default domain `urblo.pages.dev`, production branch `main`, build command `npm run build`, output directory `dist`, and root dir `/`. Jay then reauthorized the Cloudflare Pages GitHub integration; source is now connected to `jayyy-3/jayyy-3.github.io`, and deployment `542c25f4-2e55-437a-abbe-58d427aff48c` for commit `9a1e9c6` completed successfully. `npm run agent:cloudflare-preview-smoke -- --base-url https://urblo.pages.dev` passed across public/admin route shells, assets, redirects, admin bundle markers, browser service-role boundary, and no-write API safe-failure behavior. At that checkpoint, apex, `www`, and `qa` DNS still pointed to the old WordPress target and no DNS/custom-domain changes had been made; this was later superseded by the production custom-domain cutover recorded in the current focus and Worklog.
- Latest admin browser sign-out checkpoint on 2026-05-29: `scripts/check-admin-auth-browser.mjs` active-admin live mode now clicks Sign out after authenticated route-shell checks and requires the last protected route to return to `/admin/login?next=...` without private admin audit content. `scripts/check-admin-crud-coverage.mjs` guards that sign-out proof remains in the verifier. Live sign-out QA remains unrun until browser-safe config and active admin credentials are available.
- Latest unprofiled route-probe checkpoint on 2026-05-29: `scripts/check-admin-auth-browser.mjs` unauthorized-profile live mode now continues after the first `/admin/unauthorized` landing and probes the full authenticated admin route list (`/admin`, `/admin/leads`, `/admin/media`, `/admin/settings`, `/admin/stone-library`, `/admin/projects`, `/admin/products`, `/admin/articles`, and `/admin/audit`) while still signed in, requiring each launch-critical route to stay on `/admin/unauthorized` without private module headings. `scripts/check-admin-crud-coverage.mjs` guards that unauthorized probes are derived from the complete authenticated route list. Live unprofiled route-probe QA remains unrun until browser-safe config and an unprofiled Auth test account are available.
- Latest unprofiled admin browser QA checkpoint on 2026-05-29: `scripts/check-admin-auth-browser.mjs` supports `--expect-unauthorized`, requiring `URBLO_UNPROFILED_EMAIL` and `URBLO_UNPROFILED_PASSWORD` for a valid Supabase Auth user with no active `admin_profiles` row. Live mode must land on `/admin/unauthorized`, reject private admin module headings, and keep all launch-critical admin route probes unauthorized without creating content rows, Storage objects, audit events, users, or profiles. `scripts/check-live-readiness.mjs` reports this no-write unauthorized-profile QA gate separately, `.env.example` documents the local-only variables, and `scripts/check-admin-crud-coverage.mjs` guards the verifier source. `node --check scripts/check-admin-auth-browser.mjs`, `node --check scripts/check-live-readiness.mjs`, `node --check scripts/check-admin-crud-coverage.mjs`, plan-only `npm run agent:admin-auth-browser -- --expect-unauthorized`, `npm run agent:live-readiness`, and `npm run agent:admin-crud-coverage` pass. Live unauthorized-profile browser QA remains unrun until browser-safe config and an unprofiled Auth test account are available.
- Latest first-admin email matching coverage guard on 2026-05-29: `scripts/check-admin-crud-coverage.mjs` now fails if first-admin bootstrap or admin-live readiness source regresses to exact case-sensitive profile-email lookup instead of normalized profile-email matching. This keeps the source gate aligned with the live `admin_profiles_email_ci_unique_idx` database contract. `node --check scripts/check-admin-crud-coverage.mjs`, `jq empty docs/agent/tasks.json`, `npm run agent:admin-crud-coverage`, `npm run lint`, `npm run agent:check`, `git diff --check`, `npm run build`, `npx tsc -b`, and `npm run agent:smoke` pass. This checkpoint is source-only/no-write and creates no Supabase Auth users, profiles, rows, Storage objects, Cloudflare state, credentials, or live writes.
- Latest first-admin email-case readiness checkpoint on 2026-05-29: `scripts/bootstrap-first-admin.mjs` and `scripts/check-admin-live-readiness.mjs` now normalize `admin_profiles.email` before matching the supplied first-admin email, aligning readiness checks with the live `admin_profiles_email_ci_unique_idx` case-insensitive uniqueness contract. `node --check scripts/bootstrap-first-admin.mjs`, `node --check scripts/check-admin-live-readiness.mjs`, plan-only `npm run agent:first-admin-bootstrap`, plan-only `npm run agent:admin-auth-browser`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:live-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:check`, and `git diff --check` pass. Supabase connector read-only sanity still shows 24/24 expected launch tables present, 0 missing RLS, 12 published finish definitions, one published default site settings row, zero active admin profiles, and empty selected content/lead target tables. This checkpoint is source-only/no-write and creates no Supabase Auth users, profiles, rows, Storage objects, Cloudflare state, credentials, or live writes.
- Latest local secret-handling runbook update on 2026-05-29: `docs/CLOUDFLARE_DEPLOYMENT.md` now states that live verification secrets belong in ignored local env files such as `.env.local` or `.dev.vars`, not chat or committed docs, and groups the variable sets needed for form persistence, browser-key privacy checks, admin readiness, admin browser QA, admin CRUD live writes, email proof, and Turnstile proof. `npm run agent:check` and `git diff --check` pass.
- Latest content import/public cutover recheck on 2026-05-29: `npm run agent:content-import:apply-sql` passed with 0 warnings and 0 blockers, regenerating ignored `.tmp` review artifacts only. Current dry-run candidates are 115 media assets, 13 stone groups, 15 variants, 153 finish capabilities, 53 finish image rows, 6 products, 28 product models, 18 material defaults, 18 specs, 5 projects, 41 project facts, 14 project media rows, 2 project material rows, 1 material map, 2 hotspots, 4 articles, and 95 article blocks. `npm run agent:public-supabase-readiness` passed, confirming import rows stay draft, article blocks remain structured, guarded import/rollback SQL approval gates remain manual, public RLS source remains published-only, anonymous grants remain read-only, public runtime remains static/file-backed, and Cloudflare Functions remain scoped to `/api/*`.
- Latest read-only Supabase state re-audit on 2026-05-29: connector SQL against project `npkidywzwddbnfrnxlmo` verified 24/24 expected launch tables present, no expected launch table missing RLS, 12 published finish definitions, one published default `site_settings` row, zero rows in current content/lead import-target tables checked, `urblo-public-media` public and `urblo-admin-media` private Storage buckets present, zero anonymous public-table write grants, zero anonymous private admin/lead table grants, `admin_profiles_email_ci_unique_idx` present, `submit_sample_request_with_item(jsonb, jsonb)` executable by `service_role` only, and public admin helper RPC execution denied to browser roles. That audit's zero-`admin_profiles` state is superseded by the 2026-06-03 first-admin bootstrap for `info@urblo.com.au`.
- Latest admin auth browser QA runner checkpoint on 2026-05-29: added `scripts/check-admin-auth-browser.mjs` and `npm run agent:admin-auth-browser`. Default mode is plan-only and performs no Supabase login. Live mode requires `--allow-login --strict`, browser-safe Supabase config, and `URBLO_ADMIN_EMAIL`/`URBLO_ADMIN_PASSWORD`; it starts built Vite preview by default, verifies unauthenticated protected routes redirect to `/admin/login?next=...`, signs in through the UI, checks authenticated `/admin`, `/admin/leads`, `/admin/media`, `/admin/settings`, `/admin/stone-library`, `/admin/projects`, `/admin/products`, `/admin/articles`, and `/admin/audit` route shells, rejects config/unauthorized/login states after authentication, and writes ignored screenshots under `.tmp/admin-auth-browser/screenshots`. `node --check scripts/check-admin-auth-browser.mjs`, plan-only `npm run agent:admin-auth-browser`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:admin-config-gate`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, `npm run agent:live-readiness`, `npm run agent:check`, and `git diff --check` pass. Live login mode was not run because browser-safe key/admin credentials are not configured in this workspace.
- Latest admin auth browser env-loading checkpoint on 2026-05-29: `scripts/check-admin-auth-browser.mjs` now loads `.env.local`, `.env`, `.dev.vars`, and shell values like the other live-readiness tooling, prints only variable names/sources, supports extra `--env-file`, and treats `VITE_SUPABASE_URL` as optional because the browser client defaults to the Urblo project URL. `node --check scripts/check-admin-auth-browser.mjs`, plan-only `npm run agent:admin-auth-browser`, `npm run lint`, `npm run agent:check`, and `git diff --check` pass. Live login mode remains unrun because browser-safe key/admin credentials are not configured in this workspace.
- Latest repeatable admin config-gate browser check on 2026-05-29: added `scripts/check-admin-config-gate.mjs`, `npm run agent:admin-config-gate`, and a local `playwright` dev dependency. The runner starts built Vite preview when no `--base-url` is supplied, generates a temporary Playwright Firefox spec, verifies `/admin`, `/admin/login`, `/admin/unauthorized`, `/admin/leads`, `/admin/media`, `/admin/settings`, `/admin/stone-library`, `/admin/projects`, `/admin/products`, `/admin/articles`, and `/admin/audit` render `Configuration required`, rejects private admin/module text, and writes ignored screenshots under `.tmp/admin-config-gate/screenshots`. `node --check scripts/check-admin-config-gate.mjs`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:admin-config-gate`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, `npm run agent:live-readiness`, `npm run agent:check`, and `git diff --check` pass. Extra `npm audit --omit=dev --audit-level=critical` still reports existing production dependency advisories, including a critical Swiper advisory that needs a separate breaking-upgrade review.
- Latest admin no-config route-gate full coverage on 2026-05-29: built-site Playwright Firefox screenshot checks against local Vite preview verified `/admin`, `/admin/login`, `/admin/unauthorized`, `/admin/leads`, `/admin/media`, `/admin/settings`, `/admin/stone-library`, `/admin/projects`, `/admin/products`, `/admin/articles`, and `/admin/audit` all render the `Configuration required` gate without browser-safe Supabase env. Screenshots were captured under `/tmp/urblo-admin-config-required-*.png`. `npm run build`, `jq empty docs/agent/tasks.json`, `npm run agent:check`, and `git diff --check` pass; build keeps the existing Browserslist staleness notice. This checkpoint used no Supabase credentials, no live writes, no Auth/Profile changes, no Storage objects, and no Cloudflare changes.
- Latest Admin destructive-removal source guard on 2026-05-29: `scripts/check-admin-crud-coverage.mjs` now scans `src/pages/admin` and `scripts/check-admin-crud-live.mjs` for Supabase `.delete()` mutations, HTTP `DELETE` requests, destructive RPC names, and visible `Delete`/`Remove` controls. `jq empty docs/agent/tasks.json`, `node --check scripts/check-admin-crud-coverage.mjs`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:live-readiness`, `npm run agent:check`, and `git diff --check` pass. Build keeps the existing Browserslist staleness notice. `npm run agent:live-readiness` still reports the live blockers: missing service-role key, browser-safe key, first-admin email, Cloudflare preview URL, admin session credentials, and Jay approval for tagged live writes. This checkpoint is source-only and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes.
- Latest Admin CRUD state-coverage guard on 2026-05-29: `scripts/check-admin-crud-coverage.mjs` now fails if mutating admin screens lose validation feedback paths or save paths, and fails if content/media lifecycle screens lose publish/archive save paths or published/archived state controls. `node --check scripts/check-admin-crud-coverage.mjs`, `npm run agent:admin-crud-coverage`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:check`, and `git diff --check` pass. Build keeps the existing Browserslist staleness notice. This checkpoint is source-only and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes.
- Latest Cloudflare preview bundle/API safe-failure guard on 2026-05-29: `scripts/check-cloudflare-preview-smoke.mjs` now recursively follows deployed JS/CSS route chunks, verifies deployed admin bundle markers for the configuration-required state and `admin_profiles` profile gate, rejects browser service-role env access patterns, and checks `/api/enquiries` plus `/api/sample-requests` malformed JSON responses return `400 invalid_json`. `scripts/check-forms-api.mjs` now covers OPTIONS CORS headers and malformed JSON no-call behavior in the mock Pages Function wrapper checks. `node --check scripts/check-cloudflare-preview-smoke.mjs`, `node --check scripts/check-forms-api.mjs`, `node scripts/check-forms-api.mjs`, `npm run agent:cloudflare-readiness`, local `npm run agent:cloudflare-preview-smoke -- --base-url http://127.0.0.1:4184`, `npm run agent:live-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass. Build keeps the existing Browserslist staleness notice. This checkpoint is source-only/local-preview verification and does not create Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes.
- Latest Data API grant preflight checkpoint on 2026-05-29: `npm run agent:content-import:preflight-sql` now generates ignored preflight SQL with table privilege checks for `anon`, `authenticated`, and `service_role`, plus sequence usage checks for `authenticated` and `service_role`. `npm run agent:public-supabase-readiness` now fails if that grant inspection disappears. Read-only Supabase connector verification against project `npkidywzwddbnfrnxlmo` confirmed 19/19 public content tables have anon `select`, 5/5 private/admin/lead tables deny anon `select`, 24/24 tables deny anon writes, 24/24 tables grant authenticated CRUD, 24/24 tables grant service-role CRUD, and 23/23 generated sequences grant authenticated/service-role usage. This checkpoint is source/read-only and did not mutate Supabase, Auth, Storage, Cloudflare, or credentials.
- Latest content import Supabase preflight checkpoint on 2026-05-29: `npm run agent:content-import:preflight-sql` generated ignored `.tmp` review artifacts with 115 media candidates, 13 stone groups, 15 variants, 153 finish capability rows, 53 finish image rows, 6 products, 28 models, 5 projects, 4 articles, 95 article blocks, 0 warnings, and 0 blockers. A read-only Supabase connector query against project `npkidywzwddbnfrnxlmo` confirmed 12 `finish_definitions`, 1 `site_settings` row, 0 current rows in every content import target table, 0 rows in parent target tables used by merge/conflict gates (`media_assets`, `stone_groups`, `products`, `projects`, `articles`), no missing RLS across checked seed/import tables, and one public-select policy per checked public content table. Supabase security advisor returned 0 security lints. This checkpoint is source/read-only and did not apply SQL, create rows, merge content, switch public reads, or touch credentials.
- Latest final Turnstile verifier public-site-key guard on 2026-05-29: `scripts/check-forms-api-live.mjs` now makes `VITE_TURNSTILE_SITE_KEY` a hard preflight requirement whenever `--require-turnstile` is used, so final form proof cannot verify only the server-side token path while the public Contact widget is unconfigured. `node --check scripts/check-forms-api-live.mjs`, fail-closed `SUPABASE_SERVICE_ROLE_KEY=dummy TURNSTILE_SECRET_KEY=dummy node scripts/check-forms-api-live.mjs --allow-writes --require-turnstile --turnstile-token dummy`, `npm run agent:live-readiness`, `npm run agent:check`, `git diff --check`, `npm run lint`, and `npx tsc -b` pass. This checkpoint is source-only/no-write and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, live email, or live Turnstile calls.
- Latest content cutover readiness-gate checkpoint on 2026-05-29: `npm run agent:live-readiness` now reports the static-to-Supabase draft import artifact command as ready, plus manual approval gates for applying guarded draft import SQL, merge/upsert behavior when parent natural keys already exist, and public read cutover. New report-only flags are `--content-import-approved`, `--content-merge-approved`, and `--content-public-cutover-approved`; they do not apply SQL, publish content, switch runtime reads, or mutate Supabase. `node --check scripts/check-live-readiness.mjs`, `jq empty docs/agent/tasks.json`, `npm run agent:live-readiness`, `npm run agent:live-readiness -- --json --content-import-approved --content-merge-approved --content-public-cutover-approved`, `npm run agent:public-supabase-readiness`, `npm run agent:check`, `npm run lint`, and `git diff --check` pass. This checkpoint is source-only/no-write and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes.
- Latest Admin live dashboard-health verifier checkpoint on 2026-05-29: `scripts/check-admin-crud-live.mjs` now stages readback of the dashboard health predicates against tagged QA rows before archiving them in a future `--allow-writes` run. The planned proof covers published media missing alt/usage notes, published project and project-fact claim review, published products/articles missing key media, Stone Library TBC rows, and stale new leads older than 48 hours. `scripts/check-admin-crud-coverage.mjs` now guards those live-verifier hooks plus the existing dashboard health-table references and health-copy markers. `node --check scripts/check-admin-crud-live.mjs`, `node --check scripts/check-admin-crud-coverage.mjs`, plan-only `npm run agent:admin-crud-live`, `npm run agent:admin-crud-coverage`, `npm run agent:check`, `git diff --check`, `npm run lint`, `npx tsc -b`, `npm run build`, `npm run agent:smoke`, `npm run agent:forms-ui`, `node scripts/check-forms-api.mjs`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:content-import:apply-sql`, and `npm run agent:public-supabase-readiness` pass. Build keeps the existing Browserslist staleness notice. This checkpoint is source-only/no-write and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes.
- Previous Admin dashboard health queue checkpoint on 2026-05-29: `src/pages/admin/AdminDashboardPage.tsx` builds a `Content health queue` from Supabase counts for published media missing alt/usage notes, published project and project-fact claim review, published products/articles missing key media, Stone Library TBC rows, and stale new leads older than 48 hours. `npm run lint`, `npx tsc -b`, `npm run build`, `npm run agent:smoke`, `npm run agent:forms-ui`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:public-supabase-readiness`, `npm run agent:check`, `jq empty docs/agent/tasks.json`, and `git diff --check` passed. Build kept the existing Browserslist staleness notice.
- Latest content import merge-approval guard on 2026-05-29: `scripts/check-content-import-readiness.mjs` now adds a parent natural-key conflict report to `.tmp/content-import-preflight.sql` and a second fail-closed apply gate to `.tmp/content-import-apply.sql`. The guarded apply artifact still aborts unless `urblo.import_approved=true`; if matching parent natural keys already exist in `media_assets`, `stone_groups`, `products`, `projects`, or `articles`, it now also aborts unless `urblo.import_merge_approved=true` is explicitly set after merge/upsert review. `scripts/check-public-supabase-readiness.mjs` now verifies this merge gate stays commented by default and checks all guarded parent tables. `node --check scripts/check-content-import-readiness.mjs`, `node --check scripts/check-public-supabase-readiness.mjs`, `npm run agent:content-import:apply-sql`, `npm run agent:public-supabase-readiness`, `npm run agent:check`, `git diff --check`, `npm run lint`, `npx tsc -b`, `npm run build`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:cloudflare-readiness`, `npm run agent:forms-ui`, and `npm run agent:live-readiness` pass. Supabase connector read-only evidence still shows 12 applied migrations, 24/24 launch tables present with RLS, 12 published finish definitions, one published default site settings row, zero private workflow/admin rows, and zero security advisor lints. This is source-only/no-write and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes.
- Latest content import rollback SQL guard on 2026-05-29: `scripts/check-content-import-readiness.mjs` now supports `--rollback-sql-out` and `npm run agent:content-import:apply-sql` writes `.tmp/content-import-rollback.sql` alongside the preview, plan, preflight, and apply artifacts. The rollback artifact aborts unless `urblo.rollback_approved=true` is explicitly set, runs in reverse dependency order from `article_blocks` back to `media_assets`, and targets matching draft/import rows only. `scripts/check-public-supabase-readiness.mjs` now verifies the rollback artifact keeps its destructive gate commented by default, avoids drop/truncate/RLS-disabling/publish statements, preserves reverse dependency order, and keeps plan-count summaries aligned with the dry run. `node --check scripts/check-content-import-readiness.mjs`, `node --check scripts/check-public-supabase-readiness.mjs`, `node --check scripts/check-harness.mjs`, `npm run agent:content-import:apply-sql`, and `npm run agent:public-supabase-readiness` pass. This is source-only/no-write and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes.
- Latest Contact Turnstile widget source checkpoint on 2026-05-29: `src/pages/ContactPage.tsx` now renders an explicit Cloudflare Turnstile widget only when `VITE_TURNSTILE_SITE_KEY` is configured, blocks configured-widget submissions until a token exists, sends `turnstileToken` to the existing Pages Function payload, and resets the widget after success/failure. `.env.example`, `src/vite-env.d.ts`, `scripts/check-contact-form-ui-source.mjs`, `scripts/check-cloudflare-pages-readiness.mjs`, and `scripts/check-live-readiness.mjs` now guard/report the public site-key contract. `node --check scripts/check-contact-form-ui-source.mjs`, `node --check scripts/check-cloudflare-pages-readiness.mjs`, `node --check scripts/check-live-readiness.mjs`, `jq empty docs/agent/tasks.json`, `npm run agent:forms-ui`, `npm run agent:cloudflare-readiness`, `npm run agent:live-readiness`, `npm run agent:live-readiness -- --json --form-writes-approved --turnstile-token-provided`, `node scripts/check-forms-api.mjs`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, Playwright CLI Firefox snapshot on `/contact` with no site key configured, `npm run agent:check`, and `git diff --check` pass. This is source-only/no-write and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, live email, or live Turnstile calls.
- Latest live form email/Turnstile proof guard on 2026-05-29: `scripts/check-forms-api-live.mjs` now supports `--require-email` and `--require-turnstile`. `--require-email` forces final live form proof to send real notifications and verify stored enquiry/sample request `notification_status = 'sent'`; `--require-turnstile` requires a supplied token and verifies stored `turnstile_success = true`. `scripts/check-live-readiness.mjs` now reports those final proof inputs separately, and `scripts/check-cloudflare-pages-readiness.mjs` guards the Cloudflare runbook so the commands cannot disappear. `node --check scripts/check-forms-api-live.mjs`, `node --check scripts/check-live-readiness.mjs`, `node --check scripts/check-cloudflare-pages-readiness.mjs`, `jq empty docs/agent/tasks.json`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:check`, `git diff --check`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:forms-ui`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:content-import:apply-sql`, and `npm run agent:public-supabase-readiness` pass. Expected fail-closed local checks prove the new flags stop before writes when `--allow-email`, email config, or Turnstile token are missing. This is source-only/no-write and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, live email, or live Turnstile calls.
- Latest admin Storage readback/privacy verifier checkpoint on 2026-05-29: `scripts/check-admin-crud-live.mjs` now verifies that the optional `--include-storage` tiny private `urblo-admin-media` upload can be read back by the signed-in admin and is not anonymously readable through either private or public Storage object endpoints. `scripts/check-admin-crud-coverage.mjs` guards this live verifier contract so the Storage proof cannot regress to upload-only, and `scripts/check-live-readiness.mjs` now describes the Storage proof as upload, signed-in readback, plus anonymous-read denial. `node --check scripts/check-admin-crud-live.mjs`, `node --check scripts/check-admin-crud-coverage.mjs`, `node --check scripts/check-live-readiness.mjs`, `jq empty docs/agent/tasks.json`, plan-only `npm run agent:admin-crud-live -- --include-storage`, `npm run agent:admin-crud-coverage`, `npm run agent:live-readiness`, `npm run agent:check`, `git diff --check`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:cloudflare-readiness`, `npm run agent:public-supabase-readiness`, and `npm run agent:forms-ui` pass. This is source-only/no-write and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes.
- Latest harness command-map guard on 2026-05-29: `scripts/check-harness.mjs` now guards the active operational `agent:*` package script map for form, admin, Cloudflare, first-admin, live-readiness, content-import, and public Supabase readiness runners. `npm run agent:live-readiness` still reports all live form/admin/Cloudflare inputs missing or approval-gated in the current local environment. Supabase connector read-only verification now shows 12 applied migrations through `sample_request_atomic_insert`, 24/24 expected public launch tables with RLS enabled, 12 published `finish_definitions`, one published default `site_settings` row, zero rows in private workflow/admin tables, and zero security advisor lints. Performance advisor output is limited to expected early-stage unused-index INFO items and multiple-permissive-policy WARN items on new/low-traffic tables; do not remove launch-pattern indexes before real import/live admin usage evidence exists. This checkpoint is no-secret/no-write and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes.
- Latest deployed form persistence checkpoint on 2026-06-02: after `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` were configured in Cloudflare Pages production and redeployed, approved tagged QA writes against `https://urblo.pages.dev` returned `201` for one valid enquiry and one valid sample request, and `400 validation_failed` for invalid enquiry/sample payloads. Supabase connector readback confirmed `enquiries.id = 1`, `sample_requests.id = 1`, `sample_request_items.id = 1`, and `admin_audit_events.id = 1/2` with matching source-route metadata; invalid tagged emails created zero lead rows and zero audit rows. `notification_status` is `not_required` because no email provider is configured, and `turnstile_success` is `null` because Turnstile is not configured.
- Latest Sample Request atomic insert checkpoint on 2026-05-29: Supabase migration `sample_request_atomic_insert` is applied on project `npkidywzwddbnfrnxlmo`. The `submit_sample_request_with_item(jsonb, jsonb)` RPC is `security invoker`, scoped to `search_path=public, pg_temp`, executable by `service_role`, and not executable by `anon` or `authenticated`. `/api/sample-requests` now calls the RPC so `sample_requests` and the first `sample_request_items` row are created in one database transaction. Source checks pass for `functions/_lib/forms.js` and `scripts/check-forms-api.mjs`; Supabase verification confirms the migration and function privileges.
- Latest admin profile email uniqueness checkpoint on 2026-05-29: Supabase migration `admin_profile_email_uniqueness` is applied on project `npkidywzwddbnfrnxlmo`, with `admin_profiles_email_ci_unique_idx` enforcing unique `lower(btrim(email))` values and zero duplicate email groups before application. `scripts/bootstrap-first-admin.mjs` now refuses first-admin write mode if the target email is already linked to a different Supabase Auth user before upsert, and `scripts/check-admin-crud-coverage.mjs` guards the migration/source contract. Supabase connector verification confirmed the migration list entry, index definition, and zero duplicate profile email groups. `node --check scripts/bootstrap-first-admin.mjs`, `node --check scripts/check-admin-crud-coverage.mjs`, `npm run agent:first-admin-bootstrap`, expected fail-closed `npm run agent:first-admin-bootstrap -- --verify-only --admin-email first@example.com`, expected fail-closed `npm run agent:admin-live-readiness -- --admin-email first@example.com`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:public-supabase-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:forms-ui`, `npm run agent:check`, and `git diff --check` pass. No first-admin creation/invite, admin session writes, form writes, Storage uploads, Cloudflare state, or secrets were used.
- Latest admin profile form validation checkpoint on 2026-05-29: `/admin/settings` catches duplicate login account IDs and duplicate normalized admin profile emails before save, matching the live database uniqueness rules. `scripts/check-admin-crud-coverage.mjs` guards both validation messages. `node --check scripts/check-admin-crud-coverage.mjs`, `npm run agent:admin-crud-coverage`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, plan-only `npm run agent:admin-crud-live`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:public-supabase-readiness`, `npm run agent:forms-ui`, `npm run agent:check`, and `git diff --check` pass. Live profile saves were not run because browser-safe Supabase config and active owner/admin access remain missing.
- Latest admin Auth/profile link readiness guard checkpoint on 2026-05-29: `scripts/bootstrap-first-admin.mjs` and `scripts/check-admin-live-readiness.mjs` now fail read-only verification if the active admin profile is not linked to the matching Supabase Auth user id for the supplied first-admin email. `scripts/check-admin-crud-coverage.mjs` guards the source contract. `node --check scripts/bootstrap-first-admin.mjs`, `node --check scripts/check-admin-live-readiness.mjs`, `node --check scripts/check-admin-crud-coverage.mjs`, `npm run agent:first-admin-bootstrap`, expected fail-closed `npm run agent:first-admin-bootstrap -- --verify-only --admin-email first@example.com`, expected fail-closed `npm run agent:admin-live-readiness -- --admin-email first@example.com`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:public-supabase-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:forms-ui`, `npm run agent:check`, and `git diff --check` pass. This is source-only and creates no Supabase Auth users, profiles, rows, Storage objects, Cloudflare state, credentials, or live writes.
- Latest first-admin verify-only role guard checkpoint on 2026-05-29: `scripts/bootstrap-first-admin.mjs` now fails read-only `--verify-only` if the existing active `admin_profiles` row has the wrong role for the planned bootstrap role (`owner` by default, or explicit `--role admin`). `scripts/check-admin-crud-coverage.mjs` now guards that source contract. `node --check scripts/bootstrap-first-admin.mjs`, `node --check scripts/check-admin-crud-coverage.mjs`, `npm run agent:first-admin-bootstrap`, expected fail-closed `npm run agent:first-admin-bootstrap -- --verify-only --admin-email first@example.com`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:public-supabase-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:forms-ui`, `npm run agent:check`, and `git diff --check` pass. This is source-only and creates no Supabase Auth users, profiles, rows, Storage objects, Cloudflare state, credentials, or live writes.
- Latest public Supabase guarded apply-SQL readiness checkpoint on 2026-05-29: `scripts/check-public-supabase-readiness.mjs` now generates the guarded draft content import SQL in a temporary ignored `.tmp/` folder and verifies the approval gate remains commented by default, runtime approval is required, destructive statements are absent, publish-status changes are absent, imported status is forced to `draft`, and the generated verification summary matches the dry-run plan counts. `node --check scripts/check-public-supabase-readiness.mjs`, `npm run agent:content-import:apply-sql`, `npm run agent:public-supabase-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:cloudflare-readiness`, `npm run agent:live-readiness`, `npm run agent:check`, and `git diff --check` pass. This is source-only and creates no Supabase rows, Storage objects, Auth users, Cloudflare state, credentials, or live writes.
- Latest forms live audit metadata verifier checkpoint on 2026-05-29: `scripts/check-forms-api-live.mjs` now verifies valid enquiry/sample-request audit rows include the submitted source route metadata and invalid enquiry/sample-request payloads create neither rows nor matching audit events. `scripts/check-forms-api.mjs` now guards source audit payload metadata for enquiry and sample request wrapper checks. `node --check scripts/check-forms-api-live.mjs`, `node --check scripts/check-forms-api.mjs`, `node scripts/check-forms-api.mjs`, `npm run agent:forms-ui`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:check`, `npm run agent:live-readiness`, `npm run agent:public-supabase-readiness`, `npm run agent:cloudflare-readiness`, and `git diff --check` pass. This is source/mock coverage and creates no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes.
- Latest admin live audit action verifier checkpoint on 2026-05-29: `scripts/check-admin-crud-live.mjs` now verifies the exact expected `admin_audit_events` action counts, entity types, entity ids, verification marker metadata, and source metadata for tagged live admin QA writes instead of accepting a loose minimum row count. `scripts/check-admin-crud-coverage.mjs` now guards that action coverage contract. `node --check scripts/check-admin-crud-live.mjs`, `node --check scripts/check-admin-crud-coverage.mjs`, plan-only `npm run agent:admin-crud-live`, `npm run agent:admin-crud-coverage`, `npm run agent:live-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:check`, `npm run agent:public-supabase-readiness`, `npm run agent:cloudflare-readiness`, and `git diff --check` pass. This is source-only and creates no Supabase rows, Storage objects, Auth users, Cloudflare state, credentials, or live writes.
- Latest Supabase read-only sanity on 2026-05-29: Supabase connector verification against project `npkidywzwddbnfrnxlmo` shows 12 applied migrations through `sample_request_atomic_insert`, 24/24 expected public launch tables with RLS enabled, 12 published `finish_definitions`, one published default `site_settings` row, zero rows in `admin_profiles`, `admin_audit_events`, `enquiries`, `sample_requests`, and `sample_request_items`, plus Storage buckets `urblo-admin-media` private and `urblo-public-media` public. This was read-only only; no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes were created or changed.
- Latest admin browser-secret/config-gate verifier checkpoint on 2026-05-29: `scripts/check-admin-crud-coverage.mjs` now scans all `src` browser source files for actual Supabase service-role env/client usage patterns, verifies the admin config-missing state copy and login/unauthorized handling, guards admin-route WelcomePopup suppression, and fails if `scripts/check-admin-crud-live.mjs` drifts from browser-key/RLS verification back to service-role access. `node --check scripts/check-admin-crud-coverage.mjs`, `npm run agent:admin-crud-coverage`, `npm run agent:live-readiness`, plan-only `npm run agent:admin-crud-live`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:check`, `npm run agent:public-supabase-readiness`, `npm run agent:cloudflare-readiness`, and `git diff --check` pass. This is source-only and creates no Supabase rows, Storage objects, Auth users, Cloudflare state, credentials, or live writes.
- Latest Forms API wrapper coverage checkpoint on 2026-05-29: `scripts/check-forms-api.mjs` now imports the Cloudflare Pages Function endpoint wrappers and verifies GET requests return `method_not_allowed`, OPTIONS returns the 204 preflight response without Supabase calls, and invalid Sample Request POSTs fail validation before Supabase calls. `node --check scripts/check-forms-api.mjs`, `node scripts/check-forms-api.mjs`, `npm run agent:forms-ui`, `npm run agent:live-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:admin-crud-coverage`, `npm run agent:smoke`, `npm run agent:check`, plan-only `npm run agent:admin-crud-live`, `npm run agent:cloudflare-readiness`, `npm run agent:public-supabase-readiness`, and `git diff --check` pass. No Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes were created or changed.
- Latest admin login next-target hardening on 2026-05-29: `/admin/login` now accepts only true admin-console `next` targets (`/admin`, `/admin?*`, or `/admin/*`) and falls back to `/admin` for login/unauthorized loops or `/administrator`-style prefixes. `npm run agent:admin-crud-coverage` now guards this source contract and also checks that `AdminAuthProvider` validates sessions with `supabase.auth.getUser()` before querying the active `admin_profiles` row. `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:live-readiness`, `npm run agent:check`, and `git diff --check` pass. This is source-only; no Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes were created or changed.
- Latest first-admin bootstrap audit guard on 2026-05-29: approved first-admin write mode now inserts `admin_profile.bootstrap` into `admin_audit_events` with `actor_user_id = null` and target Auth/profile metadata, and fails if that audit event cannot be recorded after the profile upsert. `npm run agent:first-admin-bootstrap` still runs in no-write plan mode without Supabase calls, and `npm run agent:admin-crud-coverage` now guards the bootstrap audit source contract. `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:live-readiness`, `npm run agent:check`, and `git diff --check` pass. No Supabase rows, Auth users, Storage objects, Cloudflare state, credentials, or live writes were created or changed.
- Latest Contact form UI source contract checkpoint on 2026-05-29: `scripts/check-contact-form-ui-source.mjs` and `npm run agent:forms-ui` were added, `npm run agent:smoke` now runs the check after API mock coverage, and `npm run agent:check` now guards that package script/smoke integration. `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:forms-ui`, `npm run agent:live-readiness`, `npm run agent:check`, and `git diff --check` pass. The check is source-only and no-secret; it does not submit live forms, create Supabase rows, send email, or touch Cloudflare.
- Latest admin live publish/archive verifier checkpoint on 2026-05-29: `scripts/check-admin-crud-live.mjs` plan and live mode now exercise publish-then-archive transitions for public-facing tagged QA rows before the final anonymous browser-key invisibility check, and `scripts/check-admin-crud-coverage.mjs` now fails if those publish actions are removed. `node --check scripts/check-admin-crud-live.mjs`, `node --check scripts/check-admin-crud-coverage.mjs`, plan-only `npm run agent:admin-crud-live`, `npm run agent:admin-crud-coverage`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:public-supabase-readiness`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:check`, and `git diff --check` pass. This is source-only and does not create Supabase rows, Storage objects, Auth users, Cloudflare state, credentials, or live writes.
- Latest Cloudflare preview route-list alignment on 2026-05-29: `docs/CLOUDFLARE_DEPLOYMENT.md` now lists canonical `/products/prime-block`, article detail, and `/capabilities` as deployed-preview direct-refresh checks, while treating old product/article URLs as redirect checks. `scripts/check-cloudflare-pages-readiness.mjs` now fails if those canonical route checks drop out of the runbook.
- Latest admin archive contract verifier hardening on 2026-05-29: `scripts/check-admin-crud-coverage.mjs` now fails if admin CMS/task acceptance drifts back toward physical-delete language instead of the launch archive/removal contract, and `docs/ARCHITECTURE.md` / `docs/agent/tasks.json` now record that added machine check. This is source/docs-only and creates no Supabase rows, Storage objects, Auth users, Cloudflare state, credentials, or live writes.
- Latest admin archive/delete contract alignment on 2026-05-29: `docs/agent/tasks.json`, `docs/ADMIN_IA_ACCESS.md`, `docs/SUPABASE_SCHEMA.md`, and `docs/ARCHITECTURE.md` now align the admin CMS acceptance criteria with the implemented non-destructive archive lifecycle. Launch-critical admin live verification should prove create/update/publish/archive, public invisibility, and auditability; physical deletes remain outside the launch path until Jay approves a retention/destructive-delete policy. This is docs-only and creates no Supabase rows, Storage objects, Auth users, Cloudflare state, credentials, or live writes.
- Latest admin Storage live-readiness checkpoint on 2026-05-29: `scripts/check-live-readiness.mjs` now reports `npm run agent:admin-crud-live -- --allow-writes --include-storage` as a separate final media upload policy proof, and `scripts/check-cloudflare-pages-readiness.mjs` now fails if the Cloudflare runbook drops that command. This keeps the eventual live admin CMS verification from treating metadata CRUD/audit checks as sufficient media upload proof. This is source-only and does not create Supabase rows, Storage objects, Auth users, Cloudflare state, credentials, or live writes.
- Latest Cloudflare first-admin approval-gate guard on 2026-05-29: `docs/CLOUDFLARE_DEPLOYMENT.md` now includes `--first-admin-writes-approved`, the read-only first-admin bootstrap command, and the approval-gated `npm run agent:first-admin-bootstrap -- --allow-writes --admin-email <first-admin-email> --confirm-email <first-admin-email>` path before admin browser QA. `scripts/check-cloudflare-pages-readiness.mjs` now fails if the runbook drops those first-admin write gate instructions. This is source-only and does not create Supabase users/profiles, configure Cloudflare, add secrets, or run live writes.
- Latest first-admin write readiness checkpoint on 2026-05-29: `scripts/check-live-readiness.mjs` now reports the approval-gated `npm run agent:first-admin-bootstrap -- --allow-writes --admin-email <first-admin-email> --confirm-email <first-admin-email>` path separately from the read-only first-admin verifier. `--first-admin-writes-approved` only clears the manual readiness gate and does not replace the service-role key, `--allow-writes`, or `--confirm-email` guards. `npm run agent:live-readiness`, `npm run agent:live-readiness -- --first-admin-writes-approved`, `npm run agent:live-readiness -- --json --first-admin-writes-approved`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:public-supabase-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:check`, and `git diff --check` pass. Supabase MCP read-only sanity still shows 10 migrations, 24/24 expected tables with RLS enabled, 12 published finish definitions, one published default site settings row, and zero private workflow rows. This is no-secret/no-write tooling and created no Supabase users, profiles, rows, Storage objects, or Cloudflare state.
- Latest agent-init command alignment on 2026-05-29: `scripts/agent-init.sh` now lists `npm run agent:forms-live -- --allow-writes` instead of the old bare live form command, keeping the startup briefing aligned with the forms live write-mode guard. This is source-only and does not run live writes.
- Latest live forms write-mode guard on 2026-05-29: `scripts/check-forms-api-live.mjs` now refuses to run live form verification unless `--allow-writes` is supplied, so service-role credentials alone cannot create tagged enquiry/sample-request QA rows. `scripts/check-live-readiness.mjs` now reports the executable commands as `npm run agent:forms-live -- --allow-writes`, `npm run agent:forms-live -- --allow-writes --base-url <preview-or-production-origin>`, and `npm run agent:forms-live -- --allow-writes --require-browser-boundary`; `scripts/check-cloudflare-pages-readiness.mjs` now enforces those command forms in the Cloudflare runbook.
- Latest Cloudflare runbook approval-gate guard on 2026-05-29: `scripts/check-cloudflare-pages-readiness.mjs` now fails if `docs/CLOUDFLARE_DEPLOYMENT.md` drops the `--form-writes-approved` / `--admin-writes-approved` flags or the Jay approval language around tagged live form/admin QA writes. This is source-only and does not configure Cloudflare, add secrets, or run live Supabase writes.
- Latest live-readiness form-write approval gate on 2026-05-29: `scripts/check-live-readiness.mjs` now treats `npm run agent:forms-live -- --allow-writes`, `npm run agent:forms-live -- --allow-writes --base-url <preview-or-production-origin>`, and `npm run agent:forms-live -- --allow-writes --require-browser-boundary` as manually gated until Jay's approval for tagged live form QA writes is represented with `--form-writes-approved`. The command remains no-secret and no-write; `node --check scripts/check-live-readiness.mjs`, `npm run agent:live-readiness`, and `npm run agent:live-readiness -- --form-writes-approved` pass locally, with the approval flag only clearing the form manual gate and not substituting for missing service-role/browser-safe keys.
- Latest Cloudflare runbook private-boundary checkpoint on 2026-05-29: `docs/CLOUDFLARE_DEPLOYMENT.md` now lists `npm run agent:forms-live -- --allow-writes --require-browser-boundary` as the final private-row form proof after service-role and browser-safe Supabase keys are configured, and `scripts/check-cloudflare-pages-readiness.mjs` now fails if the runbook drops that command. `node --check scripts/check-cloudflare-pages-readiness.mjs`, `npm run agent:cloudflare-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:live-readiness`, `npm run agent:public-supabase-readiness`, `npm run agent:check`, and `git diff --check` pass.
- Latest live forms private-boundary checkpoint on 2026-05-29: `scripts/check-forms-api-live.mjs` now supports `--require-browser-boundary`, which requires a browser-safe Supabase key and verifies created `enquiries`, `sample_requests`, and `sample_request_items` rows are not anonymously readable through browser-key access. Without that flag, the verifier still runs the boundary check whenever a browser-safe key is present and logs an explicit skip when it is absent. `scripts/check-live-readiness.mjs` now reports the required inputs for `npm run agent:forms-live -- --allow-writes --require-browser-boundary`. `node --check scripts/check-forms-api-live.mjs`, `node --check scripts/check-live-readiness.mjs`, `npm run agent:live-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:public-supabase-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:check`, and `git diff --check` pass; credential-gated live form execution still fails closed before Supabase calls unless `--allow-writes`, service-role credentials, and Jay approval are present. Supabase MCP read-only counts confirm private workflow tables remain at 0 rows.
- Latest admin live private-lead RLS guard on 2026-05-29: `scripts/check-admin-crud-live.mjs` plan and live mode now include anonymous browser-key readback for tagged private `enquiries`, `sample_requests`, and `sample_request_items` QA rows in addition to tagged draft/archived public content. `scripts/check-admin-crud-coverage.mjs` now fails if that private-lead boundary check is removed from the live verifier. `node --check scripts/check-admin-crud-live.mjs`, `node --check scripts/check-admin-crud-coverage.mjs`, plan-only `npm run agent:admin-crud-live`, `npm run agent:admin-crud-coverage`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:public-supabase-readiness`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:check`, and `git diff --check` pass. Supabase MCP read-only counts confirm `admin_profiles`, `admin_audit_events`, `enquiries`, `sample_requests`, and `sample_request_items` remain at 0 rows.
- Latest admin live public-RLS invisibility checkpoint on 2026-05-29: `scripts/check-admin-crud-live.mjs` plan and live mode now include an anonymous browser-key readback after tagged QA writes. The live run will assert tagged `site_settings`, `media_assets`, `stone_groups`, `products`, `projects`, and `articles` rows are not visible through public RLS after the verifier leaves them draft/archived. `node --check scripts/check-admin-crud-live.mjs`, plan-only `npm run agent:admin-crud-live`, and `npm run agent:admin-crud-coverage` pass with no Supabase writes.
- Latest admin readiness browser-key boundary checkpoint on 2026-05-29: `scripts/check-admin-live-readiness.mjs` now uses the configured browser-safe key in read-only mode to verify published `site_settings` and `finish_definitions` are readable while `admin_profiles` returns no private rows or an expected deny response without an authenticated admin session. `node --check scripts/check-admin-live-readiness.mjs` passes; `npm run agent:admin-live-readiness -- --admin-email first@example.com` still fails closed before Supabase calls because browser-safe and service-role keys are not configured. `npm run agent:live-readiness` remains report-only green with the same missing live inputs. No Supabase rows, Auth users, Storage objects, credentials, Cloudflare state, or live writes were created or changed.
- Latest Supabase read-only launch sanity on 2026-05-29: connector checks against project `npkidywzwddbnfrnxlmo` showed 10 launch migrations through `security_definer_private_helpers`, 24/24 expected public launch tables present with RLS enabled, 99 policy expressions using `private.has_admin_role(...)`, 0 using `public.has_admin_role(...)`, no direct `anon`/`authenticated` routine privileges on exposed public admin helper functions, 12 published finish definitions, 1 published default site settings row, 0 rows in `admin_profiles`, `admin_audit_events`, `enquiries`, `sample_requests`, and `sample_request_items`, and both `urblo-public-media` / `urblo-admin-media` buckets present with authenticated Storage object policies. This was read-only and made no Supabase, Storage, Auth, Cloudflare, credential, or runtime source changes; the zero form/audit row facts are superseded by the 2026-06-02 tagged live form QA rows.
- Latest admin article structured-authoring coverage on 2026-05-29: `scripts/check-admin-crud-coverage.mjs` now verifies `/admin/articles` includes every approved schema block type option, keeps published block validation tied to structured content, and rejects raw HTML/newsletter authoring helper regressions such as `dangerouslySetInnerHTML`, `rawHtml`, or newsletter HTML fields. `node --check scripts/check-admin-crud-coverage.mjs`, `npm run agent:admin-crud-coverage`, runtime gates, Harness checks, and `git diff --check` pass. This is source-only and does not verify live article saves or audit rows.
- Latest public Supabase article-block readiness guard on 2026-05-29: `scripts/check-public-supabase-readiness.mjs` now verifies the article structured import shape as part of the no-write public cutover gate. It fails on placeholder `article_blocks`, missing image-to-`media_assets` links, shared newsletter/social image imports, newsletter footer/contact artifact leakage, missing rich-text claim-review metadata, or `reviewFlags` without `claimReviewStatus = needs_review`. `node --check scripts/check-public-supabase-readiness.mjs`, `npm run agent:content-import`, `npm run agent:content-import:apply-sql`, `npm run agent:public-supabase-readiness`, `npm run agent:live-readiness`, runtime gates, Harness checks, and `git diff --check` pass with 115 media candidates, 95 article blocks, 0 warnings, 0 blockers, and no Supabase writes.
- Latest article structured import checkpoint on 2026-05-29: `scripts/check-content-import-readiness.mjs` now extracts legacy newsletter HTML into 95 draft structured `article_blocks` rows (`rich_text`, `image`, `cta`, and `project_spotlight`), links image blocks to `media_assets`, skips newsletter footer/contact/social artifacts, and adds claim-review flags without rewriting source copy or treating it as approved. `node --check scripts/check-content-import-readiness.mjs`, `npm run agent:content-import`, `npm run agent:content-import:apply-sql`, `npm run agent:public-supabase-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:check`, and `git diff --check` pass with 115 media candidates, 95 article blocks, 0 warnings, 0 blockers, and no Supabase writes.
- Latest live forms verifier refinement on 2026-05-29: `scripts/check-forms-api-live.mjs` now asserts that each valid live enquiry/sample-request response `notificationStatus` is one of `not_required`, `sent`, or `failed` and matches the stored Supabase `notification_status` value. `node --check scripts/check-forms-api-live.mjs` passes; `npm run agent:forms-live` still fails closed before Supabase calls unless `--allow-writes` and service-role credentials are supplied.
- Latest Forms notification mock checkpoint on 2026-06-03: `scripts/check-forms-api.mjs` now verifies configured SMTP2GO and Resend notification behavior without real email secrets. Mock checks prove enquiry notifications set initial `notification_status = pending`, call the configured provider, patch `enquiries.notification_status = sent` on success, and sample request notifications patch `sample_requests.notification_status = failed` when the provider reports failure while still returning visitor success. `node --check scripts/check-forms-api.mjs` and `node scripts/check-forms-api.mjs` pass with no Supabase writes and no real email calls.
- Latest first-admin bootstrap runner checkpoint on 2026-05-29: `npm run agent:first-admin-bootstrap` now prints the no-write first-admin plan without Supabase calls, invites, profile writes, or deletes. `npm run agent:first-admin-bootstrap -- --verify-only --admin-email first@example.com` fails closed when no service-role key is configured. `npm run agent:live-readiness` now reports the first-admin bootstrap verifier inputs alongside admin readiness and live write blockers. Live bootstrap write/invite mode remains approval-gated and was not run.
- Latest Stone Library finish-image import checkpoint on 2026-05-29: `npm run agent:content-import` now extracts current static finish-specific Stone Library image mappings from `src/data/stoneFinishImages.ts` into 53 draft `stone_finish_images` payload rows, linked by stone group, variant, finish, media source, image role, and sort order. The dry run now prepares 104 media candidates, including local `data/Product` source assets used by finish imagery. `npm run agent:content-import:apply-sql` now includes `stone_finish_images` in the apply/rollback order, read-only preflight SQL, guarded draft apply SQL, and row-count verification summary. `node --check scripts/check-content-import-readiness.mjs`, `npm run agent:content-import`, `npm run agent:content-import:apply-sql`, `npm run agent:public-supabase-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:check`, and `git diff --check` pass with 0 import warnings/blockers and no Supabase writes.
- Latest Stone Library finish-image admin source checkpoint on 2026-05-29: `/admin/stone-library` now reads `stone_finish_images` and `media_assets`, provides a protected finish-image linking form for selected stone variants/finishes, records `stone_finish_image.create/update/publish/archive` audit actions after successful mutations, and requires published finish images to reference a published media record. `npm run agent:admin-crud-coverage` now verifies Stone Library image table/action coverage, and plan-only `npm run agent:admin-crud-live` now includes tagged `stone_finish_images` create/publish/archive coverage for the later credential-gated live run. `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:public-supabase-readiness`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:check`, and `git diff --check` pass. Playwright Firefox verified `/admin/stone-library` still renders only the configuration-required state without private Stone Library content or console warnings/errors when no browser-safe key is configured. Live save/upload/export verification still requires browser-safe Supabase config, active admin/editor profile access, and Jay approval for tagged writes where applicable.
- Latest content import apply-SQL source checkpoint on 2026-05-29: `npm run agent:content-import:apply-sql` now writes ignored `.tmp/content-import-preview.json`, `.tmp/content-import-plan.md`, `.tmp/content-import-preflight.sql`, and `.tmp/content-import-apply.sql` artifacts with zero warnings/blockers and no Supabase writes. The generated apply SQL has an approval guard that aborts unless `urblo.import_approved=true` is set in the transaction, keeps import rows as `draft`, and static inspection found no delete/drop/truncate statements or published-status inserts. Local gates `npm run agent:content-import`, `npm run agent:content-import:apply-sql`, `npm run agent:public-supabase-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, `npm run agent:check`, and `git diff --check` pass. No Supabase rows, Storage objects, Cloudflare state, credentials, or live admin writes were created or changed.
- Latest Supabase SECURITY DEFINER helper hardening on 2026-05-29: migration `security_definer_private_helpers` is applied on project `npkidywzwddbnfrnxlmo`; Supabase security advisor returns zero lints; 99 policies now call `private.has_admin_role(...)`, zero policies call `public.has_admin_role(...)`; `authenticated` cannot execute `public.current_admin_role()` or `public.has_admin_role(text[])`; `anon` can still read one published `site_settings` row and 12 published `finish_definitions`; role `authenticated` without a JWT sees those public rows and zero `admin_profiles`. Local gates `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:admin-crud-coverage`, `npm run agent:public-supabase-readiness`, `npm run agent:live-readiness`, `npm run agent:cloudflare-readiness`, plan-only `npm run agent:admin-crud-live`, `npm run agent:check`, `git diff --check`, and a changed-file secret pattern scan pass. No table rows, users, profiles, Storage objects, Cloudflare state, or live admin writes were created or deleted.
- Latest configured browser-key unauthenticated admin gate on 2026-05-29: using a Supabase publishable key supplied only through the local shell, Vite dev server + Playwright Firefox verified `/admin` resolves to `/admin/login?next=%2Fadmin`, `/admin/media` resolves to `/admin/login?next=%2Fadmin%2Fmedia`, both render the `Admin login` form instead of the configuration-required state, protected dashboard/media content is absent, and browser console has 0 errors/0 warnings beyond React DevTools info. `npm run agent:live-readiness` still reports missing service-role key, persistent browser-safe key env, first-admin email, admin session credentials, Jay approval for tagged live QA writes, and Cloudflare preview URL. No Supabase data writes, profile changes, credentials files, Cloudflare changes, or live QA writes were performed.
- Latest live-readiness override refinement on 2026-05-28: `npm run agent:live-readiness -- --base-url <preview-origin> --admin-email <first-admin-email> --admin-writes-approved` passes in report-only mode and treats the non-secret preview URL, first-admin email, and approval flag as present without printing values or running writes. The paired `--strict` command still fails as expected because service-role key, browser-safe key, and admin session credentials are still missing.
- Latest live-readiness runner checkpoint on 2026-05-28: `node --check scripts/check-live-readiness.mjs`, `npm run agent:init`, `npm run agent:live-readiness`, `npm run agent:live-readiness -- --json`, `npm run agent:cloudflare-readiness`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:public-supabase-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass. `npm run agent:live-readiness -- --strict` fails as expected because no local env files are present and the live inputs remain missing/manual-gated: service-role key, preview URL, browser-safe Supabase key, first-admin email, admin session credentials, and Jay approval for tagged live QA writes.
- Latest post-alignment baseline on 2026-05-28: after commits `03ff809` and `f66b5dc`, worktree is clean; `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:cloudflare-readiness`, `npm run agent:public-supabase-readiness`, `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass. Supabase connector read-only sanity still shows nine launch migrations, latest migration `security_definer_function_grants`, 12 checked core tables with RLS enabled, private workflow rows at 0 (`admin_profiles`, `admin_audit_events`, `enquiries`, `sample_requests`, `sample_request_items`), 12 finish definitions, and one site settings row. Credential-gated `npm run agent:forms-live` still stops on missing `SUPABASE_SERVICE_ROLE_KEY`, and `npm run agent:admin-live-readiness` still stops on missing browser-safe Supabase key, service-role key, and first-admin email.
- Latest goal-resume readiness audit on 2026-05-28: `npm run agent:init`, `npm run agent:admin-crud-coverage`, plan-only `npm run agent:admin-crud-live`, `npm run agent:cloudflare-readiness`, and `npm run agent:public-supabase-readiness` pass from the current clean worktree. Supabase connector sanity still shows nine launch migrations present, checked core tables with RLS enabled, private workflow rows at 0 (`admin_profiles`, `admin_audit_events`, `enquiries`, `sample_requests`, `sample_request_items`), 12 finish definitions, and one site settings row. Credential-gated `npm run agent:forms-live` still stops on missing `SUPABASE_SERVICE_ROLE_KEY`, and `npm run agent:admin-live-readiness` still stops on missing browser-safe Supabase key, service-role key, and first-admin email.
- Last Supabase foundation gate on 2026-05-27: `foundation_schema`, `foundation_hardening`, and `anon_read_only` migrations applied to project `npkidywzwddbnfrnxlmo`; Supabase checks confirmed three migrations listed, 24 expected tables, 24/24 RLS enabled, no anonymous private lead/admin privileges, anonymous public content grants are read-only, no missing public-schema foreign-key indexes, and `enquiries_new_queue_idx` / `sample_requests_new_queue_idx` exist. Local gates `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:check`, `git diff --check`, and `npm run agent:smoke` pass.
- Last Supabase baseline seed gate on 2026-05-27: `baseline_seed` migration applied to project `npkidywzwddbnfrnxlmo`; Supabase checks confirmed 12 distinct finish definitions, one published `default` site settings row, and idempotent rerun with no duplicate rows.
- Last forms backend source gate on 2026-05-27: `/api/enquiries` and `/api/sample-requests` Pages Functions added; Contact page main submit flow posts to those endpoints; `node scripts/check-forms-api.mjs` confirms valid/invalid/Turnstile-failure behavior with mocked Supabase/Turnstile calls; local gates `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass.
- Last admin auth shell source gate on 2026-05-28: `/admin`, `/admin/login`, `/admin/unauthorized`, and protected module routes were added outside the public site chrome; admin auth uses `@supabase/supabase-js`, browser-safe `VITE_SUPABASE_ANON_KEY`/`VITE_SUPABASE_PUBLISHABLE_KEY`, Supabase Auth session checks, and active `admin_profiles` lookup. Local gates `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass. Playwright verified `/admin` and `/admin/login` show configuration-required state without a browser key, no WelcomePopup, no dashboard content, and no console errors beyond React DevTools info.
- Last admin dashboard launch-check copy source gate on 2026-05-28: dashboard open launch checks now point to current blockers only: service-role form verification, first admin profile, live admin save/upload/export audit verification, and approved content import scope. Local `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:check`, and `git diff --check` pass.
- Last admin settings/RLS gate on 2026-05-28: Supabase migration `admin_settings_role_hardening` applied to project `npkidywzwddbnfrnxlmo`; `pg_policies` confirms `site_settings` INSERT/UPDATE/DELETE now require owner/admin and SELECT remains viewer/editor/admin/owner. `/admin/settings` source adds status, contact, social, SEO, and footer content editing behind the auth gate.
- Last media Storage/admin source gate on 2026-05-28: Supabase migrations `media_storage_foundation` and `media_storage_listing_hardening` applied to project `npkidywzwddbnfrnxlmo`; verification confirmed the two Urblo buckets, admin/editor object INSERT/UPDATE policies, owner/admin object DELETE policy, viewer/editor/admin/owner object SELECT policy, and removal of the broad public object-listing policy flagged by the Storage advisor. `/admin/media` source adds upload-backed draft record creation, external media records, metadata editing, audit-gated visible manifest export, and publish/archive guardrails behind the auth gate. Local `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass. Playwright verified `/admin/media` shows the configuration-required state when no browser-safe key is configured.
- Last Media manifest export source gate on 2026-05-28: `/admin/media` now includes admin/editor CSV export for the currently visible media manifest. Export attempts a `media_assets.export_manifest` audit event first and blocks the download if audit logging fails. Local `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:check`, and `git diff --check` pass.
- Last Stone Library admin source gate on 2026-05-28: `/admin/stone-library` replaced the scaffold with a protected source CRUD screen for stone groups, variants, finish definitions, and finish capability rows. Supabase policy inspection confirms Stone Library tables keep admin role read/write policies and public published-only read policies. Local `npm run lint`, `npx tsc -b`, `npm run build`, and `npm run agent:smoke` pass. Playwright with Firefox verified `/admin/stone-library` shows the configuration-required state when no browser-safe key is configured and console output had 0 errors/warnings.
- Last Projects admin source gate on 2026-05-28: `/admin/projects` replaced the scaffold with a protected source CRUD screen for project records, facts, material schedule rows, material maps, and hotspots. Local `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass, including `/admin/projects` route shell coverage. Playwright CLI with Firefox verified `/admin/projects` shows the configuration-required state when no browser-safe key is configured and console output had 0 errors/warnings.
- Last Products admin source gate on 2026-05-28: `/admin/products` replaced the scaffold with a protected source CRUD screen for product families, models, material defaults, and specs. Local `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass, including `/admin/products` route shell coverage. Playwright CLI with Firefox verified `/admin/products` shows the configuration-required state when no browser-safe key is configured and console output had 0 errors/warnings.
- Last Articles admin source gate on 2026-05-28: `/admin/articles` replaced the scaffold with a protected source CRUD screen for article metadata and structured article blocks. Local `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass, including `/admin/articles` route shell coverage. Playwright CLI with Firefox verified `/admin/articles` shows the configuration-required state when no browser-safe key is configured and console output had 0 errors/warnings.
- Last Leads admin source gate on 2026-05-28: `/admin/leads` replaced the scaffold with a protected source workflow screen for enquiry/sample request queues, sample items, status, assignment, internal notes, notification state, and read-only role behavior. Local `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass, including `/admin/leads` route shell coverage. Playwright CLI with Firefox verified `/admin/leads` shows the configuration-required state when no browser-safe key is configured.
- Last Audit admin source gate on 2026-05-28: `/admin/audit` replaced the scaffold with a protected owner/admin read-only screen for audit event list, actor/entity filters, metadata inspection, restricted-role state, and empty/error states. Local `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass, including `/admin/audit` route shell coverage. Playwright CLI with Firefox verified `/admin/audit` shows the configuration-required state when no browser-safe key is configured.
- Last Admin audit-writer source gate on 2026-05-28: `src/lib/adminAudit.ts` was added and Settings, Media, Stone Library, Projects, Products, Articles, and Leads save flows now attempt audit inserts after successful primary saves. Local `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass. Live audit row creation remains unverified until browser-safe Supabase config and an active profile are available.
- Last content import plan gate on 2026-05-28: `npm run agent:content-import` and `npm run agent:content-import:plan` passed with the then-current import scope before Stone Library finish-image links were added to the payload. The plan command writes local ignored `.tmp/content-import-preview.json` and `.tmp/content-import-plan.md` artifacts with no Supabase writes. Current counts are recorded in the latest 2026-05-29 finish-image import checkpoint above.
- Last content import preflight SQL gate on 2026-05-28: `npm run agent:content-import:preflight-sql` writes local ignored `.tmp/content-import-preview.json`, `.tmp/content-import-plan.md`, and `.tmp/content-import-preflight.sql` artifacts with no Supabase writes. Supabase connector target-count/RLS preflight confirms current import targets are empty, seed tables contain 12 finish definitions and one site settings row, and all checked target/seed tables have RLS enabled.
- Last admin helper/RLS hardening gate on 2026-05-29: `/admin/settings` source includes the non-destructive admin team manager for existing Supabase Auth users, owner/admin profile create/update flows, UI owner-protection guardrails, and audit-event attempts after profile saves. Supabase migrations `admin_profile_owner_hardening`, `security_definer_function_grants`, and `security_definer_private_helpers` are applied and verified on project `npkidywzwddbnfrnxlmo`; policy inspection confirms admins can maintain non-owner profiles while owner-role changes require owner, public helper RPC execution is revoked from browser roles, RLS/Storage policies call private helpers, and the Supabase security advisor reports zero security lints.
- Last Leads export source gate on 2026-05-28: `/admin/leads` now includes owner/admin CSV export for the currently loaded enquiry/sample-request queue. Export attempts a `leads.export_csv` audit event first and blocks the download if audit logging fails. Local `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:check`, and `git diff --check` pass.
- Last server-side form audit source gate on 2026-05-28: form handlers now attempt `admin_audit_events` writes after successful enquiry/sample request inserts. `node scripts/check-forms-api.mjs` confirms valid enquiry/sample request audit calls, audit failure still returns visitor success after lead storage, missing service-role config fails closed before Supabase calls, and Turnstile failure still fails closed. Local `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass.
- Last form environment documentation gate on 2026-06-03: `.env.example`, `docs/CLOUDFLARE_DEPLOYMENT.md`, and `docs/ARCHITECTURE.md` now list the canonical Pages Function variables plus supported compatibility aliases for Supabase service key, Turnstile, SMTP2GO/Resend provider configuration, and Resend sender compatibility. `npm run agent:check` and `git diff --check` pass.
- Last live forms verification runner source gate on 2026-05-28: `npm run agent:forms-live -- --allow-writes` is the credential-gated script for direct-handler or deployed-endpoint form verification. It loads `.env.local`, `.env`, `.dev.vars`, and shell variables; requires `--allow-writes`, `SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_SERVICE_KEY`, and Jay approval; verifies valid enquiry and sample request persistence plus audit rows; verifies invalid enquiry and sample request payloads create no rows; and keeps tagged test rows for auditability. Source checks pass without credentials; basic deployed live execution has since passed on Cloudflare Pages, while final browser-boundary, email, and Turnstile proofs remain pending their inputs.
- Last admin live readiness runner source gate on 2026-05-28: `npm run agent:admin-live-readiness -- --admin-email <first-admin-email>` was added as a read-only credential/profile readiness check. It requires a browser-safe Supabase key, service-role verification key, and first-admin email; verifies one active admin profile with the required role plus baseline settings/finish seed rows; and intentionally performs no user/profile creation or database mutation.
- Last admin CRUD coverage source gate on 2026-05-28: `npm run agent:admin-crud-coverage` was added as a source-only verifier for `/admin` route/module registration, `RequireAdmin` state handling, browser-key-only Supabase client wiring, launch-critical table references, role-gated controls, publish/archive paths, shared audit writer usage, and Media/Leads export audit gates. It passes locally and does not mutate Supabase. Supabase connector read-only sanity still shows nine launch migrations present, 24 RLS-enabled public tables, zero live private workflow rows, and baseline 12 finish definitions plus one site settings row.
- Last admin scaffold cleanup source gate on 2026-05-28: the retired `AdminModulePage` scaffold component and unused `scaffold` / `locked` module branches were removed because all launch-critical admin modules now have real source screens. Admin CRUD coverage now fails if that retired scaffold file reappears.
- Last admin CRUD live verifier source gate on 2026-05-28: `npm run agent:admin-crud-live` was added as a credential-gated live write verifier. Plan-only mode passes without credentials and performs no writes. Live mode requires `--allow-writes` plus browser-safe Supabase config and a real owner/admin Supabase Auth session, then writes tagged draft/archived QA rows across launch-critical admin tables and records audit events through normal RLS. `node --check scripts/check-admin-crud-live.mjs` and plan-only `npm run agent:admin-crud-live` pass locally.
- Last Cloudflare Pages readiness source gate on 2026-05-28: `npm run agent:cloudflare-readiness` was added as a no-secret repo-side verifier for the Cloudflare Pages build command, Vite base, SPA fallback, Function routing scope, headers, API handler files, environment placeholders, and deployment runbook. Those repo-side checks still pass; the former Pages project/custom-domain/DNS blockers are now resolved and recorded in the current Cloudflare production state above.
- Last Cloudflare preview smoke source gate on 2026-05-28: `npm run agent:cloudflare-preview-smoke` was added as a no-secret deployed-preview verifier. Local Vite preview verification passed for direct-refresh public/admin route shells, unknown-route fallback, and deployed asset references at `http://127.0.0.1:4184`; Cloudflare-only redirect and Function checks are intentionally skipped on local hosts and will run against the real `*.pages.dev` URL.
- Last public Supabase readiness source gate on 2026-05-28: content import status mapping was tightened so Stone Library import candidates remain `draft` instead of inheriting current active/TBC public state. `npm run agent:public-supabase-readiness` passes and verifies 13 stone groups, 6 products, 5 projects, and 4 articles stay draft in the dry run, plus published-only public RLS policy source, read-only anonymous grants, static public runtime boundaries, Cloudflare SPA fallback, and `/api/*` Function routing scope.
- Last full runtime gate on 2026-06-03: `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:smoke` pass during the task-state reconciliation checkpoint. Build keeps the previous healthy JavaScript chunk profile; Browserslist staleness notice remains.
- Last docs/harness gate on 2026-06-03: `npm run agent:check`, `npm run agent:admin-crud-coverage`, `npm run agent:content-import:apply-sql`, `npm run agent:public-supabase-readiness`, and `git diff --check` pass.
- Recent title typography polish on 2026-05-25: global public page H1s now follow the Projects page title treatment, and Article detail no longer uses the old Space Grotesk uppercase H1.
- Recent motion polish on 2026-05-25: route changes now use restrained pathname-keyed enter transitions, and proof metrics visibly count from 0 when scrolled into view.
- Recent Stone Library overlay polish on 2026-05-25: list-card status badges, image provenance, zoom, and collapsed finish labels now use dark translucent surfaces with white text, reserving Urblo lime for confirmed/action signals rather than broad label fills.
- Recent Stone Library status polish on 2026-05-25: external Available/Upcoming/No states now use shared lightweight status pills across detail header, variant switch, finish selector, availability summary, finish capability, and cut option rows; heavy black status blocks and full lime fills are avoided.
- Recent homepage hero polish on 2026-06-01: global header and homepage first-viewport copy use an edge-aligned gutter, and the old `Stone Solutions for Street` hero copy has been replaced with sequential all-caps `DESIGN`, `SOURCE`, `DELIVER.` lines, second-line offset, letter-by-letter left-to-right reveal motion, and a single Urblo-green punctuation mark only on the final `DELIVER.` line.
- Recent homepage proof-section polish on 2026-05-26: the old rendered sustainability/tabbed feature module has been removed from the homepage flow, and the proof metrics section now appears directly after the hero with the approved stone/city framing and updated 50+/130+/20+/3500+ metrics.
- Recent homepage partner-banner image/copy polish on 2026-06-01: the banner uses `Design-led stone solutions for streetscapes & civil landscapes.`, highlights `Design-led` in Urblo lime, renders the sentence from `src/data/homepage.ts`, and now uses the West Side Place aerial image at `public/media/launch/homepage/partner-banner-west-side-place.jpg`.
- Recent homepage section-order/nav/video polish on 2026-06-03: the `Design-led` partner banner has been reduced to a slimmer transition band, Latest Projects now sits immediately below it before Product Showcase, the bottom Play modal uses lazy `youtube-nocookie` embed `UfRtQZSi7cM`, desktop header keeps Articles/Products inside the right-side hamburger, and the desktop primary nav plus hamburger remain one right-aligned group while mobile keeps the full navigation menu in the hamburger.
- Recent homepage Latest Projects redesign on 2026-06-01: the old black project-card grid has been replaced with `The work speaks.`, a selected-project detail panel, a large active project image, and a draggable five-project thumbnail rail that shows four items on desktop. Hover/focus/tap changes the selected project; the upper `View project` link remains the route navigation.
- Recent homepage Latest Projects stability polish on 2026-06-02: the section now occupies exactly one viewport with `100svh`; active copy, active image, and rail heights remain fixed when hover/tap changes the selected project. Short desktop viewports hide the active summary and align facts beside the CTA; short mobile viewports simplify summary/facts and shrink rail media so text and controls do not overlap. Playwright fallback QA covered 1440x900, 1366x768, 1280x720, 390x844, and 375x667 with zero height delta, zero horizontal overflow, and no console errors; Browser plugin QA was unavailable because no in-app browser backend was listed.
- Recent homepage Latest Projects sketch-ratio polish on 2026-06-02: the section now follows the approved two-row/four-column sketch: upper copy spans two columns, upper feature image spans two columns, and the lower draggable rail shows four portrait project images on desktop. `HomepageProject` now supports optional `featureImage`/`featureImageAlt`, currently populated from existing second-detail project media with fallback to the rail image. Browser plugin DOM/console QA passed on the in-app viewport but Browser screenshot capture timed out, so Playwright Chromium captured screenshots and verified `1440x900`, `1366x768`, and `390x844`: section height equals viewport, feature image height equals rail image height, desktop feature width equals two rail slots plus gutter, `View project` remains visible, hover changes the active project without changing section/copy/feature/rail heights, no horizontal overflow, no framework overlay, and no console issues.
- Recent homepage performance polish on 2026-06-02: production investigation found the hero MP4 itself is a 3.1MB Cloudflare-cached asset, but the homepage was loading the MP4, poster, partner banner, project images, and other heavy homepage media at the same time. `index.html` now preloads the hero poster, the hero section uses that poster as a fallback background, desktop video uses `preload="auto"`, mobile uses the separate 540x960 `urblo-hero-mobile.mp4`, and below-the-fold heavy homepage media is deferred until its section is near the viewport.
- Recent Capability Statement web update on 2026-06-01: the homepage proof section still routes to `/capabilities`, but the page is no longer provisional. It now uses the Founder-supplied 2026 Capability Statement as source material, owns its full-bleed hero, leads with a reference-style capability hub for the five service scopes, includes lifecycle support, national reach, Urblo advantage, a selected-project proof ledger, and an email-gated PDF download form. The previously sideways site-review image has been corrected before reuse.
- Recent Supabase planning update on 2026-05-26: the Supabase line is now split into executable tasks: `NOW-SUPABASE-FOUNDATION-001`, `NOW-SUPABASE-SEED-BASELINE-001`, and `NOW-FORMS-BACKEND-001`, with acceptance checks based on Supabase migrations, table listings, RLS policy inspection, and form row creation tests.
- Recent Supabase foundation update on 2026-05-27: `foundation_schema`, `foundation_hardening`, and `anon_read_only` migrations are applied to project `npkidywzwddbnfrnxlmo`; verification confirmed 24 expected tables, RLS enabled on all 24, private lead/admin tables inaccessible to anon, read-only anon grants for public content, and no missing public-schema foreign-key indexes.
- Recent Supabase baseline seed update on 2026-05-27: `baseline_seed` migration is applied and verified with the current Stone Library finish dictionary plus one published default Urblo site settings row.
- Recent forms backend source update on 2026-05-27: Contact form now submits enquiries to `/api/enquiries`; Sample Request mode is available at `/contact?intent=sample-request` and submits to `/api/sample-requests`; footer/sample CTAs now route to the Contact sample-request mode instead of mailto.
- Recent browser QA on 2026-06-02: homepage hero is full viewport at 1440x900 and 390x844; mobile selects `urblo-hero-mobile.mp4` with 540x960 intrinsic video size; desktop selects `urblo-hero.mp4` with 1280x720 intrinsic video size; both reach `readyState=4`.
- Recent video QA on 2026-05-25: `public/media/launch/home/urblo-hero.mp4` was re-encoded from about 16MB to about 3MB while preserving 1280x720 playback; desktop video reached `readyState=4`, and mobile still selected no MP4 source.
- Recent browser QA on 2026-05-25: article detail at 320px, contact at 390px, Products, Our Story, missing project, unknown route, Product detail, and Stone Library detail all reported zero horizontal overflow and no fresh console errors after `react-helmet` removal.
- Recent browser QA on 2026-05-25: clicking the homepage Artisan Park project card from scroll position 1800 navigates to `/projects/artisan-park-yarrabend` with `scrollY=0`.
- Recent browser QA on 2026-05-25: Contact empty submit is blocked with inline copy requiring project notes plus email or phone; Stone Library active imagery discloses finish-specific image source.
- Recent browser QA on 2026-05-25: unknown route, missing product detail, and missing article detail render deliberate route states instead of falling back to Home or blank/error text.
- Recent browser QA on 2026-05-25: Product detail configuration on `/products/primeBlock` renders selected-summary feedback, a prefilled configuration enquiry CTA, and pending-image copy when Harcourt is selected. Browser screenshot/desktop DOM checks passed; mobile viewport fallback was attempted but blocked by local browser tooling.
- Secondary Stone Library frames are implemented for approved Juparana and Zen Grey source files. Runtime gates pass, but fresh desktop/mobile browser visual QA remains blocked until the in-app Browser pane or local Chrome/Playwright is available.
- Product and article public slugs now use lowercase kebab-case with explicit Cloudflare 301 rules and runtime alias redirects for the previous camelCase/title-case URLs.
- Recent browser QA on 2026-05-25: Stone Library list renders 13 results; Golden Crust, Tan Brown, Honey Comb, and Tuscany detail pages render mapped images without `IMAGE COMING SOON`; Golden Crust Dark and Tuscany Cross Cut variant switches update to the correct mapped images.
- Current controlled-media status: P0/P1 visible media has local stopgaps under `public/media/launch`; direct old WordPress `wp-content/uploads` references are removed from runtime data; article media still needs structured-block migration.
- Stone Library shared-drive audit and current-site image mapping on 2026-05-25 are complete for current website stones only; Drive-only products are intentionally out of scope for this launch pass.
- Detailed historical verification evidence lives in `docs/WORKLOG.md`; keep this snapshot limited to current state and latest gates.

## Active Risks
- Cloudflare + Supabase is approved as the launch target, and source implementation is underway. Pages deployment, production custom-domain cutover, basic form persistence, SMTP2GO notification delivery, browser-key private-row proof, and first-admin profile bootstrap are verified; remaining live verification depends on admin credentials/session, Turnstile if that proof is required, and tagged admin QA write approval.
- Clean URL routing is implemented repo-side for Cloudflare Pages, Cloudflare zone read access is verified for `urblo.com.au` in Hunter's account (`077afae2c6f4e77badadf21e49e58eb7`), and the `urblo` Pages project has a successful production deployment on `urblo.pages.dev`. Production custom domains and DNS cutover are complete for apex and `www`; `qa.urblo.com.au` still points to the previous WordPress target for reference. Rollback DNS values are recorded in `docs/CLOUDFLARE_DEPLOYMENT.md`.
- Supabase foundation schema/RLS, baseline seeds, admin settings/profile/helper hardening, admin profile email uniqueness, and media Storage policies are applied and verified. Security advisor currently reports no security lints after moving admin-role helper usage to a non-exposed private schema. Form API source, deployed lead/audit row creation, SMTP2GO notification, browser-key private-row denial, and first-admin profile bootstrap are verified. Admin auth/settings/media source is implemented, but live browser auth/save/upload/export verification still needs an admin login password/session. Stone Library, Projects, Products, Articles, Leads, and Audit source CRUD/workflow/review screens are implemented but live save/export verification remains blocked by the same authenticated admin-session gap. Browser-side admin audit writers, Stone Library finish-image audit actions, and Media/Leads export audit gating are implemented in source; live admin audit row creation through browser-key RLS is not complete yet.
- Stone Library source CRUD is implemented behind the admin gate, including finish capability and finish image link management. Public Stone Library listing/detail now prefer published Supabase rows with static fallback, while imported content remains draft/review-only until an editor publishes it.
- Contact and Sample Request no longer use mailto for the main submit path in source code, and Sample Request source now uses an atomic Supabase RPC for the request/item insert pair. Basic production persistence is verified on Cloudflare Pages; final proof still needs browser-key private-row denial, real email delivery, Turnstile, and admin lead workflow verification.
- `npm run agent:forms-live -- --allow-writes` remains the canonical live form verifier for future runs. The current deployed proof used approved tagged writes plus Supabase connector readback; do not clean up the created QA rows unless Jay explicitly approves cleanup.
- Public Projects, Products, Articles, and the Stone Library listing/detail now prefer published Supabase content with static fallback. Imported production content is intentionally still draft/review-only, so public pages continue showing static content until an editor publishes rows in `/admin`. The main public-read follow-up is now production editor review/publish proof after deployment.
- The static-to-Supabase content import dry run is source-ready, but it is deliberately no-write and now keeps all status-bearing import rows as `draft`. Do not apply imported rows to production Supabase or publish them publicly until Jay approves the import scope and live credentials/admin access are ready.
- `npm run agent:live-readiness` now keeps those content migration gates visible alongside form/admin/Cloudflare blockers so agents do not treat generated `.tmp` import SQL as approved live work.
- The admin shell exists, and Settings/admin profiles/Media/Stone Library/Projects/Products/Articles/Leads/Audit are the first source CRUD/workflow/review surfaces. Live auth, first-admin bootstrap, content import, admin CRUD live verification, and Storage-inclusive proof have passed, but the CMS still needs broader editor-experience productization before it is fully handoff-ready for non-technical users. `NOW-ADMIN-CMS-001` is an umbrella objective; continue improving IA, status language, list/detail editing flow, and publish readiness without changing the non-destructive archive lifecycle.
- Admin readiness has been verified by connector/browser-key equivalent checks for `info@urblo.com.au`; run the packaged browser QA once `URBLO_ADMIN_EMAIL=info@urblo.com.au` and `URBLO_ADMIN_PASSWORD` or `URBLO_ADMIN_ACCESS_TOKEN` are supplied outside chat.
- P0/P1 old WordPress media references in runtime data have been migrated to controlled local assets under `public/media/launch`; article covers and known detail images now have a local runtime stopgap under `public/media/launch/articles`.
- Stone Library current-site shared-drive image mapping is complete for Golden Crust, Tan Brown, Honey Comb, Ivory Sand, and Tuscany. Secondary frames are wired for Juparana and Zen Grey. Blueocean remains on the controlled fallback and Harcourt remains placeholder/TBC because no matching current-site shared-drive source was found.
- Raw article newsletter HTML remains source material and still needs Supabase structured-block migration, claim-safety review, mobile-safe templates, and full editorial cleanup before the article system is considered final.
- Article mobile overflow has a runtime CSS stopgap verified at 320px for the cost-myth article, but raw newsletter HTML remains the wrong long-term authoring model.
- Product detail pages now initialize default material selections and include selected-summary feedback, a geometry-preview disclaimer, separate material previews, a configuration enquiry CTA, and deliberate pending-image states; deeper product data approval remains part of the CMS/content migration work.
- Legacy project detail pages remain weaker than the Moon Gate material-map case study model; the broad migration task is paused by user direction for now.
- The Capability Statement page is now sourced from Natalie Ma's 2026 PDF and can be treated as the current client-supplied capability source. Its current web IA is a service-capability hub plus selected-project ledger, not a PDF-like editorial spread. Base `/api/enquiries` persistence is verified, but the Capability-specific PDF download capture path still needs a live route-specific check.
- Article claim cleanup remains paused by user direction for now; do not change article claim wording until explicitly resumed.
- Public URL style is normalized to lowercase kebab-case across products, articles, projects, and Stone Library. Old product/article URLs are preserved through `public/_redirects` and runtime alias handling.
- App shell default Vite metadata, starter README content, and default social image issues have been replaced; launch polish debt remains around future campaign-specific share imagery only if the client wants a bespoke preview.
- Long-term CMS media hosting is still resolved through the Cloudflare/Supabase launch plan: local launch assets are acceptable stopgaps, while normal editorial/project/product/Stone Library media should move into Supabase media records or an approved Cloudflare media path during CMS migration.
- GitHub Pages deployment hardening is a legacy fallback only; Cloudflare Pages is the active production target and production custom-domain cutover is complete.
- Short-term GitHub Pages preview routing now generates `404.html` from `index.html` during deploy so direct clean-route visits can load the React app. Live deep-link rendering was confirmed on `/stone-library/angola-black`, though GitHub Pages still reports HTTP 404 for fallback-served routes. This is a compatibility patch only; Cloudflare Pages remains the production launch target and should use `public/_redirects`.
- Route-level code splitting resolved the previous bundle size warning; future admin/CMS additions should keep chunk output under review.
- Moon Gate material/application notes are intentionally MVP-inferred from supplied imagery and public project context; designer confirmation is still needed before final production claims.
- Other project pages still have legacy-level content and need migration into the material-map model.
- Homepage video is now a controlled, optimized static launch asset. Live Cloudflare preview should still verify actual LCP/network behavior after deployment, and Cloudflare Stream/R2 remains optional for adaptive video management.

## Next Recommended Action
Continue launch work by deciding whether Turnstile proof and optional Storage upload proof are required before launch, then proceed with approved content import/public read cutover planning. Production domain cutover, SMTP2GO email proof, browser-key private-row proof, first-admin bootstrap, active-admin browser QA, project media block migration, and non-Storage admin CRUD/live lead workflow QA are complete; keep rollback values in `docs/CLOUDFLARE_DEPLOYMENT.md` intact.

## Guardrails
- Use repo-root relative paths in committed docs.
- Keep current state short here; write detailed history in `docs/WORKLOG.md`.
- Do not use `docs/NEXT_STEPS.md` as the machine task queue; update `docs/agent/tasks.json` for task state.

# NEXT_STEPS - Urblo Roadmap

Last updated: 2026-06-04

## Purpose
This file is the human-readable roadmap. The machine-readable task queue lives in `docs/agent/tasks.json` and is the source of truth for active task status, file ownership, acceptance criteria, and verification commands.

Use this file to understand priority shape. Use `docs/agent/tasks.json` to execute.

## Current Objective
Move Urblo from a static website toward a launchable Cloudflare + Supabase operating system: public site hosting, real forms, customer-maintained content, controlled media, and a durable admin workflow.

## Blocking Quality Gate Policy
A task touching runtime behavior is not complete unless all three pass:
- `npm run build`
- `npm run lint`
- `npx tsc -b`

Docs-only and harness-only work should run:
- `npm run agent:check`
- `git diff --check`

Cloudflare/Supabase implementation work should also follow the new verification profiles in `docs/agent/verification.md`.

## Last Runtime Baseline
Measured 2026-06-03 during the homepage section-order/nav/video checkpoint:
- `npm run build`: pass
- `npm run lint`: pass
- `npx tsc -b`: pass
- `npm run agent:smoke`: pass

## Advisory Brand + Design Gate
For any user-facing layout/copy/IA task:
- Reference relevant sections in `docs/brand-baseline.md`.
- Reference relevant sections in `docs/DESIGN.md`.
- Include a short brand/design alignment note in task delivery.
- If implementation cannot satisfy baseline yet, record explicit gap and follow-up ID.

## Now
Source of truth: `docs/agent/tasks.json`.

- `NOW-FORMS-BACKEND-001`: basic deployed live persistence, SMTP2GO notification delivery, and browser-key private-row denial are verified for Cloudflare Pages Functions. Cloudflare Pages production has `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, SMTP2GO notification variables, and the browser-safe Supabase publishable key configured for the `urblo` project. Redeploy `7d10ba13-5b9f-4d6e-86b9-e28218978189` for commit `7100bba` is successful, no-write deployed API smoke still passes, valid tagged Contact/Sample Request submissions created `enquiries.id = 1`, `sample_requests.id = 1`, `sample_request_items.id = 1`, and `admin_audit_events.id = 1/2`, invalid tagged payloads created zero lead/audit rows, SMTP2GO proof against `https://urblo.com.au` created `enquiries.id = 3`, `sample_requests.id = 2`, `sample_request_items.id = 2`, and `admin_audit_events.id = 4/5` with both stored lead rows at `notification_status = sent`, and browser-key boundary proof created `enquiries.id = 4`, `sample_requests.id = 3`, `sample_request_items.id = 3`, and `admin_audit_events.id = 6/7` while anonymous REST reads through the deployed publishable key returned HTTP 401 for all three private tables. Remaining final form proof is Turnstile after the public site key, server secret, and valid token are available.
- `/capabilities` now uses Natalie Ma's 2026 Capability Statement as the content source and presents it as a service-style capability hub with five concrete capability scopes, lifecycle/reach support, selected-project proof, and an email-gated PDF download form that posts to `/api/enquiries` as `Capability statement download`. `npm run agent:capabilities-ui` guards the source contract; base `/api/enquiries` persistence is verified, but the Capability-specific download capture path still needs a route-specific live check.
- Forms API source checks now also exercise the Cloudflare Pages Function wrappers for method behavior: GET is rejected, OPTIONS returns CORS preflight headers, malformed JSON returns `400 invalid_json`, and invalid Sample Request POSTs are rejected before Supabase calls.
- Cloudflare Pages production deployment is complete: repo-side readiness is checkable with `npm run agent:cloudflare-readiness`, deployed route/asset/redirect/API safe-failure smoke passes against `https://urblo.pages.dev`, `https://urblo.com.au`, and `https://www.urblo.com.au`, production custom domains are attached, apex and `www` DNS point to `urblo.pages.dev`, Google MX/SPF/TXT and `qa.urblo.com.au` were not changed, and rollback DNS values are recorded in `docs/CLOUDFLARE_DEPLOYMENT.md`. `NOW-CLOUDFLARE-PAGES-DEPLOY-001` is now complete in `docs/agent/tasks.json`; remaining live form/admin proofs stay in their own tasks.
- Cloudflare deployed-preview route checklist now treats canonical `/products/prime-block`, article detail, and `/capabilities` as direct-refresh checks; old product/article URLs remain redirect checks only.
- Production dependency audit: `react-router-dom`, `swiper`, and `postcss` have been upgraded, `tailwindcss/nesting` is configured before Tailwind for Swiper 12 CSS, and `npm audit --omit=dev --audit-level=critical` now reports zero vulnerabilities. Keep the homepage carousel and admin config gate in the verification set when touching this dependency group.
- `NOW-FORMS-SUPABASE-001`: base Supabase-backed Contact and Sample Request persistence, SMTP2GO notification acceptance, and browser-key private-row denial are verified; complete Turnstile proof and admin-visible lead workflow.
- `NEXT-FORMS-EMAIL-NOTIFY-001`: complete for the current provider path. SMTP2GO is selected, DNS records and Cloudflare notification variables are configured, source support is deployed, and approved live proof stored `notification_status = sent` for tagged Contact and Sample Request rows.
- `NOW-ADMIN-AUTH-RLS-001`: browser-safe Supabase config, first-admin profile, and no-write active-admin browser QA are now live for `info@urblo.com.au`. Source shell and config-gated routes are implemented; built-site Firefox QA proved all launch-critical admin routes render the configuration-required gate without browser-safe Supabase env, shell-only publishable-key Playwright proved unauthenticated protected routes show the login form and hide private content, 2026-06-03 connector/browser-key checks confirmed `info@urblo.com.au` is a confirmed Auth user linked to one active `owner` profile with public seed rows readable and `admin_profiles` anonymously denied, and 2026-06-04 production browser QA verified all 9 authenticated admin route shells plus Sign out with no content writes.
- First-admin/admin readiness email matching now follows that database contract: `scripts/bootstrap-first-admin.mjs` and `scripts/check-admin-live-readiness.mjs` normalize `admin_profiles.email` before matching the supplied first-admin email, so mixed-case profile emails do not create false missing-profile results.
- Admin login next-target handling now accepts only true admin-console targets and avoids login/unauthorized loops; `npm run agent:admin-crud-coverage` guards that source contract until live authenticated browser QA is available.
- Configured admin browser QA: `npm run agent:admin-auth-browser` is now available as a gated no-write login check. It reads `.env.local`, `.env`, `.dev.vars`, and shell values without printing secrets. Default mode is plan-only; `--allow-login --strict` requires browser-safe Supabase config plus `URBLO_ADMIN_EMAIL`/`URBLO_ADMIN_PASSWORD`, signs in through `/admin/login`, verifies authenticated admin route shells, and verifies Sign out returns the protected route to `/admin/login?next=...` without content writes. `--allow-login --expect-unauthorized --strict` uses `URBLO_UNPROFILED_EMAIL`/`URBLO_UNPROFILED_PASSWORD` to prove a valid Auth user without an active `admin_profiles` row reaches `/admin/unauthorized`, then probes all launch-critical admin routes and keeps them unauthorized without private admin module content.
- First-admin bootstrap is complete for `info@urblo.com.au`: active `owner` profile exists for Auth user `74b9e1d1-5f29-482c-836e-4feec8cd0087`, and `admin_audit_events.id = 8` records `admin_profile.bootstrap`.
- `NOW-ADMIN-SETTINGS-CRUD-001`: complete live `/admin/settings` and CMS team-access save verification after Jay approves tagged admin QA writes. Source form, footer column/item editor, editor-facing CMS team access manager, Access setup checklist, Invite and grant access, protected `/api/admin/invite-user` Function with existing-access preflight before email send, Grant existing login fallback, Copy setup code controls, Website owner / CMS manager role labels, Active access wording, owner/admin RLS hardening, owner-role protection, and SECURITY DEFINER helper hardening are implemented; security advisor currently reports zero security lints. Remaining proof is deployment plus a live invite QA run that confirms the invite email, new profile row, and Change history entry.
- `NOW-ADMIN-MEDIA-LEADS-001`: continue after the Media publish-checklist checkpoint. Supabase Storage buckets/policies, `/admin/media`, and `/admin/leads` source are implemented; Media and Leads now have change-history-gated CSV export paths for currently loaded/visible records. Media now uses editor-facing private/public library labels, File or link type and Website visibility location guidance, safer list fallback titles that avoid raw uploaded-file paths, and locks Publish behind source, public-location, alt-text, and usage-note readiness. Leads now show editor-facing website-source labels, friendly email-delivery/spam-check badges, status-specific Recommended next step guidance in the workflow editor, Workflow rules, team-role labels, sample-item Stone not selected / Finish not selected labels, and a filtered visible-queue export with editor-facing CSV headers plus Reference values that matches current search/status/kind filters. Basic live lead creation, active-admin login, lead workflow save/export, non-Storage media CRUD/export, and the approved private Storage upload/readback/anonymous-denial proof are verified.
- `NOW-ADMIN-CONTENT-CRUD-001`: continue after the Stone Library publish-checklist checkpoint. `/admin/stone-library`, `/admin/projects`, `/admin/products`, and `/admin/articles` source CRUD are implemented; Stone Library now includes family/variant Publish checklists, editor-facing finish availability labels, group/variant visibility help for Draft / Needs confirmation / Published / Archived, finish image links to Media library items with selected-media previews, and a publish lock that directs editors to publish the chosen Media library item first. Projects now surfaces a project-editor Publish checklist before publication with actionable blockers for title, URL, public copy, project proof review, fact proof review, and material proof review, explains Needs review / Approved for public use / Deferred or keep private in editor language, uses Project case studies / Editing access / Website URL key / Structured detail / Media library item / Nothing added yet wording instead of record, row, slug, JSON, and raw media ID language, and replaces raw media ID entry with Media library pickers/previews that show Published/Draft/Archived in Media for project cover/hero, material image, media block asset, material map image, and hotspot preview image. Products now uses Website URL key / Model website key labels, URL-key list/search labels, product/model status help, Media library item pickers/previews without `#id` fallback labels, Search title/description fields instead of product SEO JSON, Material reference wording for material defaults, a Product Publish checklist, a Model Publish checklist, and Stone Library status feedback for material defaults. Articles now uses Website URL key, URL/date list labels, article status help, Migration note/link labels, Article sections language, cover/section Media library item selectors without `#id` fallback labels, Search title/description fields instead of article SEO JSON, and block-type-specific section forms. `project_media_blocks` is applied, live CRUD verification passed, the reviewed static content payload has been imported into production Supabase as draft rows, and public Projects/Products/Articles plus the Stone Library listing/detail now prefer published Supabase reads with static fallback. Remaining work is customer/editor review-publish proof and production walkthrough after deployment.
- `NOW-ADMIN-CMS-001`: umbrella objective for customer-maintained Projects, Stone Library, Products, Articles, media, leads, and audit visibility; execute through the smaller admin child tasks. Shared browser-side audit writers now exist for admin CRUD/workflow saves, and `npm run agent:admin-crud-live` is staged for live tagged QA writes, dashboard-health predicate readback, tagged public-content/private-lead invisibility checks, and optional Storage privacy proof once browser-safe Supabase config, a real owner/admin session, and Jay approval are available.
- Admin editor handoff guide: `docs/ADMIN_EDITOR_GUIDE.md` now documents the production admin address, Website owner / CMS manager / Editor / Viewer roles, account setup, editor workflow, publish rules, module coverage, current public fallback boundaries, imported draft-content behavior, and remaining handoff gaps including push/deploy plus final production walkthroughs. `npm run agent:check` guards those current handoff terms and rejects older Supabase/Auth/Profile/Owner/Admin guide wording so the guide cannot silently drift while final CMS gaps close.
- `/admin` dashboard now acts as the editor start page: it shows Start here quick actions for Leads, Publish content, and Media; content status rows link to the correct editor modules including Stone Library; module cards use editor-facing handoff labels instead of Supabase/RLS dependency language; health labels use editor-facing wording; and handoff guidance is about creating CMS access and walking through real content rather than old launch secret setup. `npm run agent:admin-crud-coverage` guards this dashboard source contract until live admin browser/data QA can run.
- Projects, Stone Library, Products, and Articles editor headers now include an Open public page control that is active only for Published records; Draft, Needs confirmation, and Archived records show why the public page remains hidden. Dashboard handoff guidance now tells editors to use that link after publishing instead of referring to stale public fallback gaps.
- Articles now include an Article publish checklist and locked Publish button for title, website URL, published date, excerpt, at least one Published article section, and published-section content readiness, so editors see blockers before attempting to publish.
- Admin removal model: launch-critical CMS verification should use create/update/publish/archive flows, not physical deletes. Physical delete controls remain approval-gated until Jay approves a retention/destructive-delete policy.
- Admin source/browser coverage: `npm run agent:admin-crud-coverage` now verifies `/admin` route/module registration, protected shell coverage, expected Supabase table references, role-gated controls, validation/save state paths, publish/archive lifecycle paths, structured Article block authoring guardrails, audit writer actions, Media/Leads export audit gates, the non-destructive archive/removal contract, destructive delete/RPC/control absence, browser-source service-role env/client boundaries, browser-key/RLS admin live verifier boundaries, exact admin live audit action coverage, and the config-missing admin source contract without mutating Supabase. `npm run agent:admin-config-gate` is the repeatable built-site Firefox check for all launch-critical admin routes when no browser-safe Supabase key is configured.
- Live input readiness: `npm run agent:live-readiness` reports whether the required form, email, Turnstile, first-admin, no-write active-admin browser QA, no-write unprofiled unauthorized browser QA, admin-write, admin Storage upload, guarded content import, public read cutover, and Cloudflare preview live verification inputs are present, without printing secrets or mutating Supabase/Cloudflare. Non-secret manual inputs can be passed with `--base-url`, `--admin-email`, `--form-writes-approved`, `--first-admin-writes-approved`, `--admin-writes-approved`, `--content-import-approved`, `--content-merge-approved`, `--content-public-cutover-approved`, and `--turnstile-token-provided`; copied placeholders or malformed `--base-url`/`--admin-email` values stay missing, and `--strict` should be used only when missing/manual-gated live inputs should fail the command.
- Live verifier input boundary: `npm run agent:forms-live`, `npm run agent:cloudflare-preview-smoke`, `npm run agent:admin-auth-browser`, and `npm run agent:admin-config-gate` now reject copied placeholders or URLs with path/query/hash in `--base-url` before network or live-write work starts. `npm run agent:admin-live-readiness`, active-admin/unprofiled browser QA, and admin CRUD live writes now require real email-shaped inputs instead of copied placeholders.
- Harness command integrity: `npm run agent:check` now guards the active operational `agent:*` package script map for form, admin, Cloudflare, first-admin, live-readiness, content-import, and public Supabase readiness runners, so the launch verification commands cannot be silently removed from `package.json`.
- Harness live-readiness docs integrity: `npm run agent:check` now also guards the live-readiness flag list in `docs/ARCHITECTURE.md`, `docs/CLOUDFLARE_DEPLOYMENT.md`, and `docs/agent/verification.md` so content import/merge/cutover approval flags and the Turnstile token readiness flag do not drift from the runner.
- Admin live write verification: `npm run agent:admin-crud-live` now prints a no-write plan by default. Use `npm run agent:admin-crud-live -- --allow-writes` only after credentials/session exist and tagged QA writes are approved; live password login uses `URBLO_ADMIN_EMAIL`/`URBLO_ADMIN_PASSWORD` or an explicit `URBLO_ADMIN_ACCESS_TOKEN`, while `URBLO_FIRST_ADMIN_EMAIL` stays reserved for bootstrap/readiness. The verifier will also verify the exact expected tagged audit action/entity set, dashboard-health predicates for tagged QA rows, and tagged public-content/private-lead invisibility. Use `npm run agent:admin-crud-live -- --allow-writes --include-storage` for the final private Storage upload, signed-in admin readback, and anonymous-read denial proof.
- Content import preparation and live draft import: `npm run agent:content-import` dry-runs the current static Stone Library groups, variants, finish capabilities, finish image links, Products, Projects, Articles, and media candidates into Supabase-shaped draft payloads. `npm run agent:content-import:live` is plan-only by default and, with `--allow-writes`, imports/upserts the reviewed payload through normal browser-key owner/admin RLS while keeping rows draft. The approved 2026-06-04 run wrote/upserted 115 media assets, 13 stone groups, 6 products, 5 projects, 4 articles, and dependent rows, and verified anonymous browser-key reads expose zero imported draft parent rows.
- Latest read-only Supabase connector grant preflight confirms current Data API role grants are present for the launch schema: public content tables have anon `select`, private/admin/lead tables deny anon `select`, anon writes are denied on all 24 launch tables, authenticated and service-role CRUD grants exist on all 24 launch tables, and generated sequences grant usage to authenticated/service-role.
- Supabase foundation source readiness: `npm run agent:supabase-foundation-readiness` now verifies the no-secret local migration/source contract for the 12 expected migrations, 24 launch tables, RLS source, anonymous read-only posture, baseline seeds, service-role-only Sample Request atomic RPC, Storage bucket/listing hardening, private helper grants, and admin profile email uniqueness. It is wired into `npm run agent:check` so the foundation contract cannot silently drift while live credentials are still pending.
- Public Supabase cutover readiness: `npm run agent:public-supabase-readiness` verifies the no-write import payload stays draft-only, structured article blocks do not regress to placeholder/newsletter-artifact imports, generated guarded draft apply SQL keeps import and merge approval manual and avoids destructive/publish/status-drift statements, generated rollback SQL keeps its destructive approval gate manual and follows reverse dependency order, public RLS policy source remains published-only, anonymous grants stay read-only, public runtime uses browser-key Supabase published reads with static fallback, and Cloudflare Functions stay scoped to `/api/*`.
- `NOW-ARTICLE-STRUCTURE-CLAIMS-001`: move article details from raw newsletter HTML to mobile-safe, claim-reviewed structured article templates.

## Next
- `NEXT-UI-PARITY-001`: bring Home, Our Story, Articles, and Contact Us toward approved visual references.
- `NEXT-STONELIB-DATA-001`: replace generic finish behavior text with approved notes.
- `NEXT-PROJECTS-INTAKE-001`: define the project intake template and migrate the next project into the material-map case study model.

## Later
- `LATER-BRAND-001`: align homepage modules with brand pillars and proof framing.
- `LATER-LAUNCH-DOCS-CONSOLIDATE-001`: after production launch, split the temporary launch plan into durable architecture, operations, and worklog docs.
- `NEXT-SAMPLE-REQUEST-001`: legacy fallback only if Supabase-backed forms are not implemented.

## Blocked
- `NOW-ADMIN-AUTH-RLS-001` live verification: protected admin shell source exists, configured-key unauthenticated redirect/login gate has been verified, production browser-safe Supabase key is configured, first admin profile exists, active-admin browser QA passed on 2026-06-04, and tagged admin write verification passed for the non-Storage path. Optional Storage upload proof and unprofiled unauthorized browser QA remain separate checks.
- `NOW-ADMIN-CONTENT-CRUD-001`: imported content is draft/review-only and public runtime now reads only published Supabase rows with static fallback. Remaining follow-up is editor review/publish proof plus production walkthrough after deployment.
- `NOW-ADMIN-MEDIA-LEADS-001` live media verification and lead management: media/leads source exists, save/export/workflow proof passed, and private Storage upload proof passed. Turnstile verification remains the separate final form proof.

## Completed This Cycle
- `DONE-DOCS-HARNESS-ROOT-001`: promoted `docs/README_AGENT.md` to root `AGENTS.md`, added `docs/DESIGN.md`, moved repo docs to relative paths, and separated brand authority from design execution authority.
- Phase 1 harness hygiene committed: `docs/HANDOFF.md`, `docs/agent/tasks.json`, `docs/agent/verification.md`, `scripts/check-doc-paths.mjs`, and `scripts/check-harness.mjs`.
- Phase 2 verification harness completed: `scripts/agent-init.sh`, `scripts/agent-smoke.sh`, and package-level `agent:*` scripts.
- `NEXT-DATA-001`: project list/detail metadata now comes from `src/data/projectData.ts`; Moon Gate is the first material-map case study.
- `NOW-CLOUDFLARE-SUPABASE-ARCH-001`: Cloudflare Pages + Supabase launch architecture, cost model, and customer-facing approval PDF are documented.
- `NOW-SUPABASE-SCHEMA-001`: Supabase schema plan is documented for Projects, Stone Library, Products, Articles, media, admin access, and lead capture.
- `NOW-SUPABASE-FOUNDATION-001`: Supabase foundation migrations are applied and verified for the Urblo project: 24 launch tables, RLS, policies, FK indexes, private lead/admin protection, and read-only anonymous public-content grants.
- `NOW-SUPABASE-SEED-BASELINE-001`: Supabase baseline seed is applied and verified: 12 distinct finish definitions and one published default Urblo site settings row, with idempotent upsert behavior.
- `NOW-FORMS-BACKEND-001` partial source implementation: `/api/enquiries` and `/api/sample-requests` Pages Function source exists, Contact page posts to those endpoints, sample CTAs route to `/contact?intent=sample-request`, and mock API checks cover valid/invalid/Turnstile-failure behavior, server-side audit attempts, audit-failure resilience, missing-service-role fail-closed behavior, and SMTP2GO/Resend notification paths. Basic deployed Supabase row/audit verification passed on 2026-06-02, and SMTP2GO notification plus browser-key private-row proof passed on 2026-06-03; this task remains open only for final Turnstile and admin-visible workflow proof.
- `NOW-FORMS-BACKEND-001` notification mock checkpoint: `scripts/check-forms-api.mjs` now proves configured SMTP2GO and Resend notification success/failure handling without real email secrets, including pending initial status, provider call shape, and final `notification_status` patch to `sent` or `failed`.
- `NOW-FORMS-BACKEND-001` live verification runner checkpoint: `npm run agent:forms-live -- --allow-writes` now verifies valid enquiry/sample request rows, sample item rows, server-side audit rows, invalid-payload no-write behavior, and response-vs-stored `notification_status` consistency once a service-role key is configured and Jay approves tagged live form QA writes. With `--require-browser-boundary`, it also requires a browser-safe key and proves created private lead rows are not anonymously readable. The runner intentionally fails without `--allow-writes` or required credentials and retains tagged test rows until Jay approves cleanup.
- `NOW-FORMS-BACKEND-001` Contact form UI source contract checkpoint: `npm run agent:forms-ui` now verifies the Contact form source keeps the main submit flow on `/api/enquiries` and `/api/sample-requests`, preserves inline validation/success/error/submitting states and sample-request fields, keeps direct email/phone fallback channels, and blocks submit-flow mailto/window-navigation regressions. `npm run agent:smoke` runs this after the Forms API mock checks.
- `NOW-FORMS-BACKEND-001` Sample Request atomic insert checkpoint: Supabase migration `sample_request_atomic_insert` is applied; `/api/sample-requests` now uses service-role-only `submit_sample_request_with_item(jsonb, jsonb)` so the request row and first item row are created transactionally. Source/mock checks fail if the handler regresses to separate direct table inserts.
- `NEXT-ROUTER-SEO-001`: clean Cloudflare Pages routing is implemented with `BrowserRouter`, root Vite base, and SPA fallback.
- Asset migration stopgap: direct old WordPress media references have been removed from runtime data and replaced with controlled local assets under `public/media/launch`; article covers and known article detail images now use a local runtime cleanup layer, while CMS media records and structured article blocks remain open under the article/admin tracks.
- Homepage video launch optimization: the controlled desktop MP4 is now about 3MB, H.264 1280x720, no-audio, fast-start media; the controlled mobile MP4 is about 1.1MB, H.264 540x960, no-audio, fast-start media; `index.html` preloads the hero poster, desktop/mobile use media-specific video sources, and heavy non-hero homepage media is deferred until the relevant section nears the viewport.
- `NOW-DELIVERY-READINESS-001`: removed Vite starter README content and deleted the unused React starter SVG asset.
- `NOW-ASSET-STRATEGY-001`: interim local stopgap and delivery-phase Supabase/Cloudflare media hosting policy are documented.
- `NOW-STONELIB-IMG-FASTTRACK-001`: provided primary finish assets are mapped, controlled fallback usage is documented, and remaining true missing image groups are recorded for full coverage.
- `NOW-SEO-DELIVERY-001`: default metadata/icons/social image are launch-owned, Open Graph/Twitter image now uses a PNG, and high-risk article excerpt/runtime newsletter claims are qualified.
- `LATER-PERF-001`: route-level lazy loading is implemented, initial app shell JS is reduced, and the previous `>500kB` JavaScript chunk warning is resolved.
- Homepage initial media-load competition is reduced: local production-preview resource timing on 2026-06-02 shows the initial hero-load set limited to the poster, app/home chunks, and `urblo-hero.mp4`, rather than also loading the partner banner and Latest Projects images during first paint.
- `LATER-QA-001`: `npm run agent:smoke` now verifies key route shells, article index availability, and critical CTA targets with actionable names.
- `DONE-HOMEPAGE-STONE-SHOWCASE-REMOVE-001`: homepage Browse by stone type section and unused local showcase images were removed by request; article/mobile and product/detail polish discovered during the full-site QA pass is tracked in the active queue.
- `NEXT-STONELIB-DRIVE-IMAGE-AUDIT-001`: completed current-website-only audit of the Saistone Google Drive `Urblo Digital Stone Library` source. Drive-only products are excluded for now.
- `NEXT-STONELIB-IMG-001`: normalized and mapped current-site shared-drive images for Golden Crust, Tan Brown, Honey Comb, Ivory Sand, and Tuscany. Blueocean remains a controlled fallback and Harcourt remains placeholder/TBC because no matching current-site source imagery was found.
- `NEXT-ADMIN-IA-ACCESS-001`: defined the executable `/admin` route map, access states, role behavior, module rollout order, content field ownership model, and implementation boundaries in `docs/ADMIN_IA_ACCESS.md`.
- `NOW-ROUTE-ERROR-STATES-001`: unknown URLs now render a branded not-found state, and article/product detail routes now have deliberate loading, not-found, and load-error states.
- `NEXT-PRODUCT-DETAIL-CONVERSION-001`: product details now show selected configuration feedback, a prefilled discussion CTA, specification caveats, and pending-image treatment for missing selector imagery.
- `NEXT-STONELIB-IMG-002`: secondary finish frames are implemented for approved Juparana and Zen Grey source frames as active-finish support media with lightbox frame selection.
- Homepage Latest Projects sketch-ratio pass: the section now follows the approved two-row/four-column composition, uses optional `featureImage` data for the upper two-column image, keeps lower project images as fixed-height portrait slots, and preserves one-viewport height plus stable hover/tap regions across desktop and mobile QA.
- `NEXT-SLUG-URL-NORMALIZE-001`: product and article public slugs now use lowercase kebab-case with explicit Cloudflare 301 redirects and runtime alias handling for old URLs.
- `NEXT-MOTION-POLISH-001`: homepage and Our Story proof metrics now visibly count up from zero when scrolled into view, and public route changes use restrained reduced-motion-aware enter transitions.
- `NEXT-PAGE-TITLE-TYPOGRAPHY-001`: Projects page title typography is now the global public page H1 system, including Article detail.
- `NEXT-STONELIB-IMAGE-LABEL-READABILITY-001`: Stone Library list/detail image overlay labels now use readable dark translucent surfaces with restrained Urblo lime status/action accents.
- `NEXT-STONELIB-STATUS-PILL-CONSISTENCY-001`: Stone Library external Available/Upcoming/No states now use shared lightweight status pills instead of heavy black blocks or full lime badge fills.
- `NEXT-HOME-HERO-EDGE-REVEAL-001`: homepage first viewport now uses edge-aligned header/hero gutters and a sequential all-caps `DESIGN`, `SOURCE`, `DELIVER.` hero title, with letter-by-letter reveal motion, the second line offset, and a single Urblo-green punctuation mark only on the final `DELIVER.` line.
- `NOW-DEPLOY-PAGES-HARDEN-001`: GitHub Pages preview deploy now generates `404.html` from `index.html` so direct clean-route visits can load the React app until Cloudflare Pages goes live.
- `NOW-CLOUDFLARE-PAGES-DEPLOY-001`: Cloudflare Pages production deployment, custom domains, DNS cutover, Function routing scope, route/asset/redirect/API smoke, and rollback documentation are complete. Future checks should use `npm run agent:cloudflare-readiness` and `npm run agent:cloudflare-preview-smoke -- --base-url <origin>` when deployment contracts change.
- `NOW-ASSET-MIGRATION-001`: launch-critical media no longer depends on old WordPress URLs. Identity assets, route banners, Contact imagery, homepage poster/video, priority project/Stone Library imagery, article covers, and known article runtime media cleanup use controlled local launch paths; longer-term CMS media records and optional R2/Stream decisions remain under admin media/content follow-ups.
- `NEXT-UI-PARITY-001` partial launch polish: homepage hero now fills the first viewport, route transitions reset to top, no-banner error/loading states clear the header, article detail no longer double-renders a route banner, article mobile overflow has a CSS stopgap, Contact blocks empty email drafts, product renders are labeled as geometry previews with separate material previews, Stone Library images disclose finish-specific/reference status, and `react-helmet` was removed to clear React 19 strict-mode console noise.
- `NEXT-UI-PARITY-001` homepage proof update: the old rendered sustainability/tabbed feature module was removed from the homepage flow by request; proof metrics now sit directly after the hero with approved stone/city copy and updated project, CO2, landscape architect, and delivered-stone metrics.
- `NEXT-UI-PARITY-001` homepage partner banner image update: the `Design-led stone solutions for streetscapes & civil landscapes.` banner now uses the West Side Place aerial project image at `public/media/launch/homepage/partner-banner-west-side-place.jpg`.
- `NEXT-UI-PARITY-001` homepage Latest Projects update: the section now uses `The work speaks.` with a selected-project detail/image panel and a draggable five-project rail that shows four projects on desktop; thumbnail hover/focus/tap changes the active project, while the upper `View project` CTA handles route navigation. The section is now constrained to one viewport (`100svh`) with fixed active copy/image/rail heights across project selection, plus short-screen desktop/mobile simplification rules to avoid overlap or clipped controls.
- `NEXT-UI-PARITY-001` homepage section-order/nav/video update: the `Design-led` partner banner is now a slimmer transition band, Latest Projects sits directly beneath it before Product Showcase, the bottom Play modal uses a lazy `youtube-nocookie` embed for `UfRtQZSi7cM`, and desktop header moves Articles/Products behind the right-side hamburger while mobile keeps a full-menu hamburger. The desktop primary nav and hamburger must remain one right-aligned group, not a centered nav column.
- `NEXT-UI-PARITY-001` Capability Statement page: the homepage proof section still includes a lightweight `Our Capabilities` CTA, and `/capabilities` now renders a Founder-sourced web version of the 2026 Capability Statement as a reference-style capability hub with five service scopes, lifecycle/reach support, selected-project proof ledger, corrected site-review imagery, and an email-gated PDF download.
- `NEXT-UI-PARITY-001` Projects archive/detail update: `/projects` now uses a functional reference-style archive with breadcrumb, large title, proof intro, project count, sector filters, grid/list controls, and equal-sized project images. `/projects/:slug` now uses a full-width case-study rhythm with oversized opening, previous/next navigation, hero, Project Information facts, ordered media blocks, hotspot inspector support, Featured Materials, and shared CTA. Current project data has no client-approved YouTube video configured, but the renderer/source model supports one optional YouTube block per project.
- `NOW-ADMIN-AUTH-RLS-001` partial source implementation: `/admin`, `/admin/login`, `/admin/unauthorized`, protected admin module routes, Supabase Auth session/profile checks, config-required state, dashboard query shell, and current launch-check copy are implemented. Live active-admin verification remains pending first admin email/profile and browser-safe Supabase key configuration.
- `NOW-ADMIN-AUTH-RLS-001` no-config route gate checkpoint: built-site Playwright Firefox checks now cover `/admin`, `/admin/login`, `/admin/unauthorized`, `/admin/leads`, `/admin/media`, `/admin/settings`, `/admin/stone-library`, `/admin/projects`, `/admin/products`, `/admin/articles`, and `/admin/audit`, each rendering `Configuration required` without browser-safe Supabase env.
- Repeatable admin config-gate browser checkpoint: `npm run agent:admin-config-gate` now starts built Vite preview, runs Firefox checks against every launch-critical admin route, rejects private admin/module text, and writes ignored screenshots under `.tmp/admin-config-gate/screenshots`.
- Admin auth browser QA runner checkpoint: `npm run agent:admin-auth-browser` now prints a no-login plan by default and can run a no-write authenticated browser route check with `--allow-login --strict` after browser-safe Supabase config and a real active admin email/password are available.
- Admin auth browser env-loading checkpoint: `npm run agent:admin-auth-browser` now reads untracked local env files plus shell values, supports extra `--env-file`, prints only variable names/sources, and treats `VITE_SUPABASE_URL` as optional because the Urblo project URL is the browser-client default.
- Admin auth browser unauthorized-profile checkpoint: `npm run agent:admin-auth-browser -- --allow-login --expect-unauthorized --strict` is staged for a valid Auth user without an active `admin_profiles` row. Plan-only mode reports `URBLO_UNPROFILED_EMAIL` and `URBLO_UNPROFILED_PASSWORD`; live mode must prove `/admin/unauthorized`, all launch-critical admin route probes that remain unauthorized, and no private admin module content without creating rows.
- `NOW-ADMIN-AUTH-RLS-001` admin readiness runner checkpoint: `npm run agent:admin-live-readiness` now verifies browser-safe key presence, service-role verification access, the named active first-admin profile, baseline seed rows, and browser-key anonymous public/private REST boundary without creating users or mutating Supabase.
- First-admin bootstrap runner checkpoint: `npm run agent:first-admin-bootstrap` now prints the approved no-write setup path, supports service-role read-only verification that checks the planned role, and gates live invite/profile writes behind Jay approval plus `--allow-writes` and a matching `--confirm-email`.
- `NOW-ADMIN-SETTINGS-CRUD-001` partial source implementation: `/admin/settings` reads/creates/updates the default site settings row behind the admin gate, and Supabase `site_settings` write policies now require owner/admin. Live save verification remains pending first admin email/profile and browser-safe Supabase key configuration.
- `NOW-ADMIN-MEDIA-LEADS-001` partial source implementation: Supabase Storage buckets `urblo-public-media` and `urblo-admin-media` are applied with admin/editor write policies and public object listing disabled; `/admin/media` supports upload-backed draft records, external media records, metadata editing, audit-gated visible manifest export, and publish/archive guardrails behind the admin gate. Live upload/save/export verification remains pending first admin email/profile and browser-safe Supabase key configuration.
- `NOW-ADMIN-MEDIA-LEADS-001` Media UX baseline checkpoint: `/admin/media` now uses shared Draft/Published/Archived status language, searchable/filterable asset lists, status counts, a visible website-visibility rule, editor-facing private/public library labels, File or link type / Website visibility location guidance, safer fallback asset names, and a Media Publish checklist that locks Publish until source, public location, image alt text, and usage notes are ready.
- `NOW-ADMIN-CONTENT-CRUD-001` Stone Library source checkpoint: `/admin/stone-library` now supports Stone Library group, variant, finish capability, and finish image link CRUD behind the admin gate, with loading, empty, validation, save, publish/archive, image-link, published-media, read-only, and error states. Public Stone Library listing/detail now prefer Published Supabase rows with static fallback.
- `NOW-ADMIN-CONTENT-CRUD-001` Stone Library UX baseline checkpoint: `/admin/stone-library` now has group search/status filtering, treats Needs confirmation as a visible editor state counted with Draft because it is not public-ready, uses editor-facing Available/Needs confirmation/Not available finish labels, shows group/variant status help for public visibility, labels proof/pricing notes in editor language, and locks family/variant Publish behind checklists for URL key, public summary, variant presence, and reviewed finish availability.
- `NOW-ADMIN-CONTENT-CRUD-001` Projects source checkpoint: `/admin/projects` now supports project records, facts, material schedule rows, ordered media blocks, material maps, and draggable hotspots behind the admin gate, with loading, empty, validation, save, publish/archive, claim-review, media block type, read-only, and error states. `supabase/migrations/20260603142359_project_media_blocks.sql` is applied and verified for live `project_media` block fields/constraints. Public Project runtime remains static/file-backed until content import and public read migration are completed.
- `NOW-ADMIN-CONTENT-CRUD-001` Projects publish-readiness UX checkpoint: `/admin/projects` now shows explicit publish blockers before Publish is attempted, covering project proof review, required summary/lead copy, fact proof review, and material proof review. Blocker rows are clickable so editors can move directly to the field that needs an Approved for public use or Deferred / keep private decision.
- `NOW-ADMIN-CONTENT-CRUD-001` Products source checkpoint: `/admin/products` now supports product families, models, material defaults, and specs behind the admin gate, with loading, empty, validation, save, publish/archive, read-only, and error states. Public Product runtime remains static/file-backed until content import and public read migration are completed.
- `NOW-ADMIN-CONTENT-CRUD-001` Products publish-checklist UX checkpoint: `/admin/products` now has shared Draft/Published/Archived status language, status counts, list search, status filtering, a website-visibility rule, product/model status help, editor-facing Website URL key / Model website key labels, media selectors/previews that show Published in Media / Not published in Media, Search title/description fields instead of product SEO JSON, a Product Publish checklist that locks Publish until name, URL, short description, hero image, at least one published model with image, material defaults, and specs are ready, and a Model Publish checklist that locks model publication until website key, label, and image are ready.
- `NOW-ADMIN-CONTENT-CRUD-001` Articles source checkpoint: `/admin/articles` now supports article metadata and structured article-section rows behind the admin gate, with loading, empty, validation, save, publish/archive, original-import provenance, reference linking, read-only, and error states. Public Article detail now prefers Published Supabase `article_blocks` and keeps sanitized legacy HTML from the original import fallback path when needed.
- `NOW-ADMIN-CONTENT-CRUD-001` Articles UX baseline checkpoint: `/admin/articles` now has shared Draft/Published/Archived status language, status counts, list search, status filtering, and a website-visibility rule in the article editor.
- `NOW-ADMIN-CONTENT-CRUD-001` Articles form-authoring UX checkpoint: `/admin/articles` now uses Website URL key, URL/date list labels, article status help, Migration note/link labels, Article sections language, media selectors/previews for article cover and section image fields, Search title/description fields instead of article SEO JSON, and block-type-specific section forms for rich text, image/gallery notes, quotes, FAQ, CTA, project/stone references, comparison notes, proof metrics, video embeds, and callouts instead of direct block content JSON entry.
- `NOW-ADMIN-MEDIA-LEADS-001` Leads source checkpoint: `/admin/leads` now supports enquiry and sample request queues behind the admin gate, with contact detail, sample item inspection, status, assignment, internal notes, email delivery, change-history-gated visible-queue CSV export, read-only, and error states. Live lead row creation, notification emails, export change-history rows, and save/export verification remain pending credentials.
- `NOW-ADMIN-MEDIA-LEADS-001` Leads UX baseline checkpoint: `/admin/leads` now has inbox search plus kind/status filters so owner/admin users can work leads by enquiry/sample type and workflow state.
- `NOW-ADMIN-MEDIA-LEADS-001` Leads workflow-guidance checkpoint: `/admin/leads` now translates website source, email delivery, website spam-check, team-role, and export CSV values into editor-facing labels, and shows Recommended next step guidance for enquiry and sample-request statuses before save.
- Change history source checkpoint: `/admin/audit` now supports Website owner / CMS manager history inspection behind the admin gate, with area/action filters, friendly action/area/detail labels, restricted-role, empty, and error states.
- Admin activity-log source checkpoint: Settings, Media, Stone Library, Projects, Products, Articles, and Leads save flows now call `src/lib/adminAudit.ts` after successful primary mutations. Activity-log insert failures are surfaced in the success notice with owner/admin follow-up language without rolling back the saved content. Live activity row verification remains pending credentials.
- Admin dashboard editor-start checkpoint: `/admin` dashboard now renders Start here quick actions, content status rows with correct module links, editor-facing module-card handoff labels, editor-facing health checks for media metadata/proof review/product/article media/Stone confirmation/stale leads, and handoff guidance for creating CMS access plus walking through real content; source coverage fails if those checks disappear.
- Leads visible-export checkpoint: `/admin/leads` now exports only the current search/filter result, shows filtered-versus-total visible counts, uses editor-facing CSV headers, and records active filter/search state plus total loaded rows in the export change-history metadata.
- Admin CMS IA/UX baseline checkpoint: `/admin` navigation is grouped into Work queue, Content library, and Operations; Dashboard now explains the Draft -> Review -> Publish workflow, shows Draft/Published/Archived counts for content modules under What the website can show now, labels published counts as Live on website, explains Published can appear on website / Draft is safe to edit / Archived is hidden but kept, and uses customer-enquiry/visible-review-task empty states instead of row/Supabase/service-role wording; shared CMS status primitives describe that only Published content can appear on public pages; and `/admin/projects` now has search/status filtering plus clearer readiness labels in the project list/editor.
- CMS team access management and hardening checkpoint: `/admin/settings` now includes non-destructive CMS access create/update controls for existing login accounts, duplicate account/email form validation, Website owner/self-lockout guardrails, role explanations, and profile audit-event attempts. Supabase `admin_profile_owner_hardening`, `security_definer_function_grants`, `security_definer_private_helpers`, and `admin_profile_email_uniqueness` are applied and verified; public helper RPC execution is revoked from browser roles, RLS/Storage policies call private helpers, profile emails are case-insensitively unique, and live team-management verification now waits on authenticated owner/admin browser QA.
- Settings UX baseline checkpoint: `/admin/settings` now uses shared CMS status language for the default settings row and explains which fields change public website settings versus CMS team access.
- Settings footer UX checkpoint: `/admin/settings` now edits footer columns/items through form controls for text, internal links, and external links instead of raw JSON.
- Settings team-access UX checkpoint: `/admin/settings` now supports Invite and grant access for new CMS users through the protected `/api/admin/invite-user` server Function, keeps Grant existing login as the setup-code fallback, checks existing CMS access before sending an invite email, shows People and access plus an Access setup checklist, includes Copy setup code controls, shows Website owner / CMS manager / Editor / Viewer role descriptions in a Role guide, shortens setup codes in the list, uses Active access language, and uses editor-facing validation copy for duplicate account access, self-lockout, and Website owner protection.
- Admin editor guide checkpoint: `docs/ADMIN_EDITOR_GUIDE.md` is the current customer-facing `/admin` usage guide, including address, account setup, edit/publish flow, module coverage, public fallback behavior, and remaining handoff gaps.
- Admin CRUD coverage checkpoint: `npm run agent:admin-crud-coverage` is available as a source-only verifier for admin routes, active modules, table coverage, role gates, publish/archive controls, activity-log actions, and activity-logged exports before live credential/browser QA.
- Admin scaffold cleanup checkpoint: the retired admin module scaffold component and unused scaffold/locked module branches are removed; all launch-critical admin routes now point to real source screens behind the auth/config gate.
- Admin CRUD live verifier checkpoint: `npm run agent:admin-crud-live` is available as a credential-gated live write verifier. It defaults to no-write plan-only mode; `--allow-writes` uses browser-key RLS with a real owner/admin session to create tagged QA rows, publish then archive public-facing QA rows, record audit events, verify tagged archived public-content QA rows plus private lead QA rows are not anonymously visible, verify optional private Storage uploads can be read back by the signed-in admin and are denied to anonymous browser-key reads, and avoid physical deletes.
- Admin dashboard health live-verifier checkpoint: `npm run agent:admin-crud-live` now stages readback of the dashboard health predicates against tagged QA rows before archive cleanup, covering media metadata gaps, project/project-fact claim review, missing Product/Article media, TBC Stone Library records, and stale new leads in the eventual live run.
- Admin CRUD state-coverage checkpoint: `npm run agent:admin-crud-coverage` now fails if mutating admin screens lose validation feedback paths or save paths, and fails if content/media lifecycle screens lose publish/archive save paths or published/archived state controls.
- Cloudflare readiness checkpoint: `npm run agent:cloudflare-readiness` verifies the repo-side Pages build contract, clean-route fallback, `/api/*` Function routing scope, launch headers, API handler files, env placeholders, and deployment runbook without touching Cloudflare account state.
- Cloudflare preview smoke checkpoint: `npm run agent:cloudflare-preview-smoke` now verifies deployed direct-refresh public/admin route shells, recursively discovered deployed assets and route chunks, deployed admin config/profile-gate bundle markers, browser service-role env boundaries, legacy redirects, and no-write API safe-failure behavior including malformed JSON after a `*.pages.dev` URL exists. Local Vite preview mode validates route/asset/bundle checks and skips Cloudflare-only redirect/Function checks.
- Content import dry-run checkpoint: `npm run agent:content-import` maps current static content and local media into Supabase-shaped draft import candidates. The latest run produced zero warnings/blockers and confirmed the import can be staged without writing production rows.
- Content import artifact checkpoint: the dry run now supports `--out` for a local ignored JSON review artifact, `--plan-out` for a local ignored Markdown apply/rollback plan, `--preflight-sql-out` for a local ignored read-only target preflight SQL artifact, `--apply-sql-out` for a guarded draft import SQL artifact, `--rollback-sql-out` for a guarded draft rollback SQL artifact, and `.tmp/` is ignored so review payloads are not accidentally committed.
- Stone Library finish-image import checkpoint: the content import dry run now extracts current static finish-specific Stone Library image mappings from `src/data/stoneFinishImages.ts` into 53 draft `stone_finish_images` rows and includes them in the apply/rollback plan, preflight SQL, guarded draft apply SQL, and public-readiness count checks.
- Article structured import checkpoint: the content import dry run now extracts legacy newsletter HTML into 95 draft structured article blocks, links image blocks to `media_assets`, skips newsletter footer/contact/social artifacts, and carries claim-review flags without treating source copy as approved.
- Public Supabase readiness checkpoint: Stone Library import candidates now stay `draft` instead of inheriting active/TBC public source states, and `npm run agent:public-supabase-readiness` verifies draft-only import rows, structured article block extraction, guarded draft apply SQL safety, guarded rollback SQL safety, published-only public RLS source, read-only anonymous grants, Article public structured-block rendering with legacy fallback, public runtime boundaries, and `/api/*` Function routing scope.
- Public Supabase Data API grant checkpoint: content import preflight SQL now includes role/sequence grant inspection for `anon`, `authenticated`, and `service_role`, and `npm run agent:public-supabase-readiness` guards that source contract so future import reviews do not rely on RLS/policies alone.
- Public Supabase merge-gate checkpoint: generated preflight SQL now reports existing target parent natural-key matches, and generated guarded draft apply SQL now requires `urblo.import_merge_approved=true` in addition to `urblo.import_approved=true` whenever matching parent keys already exist. `npm run agent:public-supabase-readiness` fails if the merge approval gate or parent key checks disappear.
- Public Supabase article-block readiness guard: the public-readiness gate now fails if article block import regresses to placeholder blocks, imports newsletter footer/contact/social artifacts, loses image-to-media candidate links, or loses claim-review metadata on rich text and flagged blocks.
- Admin Article structured-authoring coverage: `npm run agent:admin-crud-coverage` now fails if `/admin/articles` loses approved schema block type options, structured-content validation, or starts introducing raw HTML/newsletter authoring helpers.

Older completion details live in `docs/WORKLOG.md`.

## Exit Criteria for Current Delivery Cycle
- Active `now` tasks in `docs/agent/tasks.json` are complete or explicitly deferred.
- Cloudflare Pages can deploy production and preview builds.
- Supabase schema, Auth, Storage, and RLS assumptions are implemented and verified.
- Contact and Sample Request persist records and send notifications.
- Admin users can CRUD Projects, Stone Library, Products, Articles, media, and lead records without code edits.
- Priority media no longer depends on old WordPress URLs for first-viewport production experience.
- All applicable quality gates pass per `docs/agent/verification.md`.
- Navigation and route contracts remain consistent with `src/App.tsx`, `src/data/siteChrome.ts`, and `src/components/site/SiteFooter.tsx`.
- Delivery shell is free of template/default app metadata and dead social links.
- Stone Library data/image follow-ups are explicit, not implied complete.

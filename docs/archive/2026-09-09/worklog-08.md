## Entry Template (Use for Every Future Session)

### Date
- `YYYY-MM-DD`

### Scope
- What changed in this session.
- Why it changed.

### Changed Files
- Repo-root relative file path list only.

### Verification Results
- `npm run build`: pass/fail (+ key notes)
- `npm run lint`: pass/fail (+ key notes)
- `npx tsc -b`: pass/fail (+ key notes)

### Risks and Gaps
- Open defects, unresolved tradeoffs, blocked items.

### Next Handoff
- Exact task IDs from `NEXT_STEPS.md` to run next.
## 2026-08-12 — Project material points use Stone Library as source (local candidate)

- Scope: aligned Admin Project material schedules, visual map points, draft preview, public Project material cards, and Stone Library deep links around one canonical `stone_group_id + stone_variant_id + finish_definition_id` reference.
- Runtime: Admin loads published Stone variants, finish capabilities, finish-image links, and every referenced finish media row; Stone/Variant/Finish controls are dependent, Project-specific material/point image and point-title inputs are removed, and normalized saves clear the legacy overrides. Project map photos, coordinates, application, and project notes remain Project-owned.
- Public: Project hotspots and Featured Materials load current Published Stone Library detail (with static availability fallback), resolve the selected variant/finish image and alt, and deep-link with `variant`/`finish` query state. Project-owned legacy images no longer win.
- Database candidate: `supabase/migrations/20260812053204_project_materials_stone_library_source.sql` adds indexed `project_materials.stone_variant_id`, backfills only uniquely supported legacy stone/finish pairs, and adds a private publish-synchronization trigger that rejects cross-stone or unsupported variant/finish combinations. Legacy image/title columns are retained for rollback. The migration is source-only and has not been applied to production.
- Verification so far: `npm run build`, `npm run lint`, `npx tsc -b`, and `npm run agent:admin-projects-aggregate` pass. In-app Browser local QA at desktop and 390×844 verified Moon Gate point switching, canonical stone/finish imagery, Stone Library deep-link selection, no console warnings/errors, and no horizontal mobile overflow. This is implementation evidence, not a production write or user acceptance.
- Next: finish Harness/contract checks and clean-container gate, then request separate approval before any production migration apply/readback. Production Project edits remain untouched.

## 2026-08-12 — Draft Stone Library references corrected for Project editing

- Read-only production inspection confirmed that Moon Gate material `4` maps uniquely to Angola Black variant `5` / Polished / finish image `24`, and material `5` maps uniquely to New Grey variant `13` / Flamed / finish image `4`. The Stone groups, variants, finish-image links, and media assets exist but are Draft; the earlier zero-candidate result came from a Published-only query, not missing content.
- Admin Project editing now exposes every non-Archived Stone and Variant, labels Draft versus Live choices, and previews non-Archived canonical finish imagery. Draft content therefore remains editable without weakening public visibility.
- The unapplied compatibility migration now backfills a unique non-Archived supporting Variant. The publish boundary remains strict and now explicitly requires a Published Stone, Variant, Finish, finish-image link, and linked media asset.
- Verification: `npm run agent:admin-projects-aggregate`, `npm run build`, `npm run lint`, `npx tsc -b`, and `git diff --check` pass. No production database write, content status change, deployment, or email occurred.
- Commit `f7b8d70` passed the clean Docker Node 20 gate and was pushed to draft PR `#23`. Cloudflare Preview deployment `0e8e4fa0-fd4d-41c8-a560-c9c49b73108a` at `https://0e8e4fa0.urblo.pages.dev` passed the full no-write route, recursive asset MIME/body, redirect, safe-failure form, bundle-secret, and protected Admin Projects Function smoke. This is Preview evidence only; the production migration and runtime promotion remain unapplied.
- After Jay's separate migration-only approval, production migration `project_materials_stone_library_source` was applied once as version `20260812062636`. Readback confirmed the nullable bigint column, `ON DELETE SET NULL` FK, covering index, enabled private trigger, search-path-pinned SECURITY DEFINER function, and no function execute grant for `PUBLIC`, `anon`, or `authenticated`.
- The migration backfilled Moon Gate material `4` to Angola Black variant `5` / Polished and material `5` to New Grey variant `13` / Flamed. Both Project/material rows and both Stone/Variant chains remain Draft, both capabilities remain `yes`, and the invalid variant-link count is zero. No Project/Stone/image status, email, Storage object, or runtime deployment was changed.
- Post-migration Advisors reported no migration-specific security warning. Existing warnings remain for the intentional public archived-slug function and disabled leaked-password protection; existing performance notices remain, while the newly created variant index is expected to report unused immediately after creation.
- After Jay's separate runtime-promotion approval, PR `#23` was marked ready and squash-merged as `0593389d3edc28778e9d08fdaa4c254479fb0c44`. Cloudflare production deployment `b160e33f-1ca2-4649-9acf-bd67a137f336` at `https://b160e33f.urblo.pages.dev` passed the full route/asset/MIME/redirect/Function/bundle-boundary smoke.
- `https://urblo.com.au` and `https://www.urblo.com.au` both passed deployment-bound smoke against the immutable `b160e33f` origin with matching root asset identity and byte/MIME equality. Production Admin login then passed all nine authenticated routes plus the blocked-Supabase static fallback check. These are release-integrity checks, not Jay's fool-test acceptance; no content or Storage write occurred.

## 2026-08-17 — Project responsive image delivery Preview

- Project uploads remain unchanged original-quality masters. Public CMS Project cards, list rows, detail heroes, detail blocks, and hotspot images now request separate responsive Supabase-rendered WebP profiles; static/local and unrelated external images remain unchanged, and failed transformations fall back once to the retained original.
- The Admin inline Project uploader now says that the original is kept at full quality and high-quality website versions are prepared for each screen. It does not describe transfer-byte reduction as destructive compression.
- Production read-only audit found 72 unique published Project Storage images totaling 390.1 MB of source data; 70 exceed 1 MB, 48 exceed 5 MB, and the largest source is 9.83 MB. A representative 4.84 MB source produced a 960px quality-82 card response near 179 KB and a 2500px quality-88 hero response near 1.39 MB while retaining the original.
- Commits `c383983` and `9209d65` were pushed to draft PR `#24`. Cloudflare Preview deployment `f9122a0d-7bfa-44b4-ab5a-bd229f8f35f3` at `https://f9122a0d.urblo.pages.dev` is bound to exact commit `9209d65905049e0460a27e6cd9f28b1f5a1d8c19` and completed successfully.
- `npm run agent:cloudflare-preview-smoke -- --base-url https://f9122a0d.urblo.pages.dev`: pass for public/Admin route shells, recursively discovered asset MIME/body integrity, redirects, safe-failure form Functions, browser-secret boundary, and the protected Admin Projects Function boundary.
- In-app Browser Preview QA: the desktop Project archive selected a 960px quality-82 CMS card rendition; the 1280px West Side Place hero selected 2500px quality-88 with high fetch priority; the 390px hero selected 960px quality-88. Desktop and mobile images completed, the 390px document had no horizontal overflow, and warning/error logs were empty.
- Host build, lint, typecheck, public overlay, Admin Projects aggregate, smoke, Admin predeploy/config, Harness checks, Harness GC, diff check, and the clean Node 20 container gate pass. The only retained worktree items outside scope are the user's untracked SEO brief and invoice PDF.
- Production has not changed. No Storage object, CMS content, Supabase configuration, database row, migration, email, custom domain, or production deployment was written. Jay's visual acceptance of the Preview remains the next release boundary; agent checks do not self-certify it.
- Jay then reported unexplained black bands beneath some desktop Grid thumbnails. Rendered inspection proved the transformed files and originals were sound: the card's implicit two-row Grid distributed equal-row stretch into the black media wrapper when neighboring cards had different copy heights, while the image itself remained fixed at 4:3. The issue did not reproduce in the fixed 120×90 List layout.
- Commit `c1920e1` assigns an intrinsic media row plus flexible content row, makes the media wrapper itself 4:3, and has the image fill that frame. The public source verifier now locks both layout conditions so future copy-length changes cannot restore the black gap.
- Cloudflare Preview deployment `aa70ae54-2d3b-4440-bf11-799114226fec` at `https://aa70ae54.urblo.pages.dev` is bound to exact commit `c1920e132561296014d657b9f06f3b2b4c55f7f2`; full Preview smoke passes. Cloud Browser readback measured the first five desktop Grid images and wrappers at equal 348×261, List images and wrappers at equal 120×90, and the 390px Grid image and wrapper at equal 342×257 with 390px document/client widths and no warning/error logs. Hover scaling remained visible without exposing the wrapper.
- Jay reviewed the combined responsive-image and black-bar Preview and explicitly accepted it. PR `#24` was marked ready and squash-merged as `d51fa26b2452a68c97186f035c489edce201e1fe`.
- Cloudflare production deployment `4242d7c5-e78e-4f70-b6c2-51e9b9bb7dc8` at `https://4242d7c5.urblo.pages.dev` is bound to that exact merge commit. Immutable smoke passed. The first immediate apex/`www` probe caught the new HTML referencing an asset not yet present at the moving edge and correctly failed on SPA-HTML fallback; after edge propagation completed, both custom domains passed deployment-reference byte/MIME comparison, every route/asset/redirect/Function check, the browser-secret boundary, and protected Admin Projects Function boundary.
- Production Browser readback at `https://urblo.com.au` measured the first five desktop Grid image/frame pairs at equal 348×261 with no black remainder. West Side Place selected the 2500px quality-88 hero at 1280px and the 960px quality-88 hero at 390px; the mobile document/client widths were both 390px and warning/error logs were empty.
- Release did not mutate any Project content, Storage object, Supabase configuration, database row, migration, Auth user, or email. Uploaded originals remain retained as the visual masters.

## 2026-08-17 — Homepage Published Projects release candidate

- Root cause: Homepage Latest Projects used a separate five-record static array, so publishing a Project through Admin could update `/projects` without ever adding that Project to Homepage. The rail itself had no five-item layout limit.
- Runtime: the section now requests the same public Project collection as `/projects`, preserves its CMS `sort_order`, and maps all returned records into the existing selected-project browser. The read begins only when the section nears the viewport so the Project/Supabase path does not compete with the Homepage first viewport. The controlled five-record set remains the read-failure/no-config fallback.
- Interaction repair: replacing fallback records with CMS records caused browser scroll anchoring to keep the former first item in view while the new first item sat offscreen. The rail now resets to zero with active index zero after the replacement commits.
- Local Browser QA: desktop read all 15 current public Projects, selected Caulfield Grammar School as `15 / 15`, and linked to `/projects/caulfield-grammar-school`. At 390×844 the section remained 844px tall, the rail stayed horizontally scrollable, the first active Project was visible at `scrollLeft = 0`, and the document had no horizontal overflow. Relevant warning/error logs were empty.
- Host verification passes: `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:smoke`, `npm run agent:check`, and `git diff --check`. Commit `ae9d3fa` passes the clean Docker Node 20 gate. Browserslist staleness notice remains. Preview and production checks are pending.
- Production is unchanged. No Project record, status, Storage object, Supabase configuration, migration, Auth user, email, Cloudflare domain, or production deployment was written.
- After PR `#26` merged as `f3fe5647` and production deployment `080a0a45` completed, immutable/`www` deployment-bound smoke passed; the first apex pass caught one not-yet-propagated asset and the immediate rerun passed after edge convergence. Production Browser QA confirmed all 15 Projects and the last-project detail link. It also exposed a return-path visual edge case: after selecting the last desktop item, entering its detail, and returning to Homepage at mobile width, browser scroll anchoring could retain the old fallback item while the active CMS item reset to index one. Follow-up commit `fcb80dc` disables scroll anchoring on the rail; the exact local return path now ends at `01 / 15`, `scrollLeft = 0`, with the active first card visible and no horizontal overflow. Host checks and the clean Docker Node 20 gate pass; PR `#27` Preview and promotion remain pending.
- Follow-up Preview `0c1595d7` passed full deployed smoke and the exact desktop-final-item → detail → 390×844 Homepage return path. PR `#27` merged as `ccab9c12`; production deployment `b85d8718-18ae-4180-8a2c-371c63e18a31` succeeded. Immutable smoke passed; the first apex/`www` probes observed the normal new-asset propagation window, then both custom domains passed deployment-reference route, recursive byte/MIME asset, redirect, Function, protected-function, and browser-secret-boundary checks.
- Final production Browser QA repeated the exact edge path: all 15 records loaded, Caulfield Grammar School opened from `15 / 15`, and returning at 390×844 rendered visible `01 / 15` with `overflow-anchor: none`, `scrollLeft = 0`, section height 844px, no horizontal document overflow, and no warning/error logs. Jay's accepted Homepage behavior is now live; no Project content, status, Storage object, database row, Supabase configuration, migration, Auth user, or email was changed.

## 2026-08-18 — Admin Image QR local implementation candidate

- Added a simple `/admin/image-qr` workspace for bulk image intake, quality-first browser optimization, one permanent QR per image, preview, rename, replace-without-changing-QR, copy/open link, PNG/SVG QR download, and Hide/Restore. There is no review or publish workflow.
- Added a protected Admin Function boundary for resource management and a public `GET`/`HEAD /image/:slug` resolver that redirects an active stable slug to its current versioned public image. The private temporary upload is validated by owner path, image signature, MIME, dimensions, and byte count before public promotion; write paths remain owner/admin/editor only and Viewer remains read-only.
- Added migration source `supabase/migrations/20260818074046_image_qr_resources.sql`. The table is RLS-enabled and service-role-only; browser roles receive no direct table access.
- Desktop and 390×844 in-app Browser QA used local non-production fixture data only, then the fixture path was removed. It verified the layout, filters and rename interaction, no console warning/error, and zero horizontal overflow. A hidden 1024px QR download canvas that initially expanded document width was corrected to an explicit 1×1 rendered footprint.
- Host verification passes: `npm run build`, `npm run lint`, `npx tsc -b`, `npm run agent:admin-image-qr`, `npm run agent:admin-crud-coverage`, `npm run agent:cloudflare-readiness`, `npm run agent:smoke`, `npm run agent:admin-config-gate` (12 routes), and `npm run agent:check`. Commit `842c11e` also passes the clean Node 20 container gate. Browserslist staleness notice remains.
- After Jay's separate approval, production migration `20260818074046_image_qr_resources` was applied once and read back. The table has all 13 expected columns, four expected indexes, the `set_updated_at` trigger, RLS enabled, zero policies, zero rows, no `anon`/`authenticated` CRUD privileges, and full `service_role` CRUD. Advisors added only the intentional INFO notice for RLS-with-no-policy plus low-priority created/updated-user FK index and unused-new-index notices; there is no migration-specific security WARN. No Image QR row, Storage object, Auth user, email, Cloudflare deployment, or custom-domain change occurred. Next boundary is Preview/no-write smoke, followed by separate approval for any tagged resource write.
- Branch `codex/admin-image-qr-library` was pushed and opened as draft PR `#29`. Cloudflare immutable Preview `https://35044c9d.urblo.pages.dev` passed full no-write public/Admin route, recursive asset MIME/body, redirect, safe-failure Function, browser-secret, protected Projects Function, and protected Image QR Function smoke. The production Image QR table remained empty; no Storage object, resource row, audit row, email, custom domain, or production runtime was changed. A tagged upload/replace/Hide/Restore workflow remains a fresh approval boundary and Jay's fool test remains the acceptance boundary.

## 2026-08-18 — Image QR scalable library and approved 18-image intake

- External intake contained 18 distinct PNG files, all 6585×4390, totaling 209.0 MB; names represented nine Stone families and their Finish variants. No exact file hash was duplicated.
- Replaced the repeated full QR-card grid with a compact searchable/sortable resource list. A row opens a focused details drawer containing the large preview, rename, permanent link, QR, PNG/SVG downloads, replace, and Hide/Restore. The bulk intake area is shorter, duplicate normalized names are rejected, and trailing `_Urblo`/numeric filename suffixes normalize to `Stone · Finish` names without changing hyphenated Finish names.
- Jay approved importing the supplied files. The authenticated immutable Preview workflow processed all 18 successfully: every output is 2560×1707 WebP at quality 90, ranging from 123 KB to 375 KB. It created 18 active `image_qr_resources` rows, corresponding public Storage objects, and audit records. One pre-existing active `silver ash` resource was discovered and left untouched, so authenticated readback is 19 active / 0 hidden. No replace, Hide, Restore, delete, Auth, email, migration, or custom-domain write occurred.
- Preview inspection caught a permanent-link defect before handoff: resources managed on an immutable Preview initially rendered Preview-domain QR values. Commit `fa9fe7d` now makes every generated value canonical `https://urblo.com.au/image/:slug`; resource rows do not store the origin, so the imported slugs required no data rewrite.
- Commits `7b050cf` and `fa9fe7d` pass host build/lint/typecheck/Image QR checks and the clean Node 20 container gate. Final immutable Preview `https://285f55f9.urblo.pages.dev` passes full route/asset/MIME/redirect/Function/secret-boundary smoke. Authenticated desktop and 390×844 in-app Browser QA verified all 19 rows, Name A–Z sorting, focused details/QR actions, canonical URL rendering, responsive layout, and clear large-image output. This is implementation evidence, not Jay's usability acceptance.
- Production runtime remains unchanged. Until PR `#29` is approved and promoted, the canonical production `/image/:slug` resolver is not live; Jay must test the Preview before production promotion.
- Jay then explicitly approved production promotion with `上线`. PR `#29` squash-merged as `19e2b41e6d6316ae70f2f25ad97f7c1fe49feaea`; Cloudflare production deployment `d6816c71-b33e-46ae-860d-121a81c8bca0` completed successfully. The immutable deployment initially returned the SPA shell for one Function during the normal propagation window, then passed on retry. `www` passed deployment-bound smoke on its first post-switch run; apex initially served one new asset path as SPA fallback, then passed the complete immutable-reference check after edge convergence.
- Final release evidence: the immutable deployment, `https://urblo.com.au`, and `https://www.urblo.com.au` pass route, recursive asset byte/MIME, redirect, form Function, protected Projects/Image QR Function, and browser-secret-boundary smoke. Authenticated production `/admin/image-qr` reads 19 active / 0 hidden resources. Opening `https://urblo.com.au/image/zen-grey-honed-d4aa3d2c` redirects to the expected public Storage WebP and renders at 2560×1707. No resource, Storage, Auth, email, migration, or custom-domain mutation occurred during release itself.

## 2026-08-21 — Capability module 04 image replacement candidate

- Jay supplied a new factory quality-review photograph and scoped the replacement only to `04 / Multi-material assemblies`; the later `The Urblo advantage` image must remain unchanged.
- The supplied 960×1280 JPEG was converted into a dedicated 960×720, 4:3, 285 KB launch asset with a subject-aware crop retaining the Urblo vest, helmet, inspection action, hand, and stone interface. Module 04 now references that independent asset and has matching alternative text; the existing `site-install-review.jpg` path remains in use only by the later advantage section.
- Local in-app Browser QA at 1280×900 and 390×844 confirmed the intended image in module 04, the original later image still present once, no horizontal mobile overflow, and no relevant warning/error logs. Host build, lint, typecheck, Capability source contract, full agent smoke, and Harness checks pass.
- Commit `2f44eeb` passed the clean Node 20 container gate and was pushed to draft PR `#31`. Immutable Cloudflare Preview `https://fd91c147.urblo.pages.dev` passes the full public/Admin route, recursive asset MIME/body, redirect, safe-failure Function, browser-secret, and protected Admin Function smoke.
- Preview Browser QA at 1280×900 and 390×844 confirmed exactly one new module image and one unchanged `site-install-review.jpg` image, complete 960×720 delivery, intended desktop/mobile crop, zero horizontal mobile overflow, and no warning/error logs. Production is unchanged; Jay's visual acceptance and separate promotion instruction remain the release boundary.
- Jay explicitly approved production promotion with `上线`. PR `#31` squash-merged as `2e5f9b83d9441066c2dc31f79a3448fb226ad421`; Cloudflare production deployment `bf83bbc2-9c9f-4e1c-8e98-d32f0547c701` completed successfully.
- The immutable deployment, `https://urblo.com.au`, and `https://www.urblo.com.au` pass deployment-bound route, recursive asset byte/MIME, redirect, Function, protected Admin Function, and browser-secret checks. Production Browser QA at 1280×900 and 390×844 confirmed exactly one new module image and one unchanged later image, complete 960×720 delivery, intended crop, zero mobile overflow, and no warning/error logs. No Supabase row, Storage object, CMS content, Auth user, migration, email, or custom-domain configuration changed during release.

## 2026-09-02 — Homepage Acknowledge clear-glass repair candidate

- Root cause: the first-visit Homepage Acknowledge panel still carried `bg-black/88` from May. Tailwind did not originally emit that non-standard opacity, but the August opacity-scale expansion made it a real 88% black layer; the unchanged class therefore surfaced later as an almost solid black block.
- Source now uses the accepted Homepage Header overlay material: `bg-black/50` with the existing light `backdrop-blur-sm` (4px). Position, copy, timing, close interaction, border, and shadow remain unchanged.
- Public smoke now requires the clear tint/light blur pair and rejects the legacy `/88` class. `docs/DESIGN.md` records the first-visit panel as part of the Homepage clear-glass family.
- Local production-build Browser QA passed at 1280×900 and 390×844. In both viewports the panel and Homepage Header compute to `rgba(0, 0, 0, 0.5)` with `blur(4px)`; mobile measures 342px wide with 24px gutters and no horizontal overflow. Close fades and unmounts correctly, and both viewports have no warning/error logs. The temporary 30-second QA timeout was restored to the production 3-second behavior before final gates.
- Host build, lint, typecheck, smoke, Harness check, diff check, and the clean Node 20 container gate pass. GitHub Actions run `33593905108` deployed exact commit `d30fc07` to immutable Cloudflare Preview `https://943e1bb7.urblo-site.pages.dev`; full route/asset/MIME/redirect/Function/browser-secret smoke passes. The remote first visit mounted the panel and confirmed the expected compiled bundle; local production-build QA remains the durable desktop/mobile computed-style and interaction evidence because the intended three-second timeout closes during remote automation capture.
- Jay reviewed the Preview and explicitly accepted it with `没问题` on 2026-09-02. PR `#34` squash-merged as `e298d06f42c9462ce02c22b6c042b524e9c25500`; GitHub Actions run `33594840823` passed and deployed immutable Cloudflare production `https://737e702b.urblo-site.pages.dev`.
- The immutable deployment, `https://urblo.com.au`, and `https://www.urblo.com.au` all pass the complete deployment-bound route, recursive asset byte/MIME, redirect, form Function, protected Admin Function, and browser-secret smoke. No Supabase row, Storage object, Auth user, migration, email, DNS record, or custom-domain configuration changed during this release.

## 2026-08-26 — Cloudflare ownership migration

- Audited the Hunter Cloudflare account before migration. Urblo used the `urblo` Pages project and the `urblo.com.au` zone; unrelated R2 buckets `grace-backups` / `inkk` and Workers `inkk-blog-starter` / `inkk-dot` are not Urblo resources and were left untouched. No relevant Urblo KV, D1, Queue, Vectorize, Hyperdrive, Worker route, or Tunnel dependency was found.
- Deployed current `main` commit `004445b3b654ec6db0dbcd78a0a7c65b83665d09` to Urblo-owned account `cb91e0806b808258d6d9c852c8d0f060`, Pages project `urblo-site`, immutable URL `https://42cabb15.urblo-site.pages.dev`. The clean Node 20 container gate passed before upload. Full immutable Preview smoke passed; authenticated Admin login covered ten routes, and read-only `/api/admin/image-qr` returned 32 resources, proving the new project has its required Supabase server binding. No form submission or email was sent.
- Rebuilt the new Cloudflare zone as 24 intended records. Removed only ten erroneous auto-scanned Cloudflare proxy-IP A/AAAA drafts before activation; retained and verified five Google MX records, eight TXT records, and the tracking CNAME, then added website, qa, SMTP2GO, SendGrid, and Squarespace records. Three stale in-zone `ns1/2/3.nameserver.net.au` records from the old zone were intentionally not migrated. SendGrid DKIM CNAMEs `1._domainkey` and `12._domainkey` are DNS-only in the new zone, which is the valid configuration.
- Attached `urblo.com.au` and `www.urblo.com.au` to `urblo-site`; both report `Active / SSL enabled`. Exact deployment-bound smoke passed for apex and `www` against `https://42cabb15.urblo-site.pages.dev`, covering public/Admin routes, recursively discovered asset bytes/MIME, redirects, safe-failure form Functions, protected Projects/Image QR Functions, and the browser secret boundary.
- Jay updated the registrar nameservers. Official `.au` WHOIS immediately read back `mckenzie.ns.cloudflare.com` and `norman.ns.cloudflare.com`, DNSSEC unsigned. Direct queries to the new authoritative server returned the expected nameserver pair, apex/www website answers, five Google MX records, SPF, DMARC quarantine policy, Google DKIM, four Google verifications, Squarespace verification/DKIM, SMTP2GO return/DKIM/tracking, SendGrid mail/DKIM, and `qa -> 159.198.65.164`. At the last readback, Cloudflare dashboard and Cloudflare/Google/local recursive resolvers were still showing propagation-pending/old cached delegation; Cloudflare's immediate nameserver recheck was requested.
- The Hunter-account Pages project and old zone were not deleted, preserving rollback evidence during propagation. The old zone's record IDs are not valid rollback IDs in the new zone. Preferred rollback is a known-good deployment inside the Urblo account; an emergency legacy-host rollback must be recreated in the new zone without changing mail, verification, nameserver, or qa records.
- Updated the deployment-bound smoke verifier to accept exact immutable origins under both historical `urblo.pages.dev` and current `urblo-site.pages.dev`; production/default aliases still require an independent immutable reference and self-comparison remains rejected.
- Cloudflare's immediate recheck completed: the new zone reports Active, the DNS page reports 24 of 200 records with no recommendations, and both apex and `www` are proxied to `urblo-site.pages.dev`. Google Public DNS returns the new nameserver pair; Cloudflare Public DNS and the local resolver still returned the old cached pair at that instant, which is expected until their prior NS TTL expires.
- After the zone became Active, both `https://urblo.com.au` and `https://www.urblo.com.au` again passed exact deployment-bound smoke against `https://42cabb15.urblo-site.pages.dev`. Production Admin authentication then passed the three blocked-Supabase public fallbacks plus all ten authenticated routes. These checks were read-only apart from the normal Auth session; no CMS/Supabase/Storage/form/email write occurred.
- Created an account-owned Cloudflare token named `Urblo GitHub Pages Deploy` with only Pages Write permission. An initially displayed token was immediately deleted before use; a replacement token was transferred directly into the GitHub repository secret without logging its value. GitHub repository secrets now hold the Cloudflare account ID/token and the browser-safe Supabase build inputs required by Vite.
- Replaced the legacy GitHub Pages workflow with branch-aware Cloudflare Pages deployment to `urblo-site`. Build, lint, typecheck, agent smoke, and no-write Admin predeploy contracts must pass before Wrangler uploads `dist`; non-main branches are Preview deployments and `main` is production. The isolated Admin configuration browser gate still passes locally across all 12 routes, but was intentionally kept outside the hosted CI critical path after the temporary GitHub browser environment proved slow and unreliable; it remains a required local predeployment gate for relevant Admin changes.
- Branch Actions run `32958807043` passed and deployed immutable Preview `https://d1f70070.urblo-site.pages.dev`; full deployed smoke passed. PR `#33` squash-merged as `aec4dce112873029de767239d7e7ee5ae88f7917`. Main Actions run `32959122724` passed and deployed immutable production `https://7e668551.urblo-site.pages.dev`; immutable, apex, and `www` deployment-bound smoke all passed.
- Audited the new zone after migration: SSL mode was upgraded from `Full` to `Full (strict)` and read back; HTTP origins resolve to HTTPS; custom WAF rules are empty; Worker routes are empty. Pages production variables include the expected Supabase service binding, browser-safe key, SMTP2GO key, and notification sender/recipient values. Turnstile remains intentionally unconfigured.
- After Jay explicitly approved the exact payloads, destination expectation, two production rows, and two real emails, the Urblo-owned Cloudflare production path passed a fresh tagged Contact/Sample Request proof with marker `urblo-live-20260826-cf-migration-01`. `/api/enquiries` created `enquiries.id = 10`; `/api/sample-requests` created `sample_requests.id = 6` and `sample_request_items.id = 6` with quantity `2`. Both responses returned `notificationStatus = sent`, independent Supabase readback confirmed both stored rows at `notification_status = sent`, and audit rows `admin_audit_events.id = 354/355` recorded the two create actions. The tagged rows remain for auditability. This proves the migrated Pages project has working Supabase persistence, audit writes, and SMTP2GO delivery; Turnstile remains intentionally unconfigured.

## 2026-09-02 — Public catalog integrity repair (local implementation)

- Jay reported five connected public-site problems: Homepage hero video stopped in mobile WeChat, several Product model buttons showed the wrong render, Stone Finish selection jumped to the top, Tuscany exposed finishes without source images and hid Cut selection too far from Finish, and Blueocean repeated one Sawn image across every finish.
- Read-only production/code audit found the video had regressed from H.264 Constrained Baseline to Main and could miss an already-fired Weixin bridge event while swallowing `play()` rejection; Product button state was correct but static and imported Draft image paths were wrong; global `ScrollRestoration` treated query-only selection as navigation; Tuscany capabilities claimed Polished/Sandblasted despite only cut-level imagery; Blueocean used one default Sawn fallback for eight finishes.
- Local implementation on `codex/fix-mobile-product-stone-integrity` re-encodes the 27-second portrait hero as Constrained Baseline level 3.1, yuv420p, 540x960, 30fps, no audio, fast-start, 2.91MB; handles existing and late Weixin bridges; and exposes an accessible `Play video` control only when playback is rejected or unexpectedly paused.
- Product mappings were visually audited for all 28 current renders. Prime Block, Prime Lume, and Strata Bench remain unchanged; Terra Line's four Timber mappings, Prime Curve's Flush/Rise+, and Terra Arc's two Rise/Flush pairs are corrected. A verifier locks all six families, keys, labels, reviewed paths, asset existence, and per-family uniqueness.
- Stone detail now restores scroll only for pathname changes, retains the current detail during Variant refresh, and places Cut direction before Finish in one right-side rail. Both Tuscany cuts expose only Honed in CSV/JSON and bind their own cut image. Blueocean explicitly maps Sawn; every other available finish has no image URL, no Zoom, and an honest pending state, including CMS-overlay protection against default-media repetition.
- Focused checks pass: `agent:homepage-video`, `agent:product-model-images`, `agent:stone-library-detail`, and `agent:check`. Local Browser QA passes desktop Homepage autoplay, 390px portrait source selection/autoplay/no overflow, Tuscany Cross Cut/Honed rendering with no top jump, Blueocean Honed pending/no image/no Zoom with query state preserved, and all 12 corrected Product button/image-path transitions without console warnings/errors.
- No Supabase data, Storage object, Cloudflare configuration, production runtime, or email was changed. Full clean gate, Cloudflare Preview smoke, Jay acceptance, and exact approval-gated synchronization of imported Product/Tuscany Draft rows remain.
- Jay clarified that Steel Blue and BlueOcean are the same material. The canonical source now keeps the `blueocean` route/key but replaces its content, sample choices, specification values, and finish photos with the complete former Steel Blue record; the separate `steel-blue` record and obsolete BlueOcean fallback asset are removed. The old `/stone-library/steel-blue` path redirects permanently to `/stone-library/blueocean`, and the duplicate URL is removed from the sitemap. Production CMS rows remain untouched pending an exact merge plan and separate approval.
- Commit `e1bdc3c` passes the clean Node 20 container gate and was pushed to draft PR `#36`. GitHub Actions run `33600619977` passed and deployed immutable Preview `https://085a75df.urblo-site.pages.dev`; full deployed route/asset/MIME/redirect/Function/browser-secret smoke passes. Preview Browser QA at desktop and 390px verifies Homepage mobile playback, Tuscany Honed-only/Cross Cut without a top jump, all corrected Terra Line transitions, one BlueOcean and zero Steel Blue cards across 12 canonical stones, seven distinct BlueOcean finish images, no horizontal overflow, and a real 301 from the retired Steel Blue URL. Production remains unchanged; Jay acceptance, iOS/Android WeChat, and approval-gated CMS synchronization/merge-delete remain.
- Jay accepted the Preview and explicitly approved production promotion. PR `#36` squash-merged as `57316515725e4a60a2bd305f26b0465214171c50`; GitHub Actions run `33601860941` passed and deployed immutable Cloudflare production `https://764dedbc.urblo-site.pages.dev`. The immutable deployment, apex, and `www` pass deployment-bound route, recursive asset byte/MIME, redirect, Function, protected-function, and browser-secret smoke. Production Browser QA at 390px confirms Homepage mobile video is playing from the mobile H.264 source, Stone Library has 12 cards with one BlueOcean and zero Steel Blue, BlueOcean exposes seven distinct finish images, Tuscany Cross Cut keeps Honed as the only selectable finish without a top jump, Terra Line's four buttons select the reviewed four paths, and no relevant warning/error or horizontal overflow appears. No Supabase row, Storage object, Auth user, email, migration, DNS, or Cloudflare configuration was changed; real iOS/Android WeChat and approval-gated CMS synchronization/duplicate-row merge remain.

# Keep the gate build context lean and force a clean in-container build.
.git
node_modules
dist
.tmp
.vite
*.log

# Never bake local env/secret files into the gate image; the gate is
# source-only and must reproduce the env-less Cloudflare Pages build.
.env
.env.*
!.env.example
.dev.vars
*.local
